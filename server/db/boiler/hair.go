// Code generated by SQLBoiler 4.8.6 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package boiler

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// Hair is an object representing the database table.
type Hair struct {
	ID        string    `boiler:"id" boil:"id" json:"id" toml:"id" yaml:"id"`
	Hue       string    `boiler:"hue" boil:"hue" json:"hue" toml:"hue" yaml:"hue"`
	ImageURL  string    `boiler:"image_url" boil:"image_url" json:"image_url" toml:"image_url" yaml:"image_url"`
	UpdatedAt time.Time `boiler:"updated_at" boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt null.Time `boiler:"deleted_at" boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`
	CreatedAt time.Time `boiler:"created_at" boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *hairR `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
	L hairL  `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
}

var HairColumns = struct {
	ID        string
	Hue       string
	ImageURL  string
	UpdatedAt string
	DeletedAt string
	CreatedAt string
}{
	ID:        "id",
	Hue:       "hue",
	ImageURL:  "image_url",
	UpdatedAt: "updated_at",
	DeletedAt: "deleted_at",
	CreatedAt: "created_at",
}

var HairTableColumns = struct {
	ID        string
	Hue       string
	ImageURL  string
	UpdatedAt string
	DeletedAt string
	CreatedAt string
}{
	ID:        "hair.id",
	Hue:       "hair.hue",
	ImageURL:  "hair.image_url",
	UpdatedAt: "hair.updated_at",
	DeletedAt: "hair.deleted_at",
	CreatedAt: "hair.created_at",
}

// Generated where

var HairWhere = struct {
	ID        whereHelperstring
	Hue       whereHelperstring
	ImageURL  whereHelperstring
	UpdatedAt whereHelpertime_Time
	DeletedAt whereHelpernull_Time
	CreatedAt whereHelpertime_Time
}{
	ID:        whereHelperstring{field: "\"hair\".\"id\""},
	Hue:       whereHelperstring{field: "\"hair\".\"hue\""},
	ImageURL:  whereHelperstring{field: "\"hair\".\"image_url\""},
	UpdatedAt: whereHelpertime_Time{field: "\"hair\".\"updated_at\""},
	DeletedAt: whereHelpernull_Time{field: "\"hair\".\"deleted_at\""},
	CreatedAt: whereHelpertime_Time{field: "\"hair\".\"created_at\""},
}

// HairRels is where relationship names are stored.
var HairRels = struct {
}{}

// hairR is where relationships are stored.
type hairR struct {
}

// NewStruct creates a new relationship struct
func (*hairR) NewStruct() *hairR {
	return &hairR{}
}

// hairL is where Load methods for each relationship are stored.
type hairL struct{}

var (
	hairAllColumns            = []string{"id", "hue", "image_url", "updated_at", "deleted_at", "created_at"}
	hairColumnsWithoutDefault = []string{"hue", "image_url"}
	hairColumnsWithDefault    = []string{"id", "updated_at", "deleted_at", "created_at"}
	hairPrimaryKeyColumns     = []string{"id"}
	hairGeneratedColumns      = []string{}
)

type (
	// HairSlice is an alias for a slice of pointers to Hair.
	// This should almost always be used instead of []Hair.
	HairSlice []*Hair
	// HairHook is the signature for custom Hair hook methods
	HairHook func(boil.Executor, *Hair) error

	hairQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	hairType                 = reflect.TypeOf(&Hair{})
	hairMapping              = queries.MakeStructMapping(hairType)
	hairPrimaryKeyMapping, _ = queries.BindMapping(hairType, hairMapping, hairPrimaryKeyColumns)
	hairInsertCacheMut       sync.RWMutex
	hairInsertCache          = make(map[string]insertCache)
	hairUpdateCacheMut       sync.RWMutex
	hairUpdateCache          = make(map[string]updateCache)
	hairUpsertCacheMut       sync.RWMutex
	hairUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var hairAfterSelectHooks []HairHook

var hairBeforeInsertHooks []HairHook
var hairAfterInsertHooks []HairHook

var hairBeforeUpdateHooks []HairHook
var hairAfterUpdateHooks []HairHook

var hairBeforeDeleteHooks []HairHook
var hairAfterDeleteHooks []HairHook

var hairBeforeUpsertHooks []HairHook
var hairAfterUpsertHooks []HairHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Hair) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range hairAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Hair) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range hairBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Hair) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range hairAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Hair) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range hairBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Hair) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range hairAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Hair) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range hairBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Hair) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range hairAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Hair) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range hairBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Hair) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range hairAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddHairHook registers your hook function for all future operations.
func AddHairHook(hookPoint boil.HookPoint, hairHook HairHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		hairAfterSelectHooks = append(hairAfterSelectHooks, hairHook)
	case boil.BeforeInsertHook:
		hairBeforeInsertHooks = append(hairBeforeInsertHooks, hairHook)
	case boil.AfterInsertHook:
		hairAfterInsertHooks = append(hairAfterInsertHooks, hairHook)
	case boil.BeforeUpdateHook:
		hairBeforeUpdateHooks = append(hairBeforeUpdateHooks, hairHook)
	case boil.AfterUpdateHook:
		hairAfterUpdateHooks = append(hairAfterUpdateHooks, hairHook)
	case boil.BeforeDeleteHook:
		hairBeforeDeleteHooks = append(hairBeforeDeleteHooks, hairHook)
	case boil.AfterDeleteHook:
		hairAfterDeleteHooks = append(hairAfterDeleteHooks, hairHook)
	case boil.BeforeUpsertHook:
		hairBeforeUpsertHooks = append(hairBeforeUpsertHooks, hairHook)
	case boil.AfterUpsertHook:
		hairAfterUpsertHooks = append(hairAfterUpsertHooks, hairHook)
	}
}

// One returns a single hair record from the query.
func (q hairQuery) One(exec boil.Executor) (*Hair, error) {
	o := &Hair{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: failed to execute a one query for hair")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Hair records from the query.
func (q hairQuery) All(exec boil.Executor) (HairSlice, error) {
	var o []*Hair

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "boiler: failed to assign all query results to Hair slice")
	}

	if len(hairAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Hair records in the query.
func (q hairQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to count hair rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q hairQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "boiler: failed to check if hair exists")
	}

	return count > 0, nil
}

// Hairs retrieves all the records using an executor.
func Hairs(mods ...qm.QueryMod) hairQuery {
	mods = append(mods, qm.From("\"hair\""), qmhelper.WhereIsNull("\"hair\".\"deleted_at\""))
	return hairQuery{NewQuery(mods...)}
}

// FindHair retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindHair(exec boil.Executor, iD string, selectCols ...string) (*Hair, error) {
	hairObj := &Hair{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"hair\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, hairObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: unable to select from hair")
	}

	if err = hairObj.doAfterSelectHooks(exec); err != nil {
		return hairObj, err
	}

	return hairObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Hair) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no hair provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.UpdatedAt.IsZero() {
		o.UpdatedAt = currTime
	}
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(hairColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	hairInsertCacheMut.RLock()
	cache, cached := hairInsertCache[key]
	hairInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			hairAllColumns,
			hairColumnsWithDefault,
			hairColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(hairType, hairMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(hairType, hairMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"hair\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"hair\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "boiler: unable to insert into hair")
	}

	if !cached {
		hairInsertCacheMut.Lock()
		hairInsertCache[key] = cache
		hairInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// Update uses an executor to update the Hair.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Hair) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime

	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	hairUpdateCacheMut.RLock()
	cache, cached := hairUpdateCache[key]
	hairUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			hairAllColumns,
			hairPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("boiler: unable to update hair, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"hair\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, hairPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(hairType, hairMapping, append(wl, hairPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update hair row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by update for hair")
	}

	if !cached {
		hairUpdateCacheMut.Lock()
		hairUpdateCache[key] = cache
		hairUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q hairQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all for hair")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected for hair")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o HairSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("boiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), hairPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"hair\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, hairPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all in hair slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected all in update all hair")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Hair) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no hair provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(hairColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	hairUpsertCacheMut.RLock()
	cache, cached := hairUpsertCache[key]
	hairUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			hairAllColumns,
			hairColumnsWithDefault,
			hairColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			hairAllColumns,
			hairPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("boiler: unable to upsert hair, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(hairPrimaryKeyColumns))
			copy(conflict, hairPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"hair\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(hairType, hairMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(hairType, hairMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "boiler: unable to upsert hair")
	}

	if !cached {
		hairUpsertCacheMut.Lock()
		hairUpsertCache[key] = cache
		hairUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// Delete deletes a single Hair record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Hair) Delete(exec boil.Executor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("boiler: no Hair provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), hairPrimaryKeyMapping)
		sql = "DELETE FROM \"hair\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"hair\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(hairType, hairMapping, append(wl, hairPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete from hair")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by delete for hair")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q hairQuery) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("boiler: no hairQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from hair")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for hair")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o HairSlice) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(hairBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), hairPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"hair\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, hairPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), hairPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"hair\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, hairPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from hair slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for hair")
	}

	if len(hairAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Hair) Reload(exec boil.Executor) error {
	ret, err := FindHair(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *HairSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := HairSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), hairPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"hair\".* FROM \"hair\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, hairPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to reload all in HairSlice")
	}

	*o = slice

	return nil
}

// HairExists checks if the Hair row exists.
func HairExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"hair\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "boiler: unable to check if hair exists")
	}

	return exists, nil
}
