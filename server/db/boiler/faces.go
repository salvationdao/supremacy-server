// Code generated by SQLBoiler 4.8.6 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package boiler

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// Face is an object representing the database table.
type Face struct {
	ID        string    `boiler:"id" boil:"id" json:"id" toml:"id" yaml:"id"`
	Hue       string    `boiler:"hue" boil:"hue" json:"hue" toml:"hue" yaml:"hue"`
	ImageURL  string    `boiler:"image_url" boil:"image_url" json:"image_url" toml:"image_url" yaml:"image_url"`
	UpdatedAt time.Time `boiler:"updated_at" boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt null.Time `boiler:"deleted_at" boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`
	CreatedAt time.Time `boiler:"created_at" boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *faceR `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
	L faceL  `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
}

var FaceColumns = struct {
	ID        string
	Hue       string
	ImageURL  string
	UpdatedAt string
	DeletedAt string
	CreatedAt string
}{
	ID:        "id",
	Hue:       "hue",
	ImageURL:  "image_url",
	UpdatedAt: "updated_at",
	DeletedAt: "deleted_at",
	CreatedAt: "created_at",
}

var FaceTableColumns = struct {
	ID        string
	Hue       string
	ImageURL  string
	UpdatedAt string
	DeletedAt string
	CreatedAt string
}{
	ID:        "faces.id",
	Hue:       "faces.hue",
	ImageURL:  "faces.image_url",
	UpdatedAt: "faces.updated_at",
	DeletedAt: "faces.deleted_at",
	CreatedAt: "faces.created_at",
}

// Generated where

var FaceWhere = struct {
	ID        whereHelperstring
	Hue       whereHelperstring
	ImageURL  whereHelperstring
	UpdatedAt whereHelpertime_Time
	DeletedAt whereHelpernull_Time
	CreatedAt whereHelpertime_Time
}{
	ID:        whereHelperstring{field: "\"faces\".\"id\""},
	Hue:       whereHelperstring{field: "\"faces\".\"hue\""},
	ImageURL:  whereHelperstring{field: "\"faces\".\"image_url\""},
	UpdatedAt: whereHelpertime_Time{field: "\"faces\".\"updated_at\""},
	DeletedAt: whereHelpernull_Time{field: "\"faces\".\"deleted_at\""},
	CreatedAt: whereHelpertime_Time{field: "\"faces\".\"created_at\""},
}

// FaceRels is where relationship names are stored.
var FaceRels = struct {
}{}

// faceR is where relationships are stored.
type faceR struct {
}

// NewStruct creates a new relationship struct
func (*faceR) NewStruct() *faceR {
	return &faceR{}
}

// faceL is where Load methods for each relationship are stored.
type faceL struct{}

var (
	faceAllColumns            = []string{"id", "hue", "image_url", "updated_at", "deleted_at", "created_at"}
	faceColumnsWithoutDefault = []string{"hue", "image_url"}
	faceColumnsWithDefault    = []string{"id", "updated_at", "deleted_at", "created_at"}
	facePrimaryKeyColumns     = []string{"id"}
	faceGeneratedColumns      = []string{}
)

type (
	// FaceSlice is an alias for a slice of pointers to Face.
	// This should almost always be used instead of []Face.
	FaceSlice []*Face
	// FaceHook is the signature for custom Face hook methods
	FaceHook func(boil.Executor, *Face) error

	faceQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	faceType                 = reflect.TypeOf(&Face{})
	faceMapping              = queries.MakeStructMapping(faceType)
	facePrimaryKeyMapping, _ = queries.BindMapping(faceType, faceMapping, facePrimaryKeyColumns)
	faceInsertCacheMut       sync.RWMutex
	faceInsertCache          = make(map[string]insertCache)
	faceUpdateCacheMut       sync.RWMutex
	faceUpdateCache          = make(map[string]updateCache)
	faceUpsertCacheMut       sync.RWMutex
	faceUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var faceAfterSelectHooks []FaceHook

var faceBeforeInsertHooks []FaceHook
var faceAfterInsertHooks []FaceHook

var faceBeforeUpdateHooks []FaceHook
var faceAfterUpdateHooks []FaceHook

var faceBeforeDeleteHooks []FaceHook
var faceAfterDeleteHooks []FaceHook

var faceBeforeUpsertHooks []FaceHook
var faceAfterUpsertHooks []FaceHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *Face) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range faceAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *Face) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range faceBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *Face) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range faceAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *Face) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range faceBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *Face) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range faceAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *Face) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range faceBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *Face) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range faceAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *Face) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range faceBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *Face) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range faceAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddFaceHook registers your hook function for all future operations.
func AddFaceHook(hookPoint boil.HookPoint, faceHook FaceHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		faceAfterSelectHooks = append(faceAfterSelectHooks, faceHook)
	case boil.BeforeInsertHook:
		faceBeforeInsertHooks = append(faceBeforeInsertHooks, faceHook)
	case boil.AfterInsertHook:
		faceAfterInsertHooks = append(faceAfterInsertHooks, faceHook)
	case boil.BeforeUpdateHook:
		faceBeforeUpdateHooks = append(faceBeforeUpdateHooks, faceHook)
	case boil.AfterUpdateHook:
		faceAfterUpdateHooks = append(faceAfterUpdateHooks, faceHook)
	case boil.BeforeDeleteHook:
		faceBeforeDeleteHooks = append(faceBeforeDeleteHooks, faceHook)
	case boil.AfterDeleteHook:
		faceAfterDeleteHooks = append(faceAfterDeleteHooks, faceHook)
	case boil.BeforeUpsertHook:
		faceBeforeUpsertHooks = append(faceBeforeUpsertHooks, faceHook)
	case boil.AfterUpsertHook:
		faceAfterUpsertHooks = append(faceAfterUpsertHooks, faceHook)
	}
}

// One returns a single face record from the query.
func (q faceQuery) One(exec boil.Executor) (*Face, error) {
	o := &Face{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: failed to execute a one query for faces")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all Face records from the query.
func (q faceQuery) All(exec boil.Executor) (FaceSlice, error) {
	var o []*Face

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "boiler: failed to assign all query results to Face slice")
	}

	if len(faceAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all Face records in the query.
func (q faceQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to count faces rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q faceQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "boiler: failed to check if faces exists")
	}

	return count > 0, nil
}

// Faces retrieves all the records using an executor.
func Faces(mods ...qm.QueryMod) faceQuery {
	mods = append(mods, qm.From("\"faces\""), qmhelper.WhereIsNull("\"faces\".\"deleted_at\""))
	return faceQuery{NewQuery(mods...)}
}

// FindFace retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindFace(exec boil.Executor, iD string, selectCols ...string) (*Face, error) {
	faceObj := &Face{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"faces\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, faceObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: unable to select from faces")
	}

	if err = faceObj.doAfterSelectHooks(exec); err != nil {
		return faceObj, err
	}

	return faceObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *Face) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no faces provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.UpdatedAt.IsZero() {
		o.UpdatedAt = currTime
	}
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(faceColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	faceInsertCacheMut.RLock()
	cache, cached := faceInsertCache[key]
	faceInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			faceAllColumns,
			faceColumnsWithDefault,
			faceColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(faceType, faceMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(faceType, faceMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"faces\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"faces\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "boiler: unable to insert into faces")
	}

	if !cached {
		faceInsertCacheMut.Lock()
		faceInsertCache[key] = cache
		faceInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// Update uses an executor to update the Face.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *Face) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime

	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	faceUpdateCacheMut.RLock()
	cache, cached := faceUpdateCache[key]
	faceUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			faceAllColumns,
			facePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("boiler: unable to update faces, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"faces\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, facePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(faceType, faceMapping, append(wl, facePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update faces row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by update for faces")
	}

	if !cached {
		faceUpdateCacheMut.Lock()
		faceUpdateCache[key] = cache
		faceUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q faceQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all for faces")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected for faces")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o FaceSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("boiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), facePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"faces\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, facePrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all in face slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected all in update all face")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *Face) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no faces provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(faceColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	faceUpsertCacheMut.RLock()
	cache, cached := faceUpsertCache[key]
	faceUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			faceAllColumns,
			faceColumnsWithDefault,
			faceColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			faceAllColumns,
			facePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("boiler: unable to upsert faces, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(facePrimaryKeyColumns))
			copy(conflict, facePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"faces\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(faceType, faceMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(faceType, faceMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "boiler: unable to upsert faces")
	}

	if !cached {
		faceUpsertCacheMut.Lock()
		faceUpsertCache[key] = cache
		faceUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// Delete deletes a single Face record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *Face) Delete(exec boil.Executor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("boiler: no Face provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), facePrimaryKeyMapping)
		sql = "DELETE FROM \"faces\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"faces\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(faceType, faceMapping, append(wl, facePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete from faces")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by delete for faces")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q faceQuery) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("boiler: no faceQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from faces")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for faces")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o FaceSlice) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(faceBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), facePrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"faces\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, facePrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), facePrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"faces\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, facePrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from face slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for faces")
	}

	if len(faceAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *Face) Reload(exec boil.Executor) error {
	ret, err := FindFace(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *FaceSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := FaceSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), facePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"faces\".* FROM \"faces\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, facePrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to reload all in FaceSlice")
	}

	*o = slice

	return nil
}

// FaceExists checks if the Face row exists.
func FaceExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"faces\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "boiler: unable to check if faces exists")
	}

	return exists, nil
}
