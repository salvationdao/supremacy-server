// Code generated by SQLBoiler 4.8.6 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package boiler

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// MechUtility is an object representing the database table.
type MechUtility struct {
	ChassisID  string      `boiler:"chassis_id" boil:"chassis_id" json:"chassis_id" toml:"chassis_id" yaml:"chassis_id"`
	UtilityID  null.String `boiler:"utility_id" boil:"utility_id" json:"utility_id,omitempty" toml:"utility_id" yaml:"utility_id,omitempty"`
	SlotNumber int         `boiler:"slot_number" boil:"slot_number" json:"slot_number" toml:"slot_number" yaml:"slot_number"`
	DeletedAt  null.Time   `boiler:"deleted_at" boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`
	UpdatedAt  time.Time   `boiler:"updated_at" boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	CreatedAt  time.Time   `boiler:"created_at" boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`

	R *mechUtilityR `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
	L mechUtilityL  `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
}

var MechUtilityColumns = struct {
	ChassisID  string
	UtilityID  string
	SlotNumber string
	DeletedAt  string
	UpdatedAt  string
	CreatedAt  string
}{
	ChassisID:  "chassis_id",
	UtilityID:  "utility_id",
	SlotNumber: "slot_number",
	DeletedAt:  "deleted_at",
	UpdatedAt:  "updated_at",
	CreatedAt:  "created_at",
}

var MechUtilityTableColumns = struct {
	ChassisID  string
	UtilityID  string
	SlotNumber string
	DeletedAt  string
	UpdatedAt  string
	CreatedAt  string
}{
	ChassisID:  "mech_utility.chassis_id",
	UtilityID:  "mech_utility.utility_id",
	SlotNumber: "mech_utility.slot_number",
	DeletedAt:  "mech_utility.deleted_at",
	UpdatedAt:  "mech_utility.updated_at",
	CreatedAt:  "mech_utility.created_at",
}

// Generated where

var MechUtilityWhere = struct {
	ChassisID  whereHelperstring
	UtilityID  whereHelpernull_String
	SlotNumber whereHelperint
	DeletedAt  whereHelpernull_Time
	UpdatedAt  whereHelpertime_Time
	CreatedAt  whereHelpertime_Time
}{
	ChassisID:  whereHelperstring{field: "\"mech_utility\".\"chassis_id\""},
	UtilityID:  whereHelpernull_String{field: "\"mech_utility\".\"utility_id\""},
	SlotNumber: whereHelperint{field: "\"mech_utility\".\"slot_number\""},
	DeletedAt:  whereHelpernull_Time{field: "\"mech_utility\".\"deleted_at\""},
	UpdatedAt:  whereHelpertime_Time{field: "\"mech_utility\".\"updated_at\""},
	CreatedAt:  whereHelpertime_Time{field: "\"mech_utility\".\"created_at\""},
}

// MechUtilityRels is where relationship names are stored.
var MechUtilityRels = struct {
	Chassis string
	Utility string
}{
	Chassis: "Chassis",
	Utility: "Utility",
}

// mechUtilityR is where relationships are stored.
type mechUtilityR struct {
	Chassis *Mech    `boiler:"Chassis" boil:"Chassis" json:"Chassis" toml:"Chassis" yaml:"Chassis"`
	Utility *Utility `boiler:"Utility" boil:"Utility" json:"Utility" toml:"Utility" yaml:"Utility"`
}

// NewStruct creates a new relationship struct
func (*mechUtilityR) NewStruct() *mechUtilityR {
	return &mechUtilityR{}
}

// mechUtilityL is where Load methods for each relationship are stored.
type mechUtilityL struct{}

var (
	mechUtilityAllColumns            = []string{"chassis_id", "utility_id", "slot_number", "deleted_at", "updated_at", "created_at"}
	mechUtilityColumnsWithoutDefault = []string{"chassis_id", "slot_number"}
	mechUtilityColumnsWithDefault    = []string{"utility_id", "deleted_at", "updated_at", "created_at"}
	mechUtilityPrimaryKeyColumns     = []string{"chassis_id", "slot_number"}
	mechUtilityGeneratedColumns      = []string{}
)

type (
	// MechUtilitySlice is an alias for a slice of pointers to MechUtility.
	// This should almost always be used instead of []MechUtility.
	MechUtilitySlice []*MechUtility
	// MechUtilityHook is the signature for custom MechUtility hook methods
	MechUtilityHook func(boil.Executor, *MechUtility) error

	mechUtilityQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	mechUtilityType                 = reflect.TypeOf(&MechUtility{})
	mechUtilityMapping              = queries.MakeStructMapping(mechUtilityType)
	mechUtilityPrimaryKeyMapping, _ = queries.BindMapping(mechUtilityType, mechUtilityMapping, mechUtilityPrimaryKeyColumns)
	mechUtilityInsertCacheMut       sync.RWMutex
	mechUtilityInsertCache          = make(map[string]insertCache)
	mechUtilityUpdateCacheMut       sync.RWMutex
	mechUtilityUpdateCache          = make(map[string]updateCache)
	mechUtilityUpsertCacheMut       sync.RWMutex
	mechUtilityUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var mechUtilityAfterSelectHooks []MechUtilityHook

var mechUtilityBeforeInsertHooks []MechUtilityHook
var mechUtilityAfterInsertHooks []MechUtilityHook

var mechUtilityBeforeUpdateHooks []MechUtilityHook
var mechUtilityAfterUpdateHooks []MechUtilityHook

var mechUtilityBeforeDeleteHooks []MechUtilityHook
var mechUtilityAfterDeleteHooks []MechUtilityHook

var mechUtilityBeforeUpsertHooks []MechUtilityHook
var mechUtilityAfterUpsertHooks []MechUtilityHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *MechUtility) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range mechUtilityAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *MechUtility) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range mechUtilityBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *MechUtility) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range mechUtilityAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *MechUtility) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range mechUtilityBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *MechUtility) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range mechUtilityAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *MechUtility) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range mechUtilityBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *MechUtility) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range mechUtilityAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *MechUtility) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range mechUtilityBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *MechUtility) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range mechUtilityAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddMechUtilityHook registers your hook function for all future operations.
func AddMechUtilityHook(hookPoint boil.HookPoint, mechUtilityHook MechUtilityHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		mechUtilityAfterSelectHooks = append(mechUtilityAfterSelectHooks, mechUtilityHook)
	case boil.BeforeInsertHook:
		mechUtilityBeforeInsertHooks = append(mechUtilityBeforeInsertHooks, mechUtilityHook)
	case boil.AfterInsertHook:
		mechUtilityAfterInsertHooks = append(mechUtilityAfterInsertHooks, mechUtilityHook)
	case boil.BeforeUpdateHook:
		mechUtilityBeforeUpdateHooks = append(mechUtilityBeforeUpdateHooks, mechUtilityHook)
	case boil.AfterUpdateHook:
		mechUtilityAfterUpdateHooks = append(mechUtilityAfterUpdateHooks, mechUtilityHook)
	case boil.BeforeDeleteHook:
		mechUtilityBeforeDeleteHooks = append(mechUtilityBeforeDeleteHooks, mechUtilityHook)
	case boil.AfterDeleteHook:
		mechUtilityAfterDeleteHooks = append(mechUtilityAfterDeleteHooks, mechUtilityHook)
	case boil.BeforeUpsertHook:
		mechUtilityBeforeUpsertHooks = append(mechUtilityBeforeUpsertHooks, mechUtilityHook)
	case boil.AfterUpsertHook:
		mechUtilityAfterUpsertHooks = append(mechUtilityAfterUpsertHooks, mechUtilityHook)
	}
}

// One returns a single mechUtility record from the query.
func (q mechUtilityQuery) One(exec boil.Executor) (*MechUtility, error) {
	o := &MechUtility{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: failed to execute a one query for mech_utility")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all MechUtility records from the query.
func (q mechUtilityQuery) All(exec boil.Executor) (MechUtilitySlice, error) {
	var o []*MechUtility

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "boiler: failed to assign all query results to MechUtility slice")
	}

	if len(mechUtilityAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all MechUtility records in the query.
func (q mechUtilityQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to count mech_utility rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q mechUtilityQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "boiler: failed to check if mech_utility exists")
	}

	return count > 0, nil
}

// Chassis pointed to by the foreign key.
func (o *MechUtility) Chassis(mods ...qm.QueryMod) mechQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.ChassisID),
		qmhelper.WhereIsNull("deleted_at"),
	}

	queryMods = append(queryMods, mods...)

	query := Mechs(queryMods...)
	queries.SetFrom(query.Query, "\"mechs\"")

	return query
}

// Utility pointed to by the foreign key.
func (o *MechUtility) Utility(mods ...qm.QueryMod) utilityQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.UtilityID),
		qmhelper.WhereIsNull("deleted_at"),
	}

	queryMods = append(queryMods, mods...)

	query := Utilities(queryMods...)
	queries.SetFrom(query.Query, "\"utility\"")

	return query
}

// LoadChassis allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (mechUtilityL) LoadChassis(e boil.Executor, singular bool, maybeMechUtility interface{}, mods queries.Applicator) error {
	var slice []*MechUtility
	var object *MechUtility

	if singular {
		object = maybeMechUtility.(*MechUtility)
	} else {
		slice = *maybeMechUtility.(*[]*MechUtility)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &mechUtilityR{}
		}
		args = append(args, object.ChassisID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &mechUtilityR{}
			}

			for _, a := range args {
				if a == obj.ChassisID {
					continue Outer
				}
			}

			args = append(args, obj.ChassisID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`mechs`),
		qm.WhereIn(`mechs.id in ?`, args...),
		qmhelper.WhereIsNull(`mechs.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Mech")
	}

	var resultSlice []*Mech
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Mech")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for mechs")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for mechs")
	}

	if len(mechUtilityAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Chassis = foreign
		if foreign.R == nil {
			foreign.R = &mechR{}
		}
		foreign.R.ChassisMechUtilities = append(foreign.R.ChassisMechUtilities, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.ChassisID == foreign.ID {
				local.R.Chassis = foreign
				if foreign.R == nil {
					foreign.R = &mechR{}
				}
				foreign.R.ChassisMechUtilities = append(foreign.R.ChassisMechUtilities, local)
				break
			}
		}
	}

	return nil
}

// LoadUtility allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (mechUtilityL) LoadUtility(e boil.Executor, singular bool, maybeMechUtility interface{}, mods queries.Applicator) error {
	var slice []*MechUtility
	var object *MechUtility

	if singular {
		object = maybeMechUtility.(*MechUtility)
	} else {
		slice = *maybeMechUtility.(*[]*MechUtility)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &mechUtilityR{}
		}
		if !queries.IsNil(object.UtilityID) {
			args = append(args, object.UtilityID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &mechUtilityR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.UtilityID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.UtilityID) {
				args = append(args, obj.UtilityID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`utility`),
		qm.WhereIn(`utility.id in ?`, args...),
		qmhelper.WhereIsNull(`utility.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Utility")
	}

	var resultSlice []*Utility
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Utility")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for utility")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for utility")
	}

	if len(mechUtilityAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Utility = foreign
		if foreign.R == nil {
			foreign.R = &utilityR{}
		}
		foreign.R.MechUtility = object
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.UtilityID, foreign.ID) {
				local.R.Utility = foreign
				if foreign.R == nil {
					foreign.R = &utilityR{}
				}
				foreign.R.MechUtility = local
				break
			}
		}
	}

	return nil
}

// SetChassis of the mechUtility to the related item.
// Sets o.R.Chassis to related.
// Adds o to related.R.ChassisMechUtilities.
func (o *MechUtility) SetChassis(exec boil.Executor, insert bool, related *Mech) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"mech_utility\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"chassis_id"}),
		strmangle.WhereClause("\"", "\"", 2, mechUtilityPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ChassisID, o.SlotNumber}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.ChassisID = related.ID
	if o.R == nil {
		o.R = &mechUtilityR{
			Chassis: related,
		}
	} else {
		o.R.Chassis = related
	}

	if related.R == nil {
		related.R = &mechR{
			ChassisMechUtilities: MechUtilitySlice{o},
		}
	} else {
		related.R.ChassisMechUtilities = append(related.R.ChassisMechUtilities, o)
	}

	return nil
}

// SetUtility of the mechUtility to the related item.
// Sets o.R.Utility to related.
// Adds o to related.R.MechUtility.
func (o *MechUtility) SetUtility(exec boil.Executor, insert bool, related *Utility) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"mech_utility\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"utility_id"}),
		strmangle.WhereClause("\"", "\"", 2, mechUtilityPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ChassisID, o.SlotNumber}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.UtilityID, related.ID)
	if o.R == nil {
		o.R = &mechUtilityR{
			Utility: related,
		}
	} else {
		o.R.Utility = related
	}

	if related.R == nil {
		related.R = &utilityR{
			MechUtility: o,
		}
	} else {
		related.R.MechUtility = o
	}

	return nil
}

// RemoveUtility relationship.
// Sets o.R.Utility to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *MechUtility) RemoveUtility(exec boil.Executor, related *Utility) error {
	var err error

	queries.SetScanner(&o.UtilityID, nil)
	if _, err = o.Update(exec, boil.Whitelist("utility_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.Utility = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	related.R.MechUtility = nil
	return nil
}

// MechUtilities retrieves all the records using an executor.
func MechUtilities(mods ...qm.QueryMod) mechUtilityQuery {
	mods = append(mods, qm.From("\"mech_utility\""), qmhelper.WhereIsNull("\"mech_utility\".\"deleted_at\""))
	return mechUtilityQuery{NewQuery(mods...)}
}

// FindMechUtility retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindMechUtility(exec boil.Executor, chassisID string, slotNumber int, selectCols ...string) (*MechUtility, error) {
	mechUtilityObj := &MechUtility{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"mech_utility\" where \"chassis_id\"=$1 AND \"slot_number\"=$2 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, chassisID, slotNumber)

	err := q.Bind(nil, exec, mechUtilityObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: unable to select from mech_utility")
	}

	if err = mechUtilityObj.doAfterSelectHooks(exec); err != nil {
		return mechUtilityObj, err
	}

	return mechUtilityObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *MechUtility) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no mech_utility provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.UpdatedAt.IsZero() {
		o.UpdatedAt = currTime
	}
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(mechUtilityColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	mechUtilityInsertCacheMut.RLock()
	cache, cached := mechUtilityInsertCache[key]
	mechUtilityInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			mechUtilityAllColumns,
			mechUtilityColumnsWithDefault,
			mechUtilityColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(mechUtilityType, mechUtilityMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(mechUtilityType, mechUtilityMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"mech_utility\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"mech_utility\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "boiler: unable to insert into mech_utility")
	}

	if !cached {
		mechUtilityInsertCacheMut.Lock()
		mechUtilityInsertCache[key] = cache
		mechUtilityInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// Update uses an executor to update the MechUtility.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *MechUtility) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime

	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	mechUtilityUpdateCacheMut.RLock()
	cache, cached := mechUtilityUpdateCache[key]
	mechUtilityUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			mechUtilityAllColumns,
			mechUtilityPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("boiler: unable to update mech_utility, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"mech_utility\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, mechUtilityPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(mechUtilityType, mechUtilityMapping, append(wl, mechUtilityPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update mech_utility row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by update for mech_utility")
	}

	if !cached {
		mechUtilityUpdateCacheMut.Lock()
		mechUtilityUpdateCache[key] = cache
		mechUtilityUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q mechUtilityQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all for mech_utility")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected for mech_utility")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o MechUtilitySlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("boiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), mechUtilityPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"mech_utility\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, mechUtilityPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all in mechUtility slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected all in update all mechUtility")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *MechUtility) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no mech_utility provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(mechUtilityColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	mechUtilityUpsertCacheMut.RLock()
	cache, cached := mechUtilityUpsertCache[key]
	mechUtilityUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			mechUtilityAllColumns,
			mechUtilityColumnsWithDefault,
			mechUtilityColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			mechUtilityAllColumns,
			mechUtilityPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("boiler: unable to upsert mech_utility, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(mechUtilityPrimaryKeyColumns))
			copy(conflict, mechUtilityPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"mech_utility\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(mechUtilityType, mechUtilityMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(mechUtilityType, mechUtilityMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "boiler: unable to upsert mech_utility")
	}

	if !cached {
		mechUtilityUpsertCacheMut.Lock()
		mechUtilityUpsertCache[key] = cache
		mechUtilityUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// Delete deletes a single MechUtility record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *MechUtility) Delete(exec boil.Executor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("boiler: no MechUtility provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), mechUtilityPrimaryKeyMapping)
		sql = "DELETE FROM \"mech_utility\" WHERE \"chassis_id\"=$1 AND \"slot_number\"=$2"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"mech_utility\" SET %s WHERE \"chassis_id\"=$2 AND \"slot_number\"=$3",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(mechUtilityType, mechUtilityMapping, append(wl, mechUtilityPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete from mech_utility")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by delete for mech_utility")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q mechUtilityQuery) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("boiler: no mechUtilityQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from mech_utility")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for mech_utility")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o MechUtilitySlice) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(mechUtilityBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), mechUtilityPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"mech_utility\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, mechUtilityPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), mechUtilityPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"mech_utility\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, mechUtilityPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from mechUtility slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for mech_utility")
	}

	if len(mechUtilityAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *MechUtility) Reload(exec boil.Executor) error {
	ret, err := FindMechUtility(exec, o.ChassisID, o.SlotNumber)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *MechUtilitySlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := MechUtilitySlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), mechUtilityPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"mech_utility\".* FROM \"mech_utility\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, mechUtilityPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to reload all in MechUtilitySlice")
	}

	*o = slice

	return nil
}

// MechUtilityExists checks if the MechUtility row exists.
func MechUtilityExists(exec boil.Executor, chassisID string, slotNumber int) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"mech_utility\" where \"chassis_id\"=$1 AND \"slot_number\"=$2 and \"deleted_at\" is null limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, chassisID, slotNumber)
	}
	row := exec.QueryRow(sql, chassisID, slotNumber)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "boiler: unable to check if mech_utility exists")
	}

	return exists, nil
}
