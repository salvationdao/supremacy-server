// Code generated by SQLBoiler 4.8.6 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package boiler

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/shopspring/decimal"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// BattleLobbyExtraSupsReward is an object representing the database table.
type BattleLobbyExtraSupsReward struct {
	ID            string          `boiler:"id" boil:"id" json:"id" toml:"id" yaml:"id"`
	BattleLobbyID string          `boiler:"battle_lobby_id" boil:"battle_lobby_id" json:"battle_lobby_id" toml:"battle_lobby_id" yaml:"battle_lobby_id"`
	OfferedByID   string          `boiler:"offered_by_id" boil:"offered_by_id" json:"offered_by_id" toml:"offered_by_id" yaml:"offered_by_id"`
	Amount        decimal.Decimal `boiler:"amount" boil:"amount" json:"amount" toml:"amount" yaml:"amount"`
	PaidTXID      string          `boiler:"paid_tx_id" boil:"paid_tx_id" json:"paid_tx_id" toml:"paid_tx_id" yaml:"paid_tx_id"`
	RefundedTXID  null.String     `boiler:"refunded_tx_id" boil:"refunded_tx_id" json:"refunded_tx_id,omitempty" toml:"refunded_tx_id" yaml:"refunded_tx_id,omitempty"`
	CreatedAt     time.Time       `boiler:"created_at" boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt     time.Time       `boiler:"updated_at" boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt     null.Time       `boiler:"deleted_at" boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *battleLobbyExtraSupsRewardR `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
	L battleLobbyExtraSupsRewardL  `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BattleLobbyExtraSupsRewardColumns = struct {
	ID            string
	BattleLobbyID string
	OfferedByID   string
	Amount        string
	PaidTXID      string
	RefundedTXID  string
	CreatedAt     string
	UpdatedAt     string
	DeletedAt     string
}{
	ID:            "id",
	BattleLobbyID: "battle_lobby_id",
	OfferedByID:   "offered_by_id",
	Amount:        "amount",
	PaidTXID:      "paid_tx_id",
	RefundedTXID:  "refunded_tx_id",
	CreatedAt:     "created_at",
	UpdatedAt:     "updated_at",
	DeletedAt:     "deleted_at",
}

var BattleLobbyExtraSupsRewardTableColumns = struct {
	ID            string
	BattleLobbyID string
	OfferedByID   string
	Amount        string
	PaidTXID      string
	RefundedTXID  string
	CreatedAt     string
	UpdatedAt     string
	DeletedAt     string
}{
	ID:            "battle_lobby_extra_sups_rewards.id",
	BattleLobbyID: "battle_lobby_extra_sups_rewards.battle_lobby_id",
	OfferedByID:   "battle_lobby_extra_sups_rewards.offered_by_id",
	Amount:        "battle_lobby_extra_sups_rewards.amount",
	PaidTXID:      "battle_lobby_extra_sups_rewards.paid_tx_id",
	RefundedTXID:  "battle_lobby_extra_sups_rewards.refunded_tx_id",
	CreatedAt:     "battle_lobby_extra_sups_rewards.created_at",
	UpdatedAt:     "battle_lobby_extra_sups_rewards.updated_at",
	DeletedAt:     "battle_lobby_extra_sups_rewards.deleted_at",
}

// Generated where

var BattleLobbyExtraSupsRewardWhere = struct {
	ID            whereHelperstring
	BattleLobbyID whereHelperstring
	OfferedByID   whereHelperstring
	Amount        whereHelperdecimal_Decimal
	PaidTXID      whereHelperstring
	RefundedTXID  whereHelpernull_String
	CreatedAt     whereHelpertime_Time
	UpdatedAt     whereHelpertime_Time
	DeletedAt     whereHelpernull_Time
}{
	ID:            whereHelperstring{field: "\"battle_lobby_extra_sups_rewards\".\"id\""},
	BattleLobbyID: whereHelperstring{field: "\"battle_lobby_extra_sups_rewards\".\"battle_lobby_id\""},
	OfferedByID:   whereHelperstring{field: "\"battle_lobby_extra_sups_rewards\".\"offered_by_id\""},
	Amount:        whereHelperdecimal_Decimal{field: "\"battle_lobby_extra_sups_rewards\".\"amount\""},
	PaidTXID:      whereHelperstring{field: "\"battle_lobby_extra_sups_rewards\".\"paid_tx_id\""},
	RefundedTXID:  whereHelpernull_String{field: "\"battle_lobby_extra_sups_rewards\".\"refunded_tx_id\""},
	CreatedAt:     whereHelpertime_Time{field: "\"battle_lobby_extra_sups_rewards\".\"created_at\""},
	UpdatedAt:     whereHelpertime_Time{field: "\"battle_lobby_extra_sups_rewards\".\"updated_at\""},
	DeletedAt:     whereHelpernull_Time{field: "\"battle_lobby_extra_sups_rewards\".\"deleted_at\""},
}

// BattleLobbyExtraSupsRewardRels is where relationship names are stored.
var BattleLobbyExtraSupsRewardRels = struct {
	BattleLobby string
	OfferedBy   string
}{
	BattleLobby: "BattleLobby",
	OfferedBy:   "OfferedBy",
}

// battleLobbyExtraSupsRewardR is where relationships are stored.
type battleLobbyExtraSupsRewardR struct {
	BattleLobby *BattleLobby `boiler:"BattleLobby" boil:"BattleLobby" json:"BattleLobby" toml:"BattleLobby" yaml:"BattleLobby"`
	OfferedBy   *Player      `boiler:"OfferedBy" boil:"OfferedBy" json:"OfferedBy" toml:"OfferedBy" yaml:"OfferedBy"`
}

// NewStruct creates a new relationship struct
func (*battleLobbyExtraSupsRewardR) NewStruct() *battleLobbyExtraSupsRewardR {
	return &battleLobbyExtraSupsRewardR{}
}

// battleLobbyExtraSupsRewardL is where Load methods for each relationship are stored.
type battleLobbyExtraSupsRewardL struct{}

var (
	battleLobbyExtraSupsRewardAllColumns            = []string{"id", "battle_lobby_id", "offered_by_id", "amount", "paid_tx_id", "refunded_tx_id", "created_at", "updated_at", "deleted_at"}
	battleLobbyExtraSupsRewardColumnsWithoutDefault = []string{"battle_lobby_id", "offered_by_id", "paid_tx_id"}
	battleLobbyExtraSupsRewardColumnsWithDefault    = []string{"id", "amount", "refunded_tx_id", "created_at", "updated_at", "deleted_at"}
	battleLobbyExtraSupsRewardPrimaryKeyColumns     = []string{"id"}
	battleLobbyExtraSupsRewardGeneratedColumns      = []string{}
)

type (
	// BattleLobbyExtraSupsRewardSlice is an alias for a slice of pointers to BattleLobbyExtraSupsReward.
	// This should almost always be used instead of []BattleLobbyExtraSupsReward.
	BattleLobbyExtraSupsRewardSlice []*BattleLobbyExtraSupsReward
	// BattleLobbyExtraSupsRewardHook is the signature for custom BattleLobbyExtraSupsReward hook methods
	BattleLobbyExtraSupsRewardHook func(boil.Executor, *BattleLobbyExtraSupsReward) error

	battleLobbyExtraSupsRewardQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	battleLobbyExtraSupsRewardType                 = reflect.TypeOf(&BattleLobbyExtraSupsReward{})
	battleLobbyExtraSupsRewardMapping              = queries.MakeStructMapping(battleLobbyExtraSupsRewardType)
	battleLobbyExtraSupsRewardPrimaryKeyMapping, _ = queries.BindMapping(battleLobbyExtraSupsRewardType, battleLobbyExtraSupsRewardMapping, battleLobbyExtraSupsRewardPrimaryKeyColumns)
	battleLobbyExtraSupsRewardInsertCacheMut       sync.RWMutex
	battleLobbyExtraSupsRewardInsertCache          = make(map[string]insertCache)
	battleLobbyExtraSupsRewardUpdateCacheMut       sync.RWMutex
	battleLobbyExtraSupsRewardUpdateCache          = make(map[string]updateCache)
	battleLobbyExtraSupsRewardUpsertCacheMut       sync.RWMutex
	battleLobbyExtraSupsRewardUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var battleLobbyExtraSupsRewardAfterSelectHooks []BattleLobbyExtraSupsRewardHook

var battleLobbyExtraSupsRewardBeforeInsertHooks []BattleLobbyExtraSupsRewardHook
var battleLobbyExtraSupsRewardAfterInsertHooks []BattleLobbyExtraSupsRewardHook

var battleLobbyExtraSupsRewardBeforeUpdateHooks []BattleLobbyExtraSupsRewardHook
var battleLobbyExtraSupsRewardAfterUpdateHooks []BattleLobbyExtraSupsRewardHook

var battleLobbyExtraSupsRewardBeforeDeleteHooks []BattleLobbyExtraSupsRewardHook
var battleLobbyExtraSupsRewardAfterDeleteHooks []BattleLobbyExtraSupsRewardHook

var battleLobbyExtraSupsRewardBeforeUpsertHooks []BattleLobbyExtraSupsRewardHook
var battleLobbyExtraSupsRewardAfterUpsertHooks []BattleLobbyExtraSupsRewardHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *BattleLobbyExtraSupsReward) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range battleLobbyExtraSupsRewardAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *BattleLobbyExtraSupsReward) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range battleLobbyExtraSupsRewardBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *BattleLobbyExtraSupsReward) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range battleLobbyExtraSupsRewardAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *BattleLobbyExtraSupsReward) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range battleLobbyExtraSupsRewardBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *BattleLobbyExtraSupsReward) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range battleLobbyExtraSupsRewardAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *BattleLobbyExtraSupsReward) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range battleLobbyExtraSupsRewardBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *BattleLobbyExtraSupsReward) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range battleLobbyExtraSupsRewardAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *BattleLobbyExtraSupsReward) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range battleLobbyExtraSupsRewardBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *BattleLobbyExtraSupsReward) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range battleLobbyExtraSupsRewardAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddBattleLobbyExtraSupsRewardHook registers your hook function for all future operations.
func AddBattleLobbyExtraSupsRewardHook(hookPoint boil.HookPoint, battleLobbyExtraSupsRewardHook BattleLobbyExtraSupsRewardHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		battleLobbyExtraSupsRewardAfterSelectHooks = append(battleLobbyExtraSupsRewardAfterSelectHooks, battleLobbyExtraSupsRewardHook)
	case boil.BeforeInsertHook:
		battleLobbyExtraSupsRewardBeforeInsertHooks = append(battleLobbyExtraSupsRewardBeforeInsertHooks, battleLobbyExtraSupsRewardHook)
	case boil.AfterInsertHook:
		battleLobbyExtraSupsRewardAfterInsertHooks = append(battleLobbyExtraSupsRewardAfterInsertHooks, battleLobbyExtraSupsRewardHook)
	case boil.BeforeUpdateHook:
		battleLobbyExtraSupsRewardBeforeUpdateHooks = append(battleLobbyExtraSupsRewardBeforeUpdateHooks, battleLobbyExtraSupsRewardHook)
	case boil.AfterUpdateHook:
		battleLobbyExtraSupsRewardAfterUpdateHooks = append(battleLobbyExtraSupsRewardAfterUpdateHooks, battleLobbyExtraSupsRewardHook)
	case boil.BeforeDeleteHook:
		battleLobbyExtraSupsRewardBeforeDeleteHooks = append(battleLobbyExtraSupsRewardBeforeDeleteHooks, battleLobbyExtraSupsRewardHook)
	case boil.AfterDeleteHook:
		battleLobbyExtraSupsRewardAfterDeleteHooks = append(battleLobbyExtraSupsRewardAfterDeleteHooks, battleLobbyExtraSupsRewardHook)
	case boil.BeforeUpsertHook:
		battleLobbyExtraSupsRewardBeforeUpsertHooks = append(battleLobbyExtraSupsRewardBeforeUpsertHooks, battleLobbyExtraSupsRewardHook)
	case boil.AfterUpsertHook:
		battleLobbyExtraSupsRewardAfterUpsertHooks = append(battleLobbyExtraSupsRewardAfterUpsertHooks, battleLobbyExtraSupsRewardHook)
	}
}

// One returns a single battleLobbyExtraSupsReward record from the query.
func (q battleLobbyExtraSupsRewardQuery) One(exec boil.Executor) (*BattleLobbyExtraSupsReward, error) {
	o := &BattleLobbyExtraSupsReward{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: failed to execute a one query for battle_lobby_extra_sups_rewards")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all BattleLobbyExtraSupsReward records from the query.
func (q battleLobbyExtraSupsRewardQuery) All(exec boil.Executor) (BattleLobbyExtraSupsRewardSlice, error) {
	var o []*BattleLobbyExtraSupsReward

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "boiler: failed to assign all query results to BattleLobbyExtraSupsReward slice")
	}

	if len(battleLobbyExtraSupsRewardAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all BattleLobbyExtraSupsReward records in the query.
func (q battleLobbyExtraSupsRewardQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to count battle_lobby_extra_sups_rewards rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q battleLobbyExtraSupsRewardQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "boiler: failed to check if battle_lobby_extra_sups_rewards exists")
	}

	return count > 0, nil
}

// BattleLobby pointed to by the foreign key.
func (o *BattleLobbyExtraSupsReward) BattleLobby(mods ...qm.QueryMod) battleLobbyQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.BattleLobbyID),
		qmhelper.WhereIsNull("deleted_at"),
	}

	queryMods = append(queryMods, mods...)

	query := BattleLobbies(queryMods...)
	queries.SetFrom(query.Query, "\"battle_lobbies\"")

	return query
}

// OfferedBy pointed to by the foreign key.
func (o *BattleLobbyExtraSupsReward) OfferedBy(mods ...qm.QueryMod) playerQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.OfferedByID),
		qmhelper.WhereIsNull("deleted_at"),
	}

	queryMods = append(queryMods, mods...)

	query := Players(queryMods...)
	queries.SetFrom(query.Query, "\"players\"")

	return query
}

// LoadBattleLobby allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (battleLobbyExtraSupsRewardL) LoadBattleLobby(e boil.Executor, singular bool, maybeBattleLobbyExtraSupsReward interface{}, mods queries.Applicator) error {
	var slice []*BattleLobbyExtraSupsReward
	var object *BattleLobbyExtraSupsReward

	if singular {
		object = maybeBattleLobbyExtraSupsReward.(*BattleLobbyExtraSupsReward)
	} else {
		slice = *maybeBattleLobbyExtraSupsReward.(*[]*BattleLobbyExtraSupsReward)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &battleLobbyExtraSupsRewardR{}
		}
		args = append(args, object.BattleLobbyID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &battleLobbyExtraSupsRewardR{}
			}

			for _, a := range args {
				if a == obj.BattleLobbyID {
					continue Outer
				}
			}

			args = append(args, obj.BattleLobbyID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`battle_lobbies`),
		qm.WhereIn(`battle_lobbies.id in ?`, args...),
		qmhelper.WhereIsNull(`battle_lobbies.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load BattleLobby")
	}

	var resultSlice []*BattleLobby
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice BattleLobby")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for battle_lobbies")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for battle_lobbies")
	}

	if len(battleLobbyExtraSupsRewardAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.BattleLobby = foreign
		if foreign.R == nil {
			foreign.R = &battleLobbyR{}
		}
		foreign.R.BattleLobbyExtraSupsRewards = append(foreign.R.BattleLobbyExtraSupsRewards, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BattleLobbyID == foreign.ID {
				local.R.BattleLobby = foreign
				if foreign.R == nil {
					foreign.R = &battleLobbyR{}
				}
				foreign.R.BattleLobbyExtraSupsRewards = append(foreign.R.BattleLobbyExtraSupsRewards, local)
				break
			}
		}
	}

	return nil
}

// LoadOfferedBy allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (battleLobbyExtraSupsRewardL) LoadOfferedBy(e boil.Executor, singular bool, maybeBattleLobbyExtraSupsReward interface{}, mods queries.Applicator) error {
	var slice []*BattleLobbyExtraSupsReward
	var object *BattleLobbyExtraSupsReward

	if singular {
		object = maybeBattleLobbyExtraSupsReward.(*BattleLobbyExtraSupsReward)
	} else {
		slice = *maybeBattleLobbyExtraSupsReward.(*[]*BattleLobbyExtraSupsReward)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &battleLobbyExtraSupsRewardR{}
		}
		args = append(args, object.OfferedByID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &battleLobbyExtraSupsRewardR{}
			}

			for _, a := range args {
				if a == obj.OfferedByID {
					continue Outer
				}
			}

			args = append(args, obj.OfferedByID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`players`),
		qm.WhereIn(`players.id in ?`, args...),
		qmhelper.WhereIsNull(`players.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Player")
	}

	var resultSlice []*Player
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Player")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for players")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for players")
	}

	if len(battleLobbyExtraSupsRewardAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.OfferedBy = foreign
		if foreign.R == nil {
			foreign.R = &playerR{}
		}
		foreign.R.OfferedByBattleLobbyExtraSupsRewards = append(foreign.R.OfferedByBattleLobbyExtraSupsRewards, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.OfferedByID == foreign.ID {
				local.R.OfferedBy = foreign
				if foreign.R == nil {
					foreign.R = &playerR{}
				}
				foreign.R.OfferedByBattleLobbyExtraSupsRewards = append(foreign.R.OfferedByBattleLobbyExtraSupsRewards, local)
				break
			}
		}
	}

	return nil
}

// SetBattleLobby of the battleLobbyExtraSupsReward to the related item.
// Sets o.R.BattleLobby to related.
// Adds o to related.R.BattleLobbyExtraSupsRewards.
func (o *BattleLobbyExtraSupsReward) SetBattleLobby(exec boil.Executor, insert bool, related *BattleLobby) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"battle_lobby_extra_sups_rewards\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"battle_lobby_id"}),
		strmangle.WhereClause("\"", "\"", 2, battleLobbyExtraSupsRewardPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BattleLobbyID = related.ID
	if o.R == nil {
		o.R = &battleLobbyExtraSupsRewardR{
			BattleLobby: related,
		}
	} else {
		o.R.BattleLobby = related
	}

	if related.R == nil {
		related.R = &battleLobbyR{
			BattleLobbyExtraSupsRewards: BattleLobbyExtraSupsRewardSlice{o},
		}
	} else {
		related.R.BattleLobbyExtraSupsRewards = append(related.R.BattleLobbyExtraSupsRewards, o)
	}

	return nil
}

// SetOfferedBy of the battleLobbyExtraSupsReward to the related item.
// Sets o.R.OfferedBy to related.
// Adds o to related.R.OfferedByBattleLobbyExtraSupsRewards.
func (o *BattleLobbyExtraSupsReward) SetOfferedBy(exec boil.Executor, insert bool, related *Player) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"battle_lobby_extra_sups_rewards\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"offered_by_id"}),
		strmangle.WhereClause("\"", "\"", 2, battleLobbyExtraSupsRewardPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.OfferedByID = related.ID
	if o.R == nil {
		o.R = &battleLobbyExtraSupsRewardR{
			OfferedBy: related,
		}
	} else {
		o.R.OfferedBy = related
	}

	if related.R == nil {
		related.R = &playerR{
			OfferedByBattleLobbyExtraSupsRewards: BattleLobbyExtraSupsRewardSlice{o},
		}
	} else {
		related.R.OfferedByBattleLobbyExtraSupsRewards = append(related.R.OfferedByBattleLobbyExtraSupsRewards, o)
	}

	return nil
}

// BattleLobbyExtraSupsRewards retrieves all the records using an executor.
func BattleLobbyExtraSupsRewards(mods ...qm.QueryMod) battleLobbyExtraSupsRewardQuery {
	mods = append(mods, qm.From("\"battle_lobby_extra_sups_rewards\""), qmhelper.WhereIsNull("\"battle_lobby_extra_sups_rewards\".\"deleted_at\""))
	return battleLobbyExtraSupsRewardQuery{NewQuery(mods...)}
}

// FindBattleLobbyExtraSupsReward retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBattleLobbyExtraSupsReward(exec boil.Executor, iD string, selectCols ...string) (*BattleLobbyExtraSupsReward, error) {
	battleLobbyExtraSupsRewardObj := &BattleLobbyExtraSupsReward{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"battle_lobby_extra_sups_rewards\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, battleLobbyExtraSupsRewardObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: unable to select from battle_lobby_extra_sups_rewards")
	}

	if err = battleLobbyExtraSupsRewardObj.doAfterSelectHooks(exec); err != nil {
		return battleLobbyExtraSupsRewardObj, err
	}

	return battleLobbyExtraSupsRewardObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *BattleLobbyExtraSupsReward) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no battle_lobby_extra_sups_rewards provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}
	if o.UpdatedAt.IsZero() {
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(battleLobbyExtraSupsRewardColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	battleLobbyExtraSupsRewardInsertCacheMut.RLock()
	cache, cached := battleLobbyExtraSupsRewardInsertCache[key]
	battleLobbyExtraSupsRewardInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			battleLobbyExtraSupsRewardAllColumns,
			battleLobbyExtraSupsRewardColumnsWithDefault,
			battleLobbyExtraSupsRewardColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(battleLobbyExtraSupsRewardType, battleLobbyExtraSupsRewardMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(battleLobbyExtraSupsRewardType, battleLobbyExtraSupsRewardMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"battle_lobby_extra_sups_rewards\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"battle_lobby_extra_sups_rewards\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "boiler: unable to insert into battle_lobby_extra_sups_rewards")
	}

	if !cached {
		battleLobbyExtraSupsRewardInsertCacheMut.Lock()
		battleLobbyExtraSupsRewardInsertCache[key] = cache
		battleLobbyExtraSupsRewardInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// Update uses an executor to update the BattleLobbyExtraSupsReward.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *BattleLobbyExtraSupsReward) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime

	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	battleLobbyExtraSupsRewardUpdateCacheMut.RLock()
	cache, cached := battleLobbyExtraSupsRewardUpdateCache[key]
	battleLobbyExtraSupsRewardUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			battleLobbyExtraSupsRewardAllColumns,
			battleLobbyExtraSupsRewardPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("boiler: unable to update battle_lobby_extra_sups_rewards, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"battle_lobby_extra_sups_rewards\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, battleLobbyExtraSupsRewardPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(battleLobbyExtraSupsRewardType, battleLobbyExtraSupsRewardMapping, append(wl, battleLobbyExtraSupsRewardPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update battle_lobby_extra_sups_rewards row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by update for battle_lobby_extra_sups_rewards")
	}

	if !cached {
		battleLobbyExtraSupsRewardUpdateCacheMut.Lock()
		battleLobbyExtraSupsRewardUpdateCache[key] = cache
		battleLobbyExtraSupsRewardUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q battleLobbyExtraSupsRewardQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all for battle_lobby_extra_sups_rewards")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected for battle_lobby_extra_sups_rewards")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BattleLobbyExtraSupsRewardSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("boiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), battleLobbyExtraSupsRewardPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"battle_lobby_extra_sups_rewards\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, battleLobbyExtraSupsRewardPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all in battleLobbyExtraSupsReward slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected all in update all battleLobbyExtraSupsReward")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *BattleLobbyExtraSupsReward) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no battle_lobby_extra_sups_rewards provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}
	o.UpdatedAt = currTime

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(battleLobbyExtraSupsRewardColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	battleLobbyExtraSupsRewardUpsertCacheMut.RLock()
	cache, cached := battleLobbyExtraSupsRewardUpsertCache[key]
	battleLobbyExtraSupsRewardUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			battleLobbyExtraSupsRewardAllColumns,
			battleLobbyExtraSupsRewardColumnsWithDefault,
			battleLobbyExtraSupsRewardColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			battleLobbyExtraSupsRewardAllColumns,
			battleLobbyExtraSupsRewardPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("boiler: unable to upsert battle_lobby_extra_sups_rewards, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(battleLobbyExtraSupsRewardPrimaryKeyColumns))
			copy(conflict, battleLobbyExtraSupsRewardPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"battle_lobby_extra_sups_rewards\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(battleLobbyExtraSupsRewardType, battleLobbyExtraSupsRewardMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(battleLobbyExtraSupsRewardType, battleLobbyExtraSupsRewardMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "boiler: unable to upsert battle_lobby_extra_sups_rewards")
	}

	if !cached {
		battleLobbyExtraSupsRewardUpsertCacheMut.Lock()
		battleLobbyExtraSupsRewardUpsertCache[key] = cache
		battleLobbyExtraSupsRewardUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// Delete deletes a single BattleLobbyExtraSupsReward record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BattleLobbyExtraSupsReward) Delete(exec boil.Executor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("boiler: no BattleLobbyExtraSupsReward provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), battleLobbyExtraSupsRewardPrimaryKeyMapping)
		sql = "DELETE FROM \"battle_lobby_extra_sups_rewards\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"battle_lobby_extra_sups_rewards\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(battleLobbyExtraSupsRewardType, battleLobbyExtraSupsRewardMapping, append(wl, battleLobbyExtraSupsRewardPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete from battle_lobby_extra_sups_rewards")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by delete for battle_lobby_extra_sups_rewards")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q battleLobbyExtraSupsRewardQuery) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("boiler: no battleLobbyExtraSupsRewardQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from battle_lobby_extra_sups_rewards")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for battle_lobby_extra_sups_rewards")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BattleLobbyExtraSupsRewardSlice) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(battleLobbyExtraSupsRewardBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), battleLobbyExtraSupsRewardPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"battle_lobby_extra_sups_rewards\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, battleLobbyExtraSupsRewardPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), battleLobbyExtraSupsRewardPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"battle_lobby_extra_sups_rewards\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, battleLobbyExtraSupsRewardPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from battleLobbyExtraSupsReward slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for battle_lobby_extra_sups_rewards")
	}

	if len(battleLobbyExtraSupsRewardAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BattleLobbyExtraSupsReward) Reload(exec boil.Executor) error {
	ret, err := FindBattleLobbyExtraSupsReward(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BattleLobbyExtraSupsRewardSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := BattleLobbyExtraSupsRewardSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), battleLobbyExtraSupsRewardPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"battle_lobby_extra_sups_rewards\".* FROM \"battle_lobby_extra_sups_rewards\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, battleLobbyExtraSupsRewardPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to reload all in BattleLobbyExtraSupsRewardSlice")
	}

	*o = slice

	return nil
}

// BattleLobbyExtraSupsRewardExists checks if the BattleLobbyExtraSupsReward row exists.
func BattleLobbyExtraSupsRewardExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"battle_lobby_extra_sups_rewards\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "boiler: unable to check if battle_lobby_extra_sups_rewards exists")
	}

	return exists, nil
}
