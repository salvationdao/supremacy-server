// Code generated by SQLBoiler 4.8.6 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package boiler

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/shopspring/decimal"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// BlueprintWeaponSkin is an object representing the database table.
type BlueprintWeaponSkin struct {
	ID               string              `boiler:"id" boil:"id" json:"id" toml:"id" yaml:"id"`
	Label            string              `boiler:"label" boil:"label" json:"label" toml:"label" yaml:"label"`
	Tier             string              `boiler:"tier" boil:"tier" json:"tier" toml:"tier" yaml:"tier"`
	CreatedAt        time.Time           `boiler:"created_at" boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	Collection       string              `boiler:"collection" boil:"collection" json:"collection" toml:"collection" yaml:"collection"`
	StatModifier     decimal.NullDecimal `boiler:"stat_modifier" boil:"stat_modifier" json:"stat_modifier,omitempty" toml:"stat_modifier" yaml:"stat_modifier,omitempty"`
	ImageURL         null.String         `boiler:"image_url" boil:"image_url" json:"image_url,omitempty" toml:"image_url" yaml:"image_url,omitempty"`
	AnimationURL     null.String         `boiler:"animation_url" boil:"animation_url" json:"animation_url,omitempty" toml:"animation_url" yaml:"animation_url,omitempty"`
	CardAnimationURL null.String         `boiler:"card_animation_url" boil:"card_animation_url" json:"card_animation_url,omitempty" toml:"card_animation_url" yaml:"card_animation_url,omitempty"`
	LargeImageURL    null.String         `boiler:"large_image_url" boil:"large_image_url" json:"large_image_url,omitempty" toml:"large_image_url" yaml:"large_image_url,omitempty"`
	AvatarURL        null.String         `boiler:"avatar_url" boil:"avatar_url" json:"avatar_url,omitempty" toml:"avatar_url" yaml:"avatar_url,omitempty"`
	BackgroundColor  null.String         `boiler:"background_color" boil:"background_color" json:"background_color,omitempty" toml:"background_color" yaml:"background_color,omitempty"`
	YoutubeURL       null.String         `boiler:"youtube_url" boil:"youtube_url" json:"youtube_url,omitempty" toml:"youtube_url" yaml:"youtube_url,omitempty"`

	R *blueprintWeaponSkinR `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
	L blueprintWeaponSkinL  `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BlueprintWeaponSkinColumns = struct {
	ID               string
	Label            string
	Tier             string
	CreatedAt        string
	Collection       string
	StatModifier     string
	ImageURL         string
	AnimationURL     string
	CardAnimationURL string
	LargeImageURL    string
	AvatarURL        string
	BackgroundColor  string
	YoutubeURL       string
}{
	ID:               "id",
	Label:            "label",
	Tier:             "tier",
	CreatedAt:        "created_at",
	Collection:       "collection",
	StatModifier:     "stat_modifier",
	ImageURL:         "image_url",
	AnimationURL:     "animation_url",
	CardAnimationURL: "card_animation_url",
	LargeImageURL:    "large_image_url",
	AvatarURL:        "avatar_url",
	BackgroundColor:  "background_color",
	YoutubeURL:       "youtube_url",
}

var BlueprintWeaponSkinTableColumns = struct {
	ID               string
	Label            string
	Tier             string
	CreatedAt        string
	Collection       string
	StatModifier     string
	ImageURL         string
	AnimationURL     string
	CardAnimationURL string
	LargeImageURL    string
	AvatarURL        string
	BackgroundColor  string
	YoutubeURL       string
}{
	ID:               "blueprint_weapon_skin.id",
	Label:            "blueprint_weapon_skin.label",
	Tier:             "blueprint_weapon_skin.tier",
	CreatedAt:        "blueprint_weapon_skin.created_at",
	Collection:       "blueprint_weapon_skin.collection",
	StatModifier:     "blueprint_weapon_skin.stat_modifier",
	ImageURL:         "blueprint_weapon_skin.image_url",
	AnimationURL:     "blueprint_weapon_skin.animation_url",
	CardAnimationURL: "blueprint_weapon_skin.card_animation_url",
	LargeImageURL:    "blueprint_weapon_skin.large_image_url",
	AvatarURL:        "blueprint_weapon_skin.avatar_url",
	BackgroundColor:  "blueprint_weapon_skin.background_color",
	YoutubeURL:       "blueprint_weapon_skin.youtube_url",
}

// Generated where

var BlueprintWeaponSkinWhere = struct {
	ID               whereHelperstring
	Label            whereHelperstring
	Tier             whereHelperstring
	CreatedAt        whereHelpertime_Time
	Collection       whereHelperstring
	StatModifier     whereHelperdecimal_NullDecimal
	ImageURL         whereHelpernull_String
	AnimationURL     whereHelpernull_String
	CardAnimationURL whereHelpernull_String
	LargeImageURL    whereHelpernull_String
	AvatarURL        whereHelpernull_String
	BackgroundColor  whereHelpernull_String
	YoutubeURL       whereHelpernull_String
}{
	ID:               whereHelperstring{field: "\"blueprint_weapon_skin\".\"id\""},
	Label:            whereHelperstring{field: "\"blueprint_weapon_skin\".\"label\""},
	Tier:             whereHelperstring{field: "\"blueprint_weapon_skin\".\"tier\""},
	CreatedAt:        whereHelpertime_Time{field: "\"blueprint_weapon_skin\".\"created_at\""},
	Collection:       whereHelperstring{field: "\"blueprint_weapon_skin\".\"collection\""},
	StatModifier:     whereHelperdecimal_NullDecimal{field: "\"blueprint_weapon_skin\".\"stat_modifier\""},
	ImageURL:         whereHelpernull_String{field: "\"blueprint_weapon_skin\".\"image_url\""},
	AnimationURL:     whereHelpernull_String{field: "\"blueprint_weapon_skin\".\"animation_url\""},
	CardAnimationURL: whereHelpernull_String{field: "\"blueprint_weapon_skin\".\"card_animation_url\""},
	LargeImageURL:    whereHelpernull_String{field: "\"blueprint_weapon_skin\".\"large_image_url\""},
	AvatarURL:        whereHelpernull_String{field: "\"blueprint_weapon_skin\".\"avatar_url\""},
	BackgroundColor:  whereHelpernull_String{field: "\"blueprint_weapon_skin\".\"background_color\""},
	YoutubeURL:       whereHelpernull_String{field: "\"blueprint_weapon_skin\".\"youtube_url\""},
}

// BlueprintWeaponSkinRels is where relationship names are stored.
var BlueprintWeaponSkinRels = struct {
	BlueprintMechSkins                           string
	DefaultSkinBlueprintWeapons                  string
	WeaponSkinBlueprintFiatProductItemBlueprints string
	WeaponModelSkinCompatibilities               string
	BlueprintWeaponSkins                         string
}{
	BlueprintMechSkins:                           "BlueprintMechSkins",
	DefaultSkinBlueprintWeapons:                  "DefaultSkinBlueprintWeapons",
	WeaponSkinBlueprintFiatProductItemBlueprints: "WeaponSkinBlueprintFiatProductItemBlueprints",
	WeaponModelSkinCompatibilities:               "WeaponModelSkinCompatibilities",
	BlueprintWeaponSkins:                         "BlueprintWeaponSkins",
}

// blueprintWeaponSkinR is where relationships are stored.
type blueprintWeaponSkinR struct {
	BlueprintMechSkins                           BlueprintMechSkinSlice            `boiler:"BlueprintMechSkins" boil:"BlueprintMechSkins" json:"BlueprintMechSkins" toml:"BlueprintMechSkins" yaml:"BlueprintMechSkins"`
	DefaultSkinBlueprintWeapons                  BlueprintWeaponSlice              `boiler:"DefaultSkinBlueprintWeapons" boil:"DefaultSkinBlueprintWeapons" json:"DefaultSkinBlueprintWeapons" toml:"DefaultSkinBlueprintWeapons" yaml:"DefaultSkinBlueprintWeapons"`
	WeaponSkinBlueprintFiatProductItemBlueprints FiatProductItemBlueprintSlice     `boiler:"WeaponSkinBlueprintFiatProductItemBlueprints" boil:"WeaponSkinBlueprintFiatProductItemBlueprints" json:"WeaponSkinBlueprintFiatProductItemBlueprints" toml:"WeaponSkinBlueprintFiatProductItemBlueprints" yaml:"WeaponSkinBlueprintFiatProductItemBlueprints"`
	WeaponModelSkinCompatibilities               WeaponModelSkinCompatibilitySlice `boiler:"WeaponModelSkinCompatibilities" boil:"WeaponModelSkinCompatibilities" json:"WeaponModelSkinCompatibilities" toml:"WeaponModelSkinCompatibilities" yaml:"WeaponModelSkinCompatibilities"`
	BlueprintWeaponSkins                         WeaponSkinSlice                   `boiler:"BlueprintWeaponSkins" boil:"BlueprintWeaponSkins" json:"BlueprintWeaponSkins" toml:"BlueprintWeaponSkins" yaml:"BlueprintWeaponSkins"`
}

// NewStruct creates a new relationship struct
func (*blueprintWeaponSkinR) NewStruct() *blueprintWeaponSkinR {
	return &blueprintWeaponSkinR{}
}

// blueprintWeaponSkinL is where Load methods for each relationship are stored.
type blueprintWeaponSkinL struct{}

var (
	blueprintWeaponSkinAllColumns            = []string{"id", "label", "tier", "created_at", "collection", "stat_modifier", "image_url", "animation_url", "card_animation_url", "large_image_url", "avatar_url", "background_color", "youtube_url"}
	blueprintWeaponSkinColumnsWithoutDefault = []string{"label"}
	blueprintWeaponSkinColumnsWithDefault    = []string{"id", "tier", "created_at", "collection", "stat_modifier", "image_url", "animation_url", "card_animation_url", "large_image_url", "avatar_url", "background_color", "youtube_url"}
	blueprintWeaponSkinPrimaryKeyColumns     = []string{"id"}
	blueprintWeaponSkinGeneratedColumns      = []string{}
)

type (
	// BlueprintWeaponSkinSlice is an alias for a slice of pointers to BlueprintWeaponSkin.
	// This should almost always be used instead of []BlueprintWeaponSkin.
	BlueprintWeaponSkinSlice []*BlueprintWeaponSkin
	// BlueprintWeaponSkinHook is the signature for custom BlueprintWeaponSkin hook methods
	BlueprintWeaponSkinHook func(boil.Executor, *BlueprintWeaponSkin) error

	blueprintWeaponSkinQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	blueprintWeaponSkinType                 = reflect.TypeOf(&BlueprintWeaponSkin{})
	blueprintWeaponSkinMapping              = queries.MakeStructMapping(blueprintWeaponSkinType)
	blueprintWeaponSkinPrimaryKeyMapping, _ = queries.BindMapping(blueprintWeaponSkinType, blueprintWeaponSkinMapping, blueprintWeaponSkinPrimaryKeyColumns)
	blueprintWeaponSkinInsertCacheMut       sync.RWMutex
	blueprintWeaponSkinInsertCache          = make(map[string]insertCache)
	blueprintWeaponSkinUpdateCacheMut       sync.RWMutex
	blueprintWeaponSkinUpdateCache          = make(map[string]updateCache)
	blueprintWeaponSkinUpsertCacheMut       sync.RWMutex
	blueprintWeaponSkinUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var blueprintWeaponSkinAfterSelectHooks []BlueprintWeaponSkinHook

var blueprintWeaponSkinBeforeInsertHooks []BlueprintWeaponSkinHook
var blueprintWeaponSkinAfterInsertHooks []BlueprintWeaponSkinHook

var blueprintWeaponSkinBeforeUpdateHooks []BlueprintWeaponSkinHook
var blueprintWeaponSkinAfterUpdateHooks []BlueprintWeaponSkinHook

var blueprintWeaponSkinBeforeDeleteHooks []BlueprintWeaponSkinHook
var blueprintWeaponSkinAfterDeleteHooks []BlueprintWeaponSkinHook

var blueprintWeaponSkinBeforeUpsertHooks []BlueprintWeaponSkinHook
var blueprintWeaponSkinAfterUpsertHooks []BlueprintWeaponSkinHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *BlueprintWeaponSkin) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range blueprintWeaponSkinAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *BlueprintWeaponSkin) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range blueprintWeaponSkinBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *BlueprintWeaponSkin) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range blueprintWeaponSkinAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *BlueprintWeaponSkin) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range blueprintWeaponSkinBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *BlueprintWeaponSkin) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range blueprintWeaponSkinAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *BlueprintWeaponSkin) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range blueprintWeaponSkinBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *BlueprintWeaponSkin) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range blueprintWeaponSkinAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *BlueprintWeaponSkin) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range blueprintWeaponSkinBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *BlueprintWeaponSkin) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range blueprintWeaponSkinAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddBlueprintWeaponSkinHook registers your hook function for all future operations.
func AddBlueprintWeaponSkinHook(hookPoint boil.HookPoint, blueprintWeaponSkinHook BlueprintWeaponSkinHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		blueprintWeaponSkinAfterSelectHooks = append(blueprintWeaponSkinAfterSelectHooks, blueprintWeaponSkinHook)
	case boil.BeforeInsertHook:
		blueprintWeaponSkinBeforeInsertHooks = append(blueprintWeaponSkinBeforeInsertHooks, blueprintWeaponSkinHook)
	case boil.AfterInsertHook:
		blueprintWeaponSkinAfterInsertHooks = append(blueprintWeaponSkinAfterInsertHooks, blueprintWeaponSkinHook)
	case boil.BeforeUpdateHook:
		blueprintWeaponSkinBeforeUpdateHooks = append(blueprintWeaponSkinBeforeUpdateHooks, blueprintWeaponSkinHook)
	case boil.AfterUpdateHook:
		blueprintWeaponSkinAfterUpdateHooks = append(blueprintWeaponSkinAfterUpdateHooks, blueprintWeaponSkinHook)
	case boil.BeforeDeleteHook:
		blueprintWeaponSkinBeforeDeleteHooks = append(blueprintWeaponSkinBeforeDeleteHooks, blueprintWeaponSkinHook)
	case boil.AfterDeleteHook:
		blueprintWeaponSkinAfterDeleteHooks = append(blueprintWeaponSkinAfterDeleteHooks, blueprintWeaponSkinHook)
	case boil.BeforeUpsertHook:
		blueprintWeaponSkinBeforeUpsertHooks = append(blueprintWeaponSkinBeforeUpsertHooks, blueprintWeaponSkinHook)
	case boil.AfterUpsertHook:
		blueprintWeaponSkinAfterUpsertHooks = append(blueprintWeaponSkinAfterUpsertHooks, blueprintWeaponSkinHook)
	}
}

// One returns a single blueprintWeaponSkin record from the query.
func (q blueprintWeaponSkinQuery) One(exec boil.Executor) (*BlueprintWeaponSkin, error) {
	o := &BlueprintWeaponSkin{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: failed to execute a one query for blueprint_weapon_skin")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all BlueprintWeaponSkin records from the query.
func (q blueprintWeaponSkinQuery) All(exec boil.Executor) (BlueprintWeaponSkinSlice, error) {
	var o []*BlueprintWeaponSkin

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "boiler: failed to assign all query results to BlueprintWeaponSkin slice")
	}

	if len(blueprintWeaponSkinAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all BlueprintWeaponSkin records in the query.
func (q blueprintWeaponSkinQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to count blueprint_weapon_skin rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q blueprintWeaponSkinQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "boiler: failed to check if blueprint_weapon_skin exists")
	}

	return count > 0, nil
}

// BlueprintMechSkins retrieves all the blueprint_mech_skin's BlueprintMechSkins with an executor.
func (o *BlueprintWeaponSkin) BlueprintMechSkins(mods ...qm.QueryMod) blueprintMechSkinQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"blueprint_mech_skin\".\"blueprint_weapon_skin_id\"=?", o.ID),
	)

	query := BlueprintMechSkins(queryMods...)
	queries.SetFrom(query.Query, "\"blueprint_mech_skin\"")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"\"blueprint_mech_skin\".*"})
	}

	return query
}

// DefaultSkinBlueprintWeapons retrieves all the blueprint_weapon's BlueprintWeapons with an executor via default_skin_id column.
func (o *BlueprintWeaponSkin) DefaultSkinBlueprintWeapons(mods ...qm.QueryMod) blueprintWeaponQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"blueprint_weapons\".\"default_skin_id\"=?", o.ID),
		qmhelper.WhereIsNull("\"blueprint_weapons\".\"deleted_at\""),
	)

	query := BlueprintWeapons(queryMods...)
	queries.SetFrom(query.Query, "\"blueprint_weapons\"")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"\"blueprint_weapons\".*"})
	}

	return query
}

// WeaponSkinBlueprintFiatProductItemBlueprints retrieves all the fiat_product_item_blueprint's FiatProductItemBlueprints with an executor via weapon_skin_blueprint_id column.
func (o *BlueprintWeaponSkin) WeaponSkinBlueprintFiatProductItemBlueprints(mods ...qm.QueryMod) fiatProductItemBlueprintQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"fiat_product_item_blueprints\".\"weapon_skin_blueprint_id\"=?", o.ID),
	)

	query := FiatProductItemBlueprints(queryMods...)
	queries.SetFrom(query.Query, "\"fiat_product_item_blueprints\"")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"\"fiat_product_item_blueprints\".*"})
	}

	return query
}

// WeaponModelSkinCompatibilities retrieves all the weapon_model_skin_compatibility's WeaponModelSkinCompatibilities with an executor.
func (o *BlueprintWeaponSkin) WeaponModelSkinCompatibilities(mods ...qm.QueryMod) weaponModelSkinCompatibilityQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"weapon_model_skin_compatibilities\".\"blueprint_weapon_skin_id\"=?", o.ID),
		qmhelper.WhereIsNull("\"weapon_model_skin_compatibilities\".\"deleted_at\""),
	)

	query := WeaponModelSkinCompatibilities(queryMods...)
	queries.SetFrom(query.Query, "\"weapon_model_skin_compatibilities\"")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"\"weapon_model_skin_compatibilities\".*"})
	}

	return query
}

// BlueprintWeaponSkins retrieves all the weapon_skin's WeaponSkins with an executor via blueprint_id column.
func (o *BlueprintWeaponSkin) BlueprintWeaponSkins(mods ...qm.QueryMod) weaponSkinQuery {
	var queryMods []qm.QueryMod
	if len(mods) != 0 {
		queryMods = append(queryMods, mods...)
	}

	queryMods = append(queryMods,
		qm.Where("\"weapon_skin\".\"blueprint_id\"=?", o.ID),
	)

	query := WeaponSkins(queryMods...)
	queries.SetFrom(query.Query, "\"weapon_skin\"")

	if len(queries.GetSelect(query.Query)) == 0 {
		queries.SetSelect(query.Query, []string{"\"weapon_skin\".*"})
	}

	return query
}

// LoadBlueprintMechSkins allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (blueprintWeaponSkinL) LoadBlueprintMechSkins(e boil.Executor, singular bool, maybeBlueprintWeaponSkin interface{}, mods queries.Applicator) error {
	var slice []*BlueprintWeaponSkin
	var object *BlueprintWeaponSkin

	if singular {
		object = maybeBlueprintWeaponSkin.(*BlueprintWeaponSkin)
	} else {
		slice = *maybeBlueprintWeaponSkin.(*[]*BlueprintWeaponSkin)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &blueprintWeaponSkinR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &blueprintWeaponSkinR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.ID) {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`blueprint_mech_skin`),
		qm.WhereIn(`blueprint_mech_skin.blueprint_weapon_skin_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load blueprint_mech_skin")
	}

	var resultSlice []*BlueprintMechSkin
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice blueprint_mech_skin")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on blueprint_mech_skin")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for blueprint_mech_skin")
	}

	if len(blueprintMechSkinAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.BlueprintMechSkins = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &blueprintMechSkinR{}
			}
			foreign.R.BlueprintWeaponSkin = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.BlueprintWeaponSkinID) {
				local.R.BlueprintMechSkins = append(local.R.BlueprintMechSkins, foreign)
				if foreign.R == nil {
					foreign.R = &blueprintMechSkinR{}
				}
				foreign.R.BlueprintWeaponSkin = local
				break
			}
		}
	}

	return nil
}

// LoadDefaultSkinBlueprintWeapons allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (blueprintWeaponSkinL) LoadDefaultSkinBlueprintWeapons(e boil.Executor, singular bool, maybeBlueprintWeaponSkin interface{}, mods queries.Applicator) error {
	var slice []*BlueprintWeaponSkin
	var object *BlueprintWeaponSkin

	if singular {
		object = maybeBlueprintWeaponSkin.(*BlueprintWeaponSkin)
	} else {
		slice = *maybeBlueprintWeaponSkin.(*[]*BlueprintWeaponSkin)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &blueprintWeaponSkinR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &blueprintWeaponSkinR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`blueprint_weapons`),
		qm.WhereIn(`blueprint_weapons.default_skin_id in ?`, args...),
		qmhelper.WhereIsNull(`blueprint_weapons.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load blueprint_weapons")
	}

	var resultSlice []*BlueprintWeapon
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice blueprint_weapons")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on blueprint_weapons")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for blueprint_weapons")
	}

	if len(blueprintWeaponAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.DefaultSkinBlueprintWeapons = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &blueprintWeaponR{}
			}
			foreign.R.DefaultSkin = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.DefaultSkinID {
				local.R.DefaultSkinBlueprintWeapons = append(local.R.DefaultSkinBlueprintWeapons, foreign)
				if foreign.R == nil {
					foreign.R = &blueprintWeaponR{}
				}
				foreign.R.DefaultSkin = local
				break
			}
		}
	}

	return nil
}

// LoadWeaponSkinBlueprintFiatProductItemBlueprints allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (blueprintWeaponSkinL) LoadWeaponSkinBlueprintFiatProductItemBlueprints(e boil.Executor, singular bool, maybeBlueprintWeaponSkin interface{}, mods queries.Applicator) error {
	var slice []*BlueprintWeaponSkin
	var object *BlueprintWeaponSkin

	if singular {
		object = maybeBlueprintWeaponSkin.(*BlueprintWeaponSkin)
	} else {
		slice = *maybeBlueprintWeaponSkin.(*[]*BlueprintWeaponSkin)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &blueprintWeaponSkinR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &blueprintWeaponSkinR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.ID) {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`fiat_product_item_blueprints`),
		qm.WhereIn(`fiat_product_item_blueprints.weapon_skin_blueprint_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load fiat_product_item_blueprints")
	}

	var resultSlice []*FiatProductItemBlueprint
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice fiat_product_item_blueprints")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on fiat_product_item_blueprints")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for fiat_product_item_blueprints")
	}

	if len(fiatProductItemBlueprintAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.WeaponSkinBlueprintFiatProductItemBlueprints = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &fiatProductItemBlueprintR{}
			}
			foreign.R.WeaponSkinBlueprint = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if queries.Equal(local.ID, foreign.WeaponSkinBlueprintID) {
				local.R.WeaponSkinBlueprintFiatProductItemBlueprints = append(local.R.WeaponSkinBlueprintFiatProductItemBlueprints, foreign)
				if foreign.R == nil {
					foreign.R = &fiatProductItemBlueprintR{}
				}
				foreign.R.WeaponSkinBlueprint = local
				break
			}
		}
	}

	return nil
}

// LoadWeaponModelSkinCompatibilities allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (blueprintWeaponSkinL) LoadWeaponModelSkinCompatibilities(e boil.Executor, singular bool, maybeBlueprintWeaponSkin interface{}, mods queries.Applicator) error {
	var slice []*BlueprintWeaponSkin
	var object *BlueprintWeaponSkin

	if singular {
		object = maybeBlueprintWeaponSkin.(*BlueprintWeaponSkin)
	} else {
		slice = *maybeBlueprintWeaponSkin.(*[]*BlueprintWeaponSkin)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &blueprintWeaponSkinR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &blueprintWeaponSkinR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`weapon_model_skin_compatibilities`),
		qm.WhereIn(`weapon_model_skin_compatibilities.blueprint_weapon_skin_id in ?`, args...),
		qmhelper.WhereIsNull(`weapon_model_skin_compatibilities.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load weapon_model_skin_compatibilities")
	}

	var resultSlice []*WeaponModelSkinCompatibility
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice weapon_model_skin_compatibilities")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on weapon_model_skin_compatibilities")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for weapon_model_skin_compatibilities")
	}

	if len(weaponModelSkinCompatibilityAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.WeaponModelSkinCompatibilities = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &weaponModelSkinCompatibilityR{}
			}
			foreign.R.BlueprintWeaponSkin = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.BlueprintWeaponSkinID {
				local.R.WeaponModelSkinCompatibilities = append(local.R.WeaponModelSkinCompatibilities, foreign)
				if foreign.R == nil {
					foreign.R = &weaponModelSkinCompatibilityR{}
				}
				foreign.R.BlueprintWeaponSkin = local
				break
			}
		}
	}

	return nil
}

// LoadBlueprintWeaponSkins allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for a 1-M or N-M relationship.
func (blueprintWeaponSkinL) LoadBlueprintWeaponSkins(e boil.Executor, singular bool, maybeBlueprintWeaponSkin interface{}, mods queries.Applicator) error {
	var slice []*BlueprintWeaponSkin
	var object *BlueprintWeaponSkin

	if singular {
		object = maybeBlueprintWeaponSkin.(*BlueprintWeaponSkin)
	} else {
		slice = *maybeBlueprintWeaponSkin.(*[]*BlueprintWeaponSkin)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &blueprintWeaponSkinR{}
		}
		args = append(args, object.ID)
	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &blueprintWeaponSkinR{}
			}

			for _, a := range args {
				if a == obj.ID {
					continue Outer
				}
			}

			args = append(args, obj.ID)
		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`weapon_skin`),
		qm.WhereIn(`weapon_skin.blueprint_id in ?`, args...),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load weapon_skin")
	}

	var resultSlice []*WeaponSkin
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice weapon_skin")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results in eager load on weapon_skin")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for weapon_skin")
	}

	if len(weaponSkinAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}
	if singular {
		object.R.BlueprintWeaponSkins = resultSlice
		for _, foreign := range resultSlice {
			if foreign.R == nil {
				foreign.R = &weaponSkinR{}
			}
			foreign.R.Blueprint = object
		}
		return nil
	}

	for _, foreign := range resultSlice {
		for _, local := range slice {
			if local.ID == foreign.BlueprintID {
				local.R.BlueprintWeaponSkins = append(local.R.BlueprintWeaponSkins, foreign)
				if foreign.R == nil {
					foreign.R = &weaponSkinR{}
				}
				foreign.R.Blueprint = local
				break
			}
		}
	}

	return nil
}

// AddBlueprintMechSkins adds the given related objects to the existing relationships
// of the blueprint_weapon_skin, optionally inserting them as new records.
// Appends related to o.R.BlueprintMechSkins.
// Sets related.R.BlueprintWeaponSkin appropriately.
func (o *BlueprintWeaponSkin) AddBlueprintMechSkins(exec boil.Executor, insert bool, related ...*BlueprintMechSkin) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.BlueprintWeaponSkinID, o.ID)
			if err = rel.Insert(exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"blueprint_mech_skin\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"blueprint_weapon_skin_id"}),
				strmangle.WhereClause("\"", "\"", 2, blueprintMechSkinPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}
			if _, err = exec.Exec(updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.BlueprintWeaponSkinID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &blueprintWeaponSkinR{
			BlueprintMechSkins: related,
		}
	} else {
		o.R.BlueprintMechSkins = append(o.R.BlueprintMechSkins, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &blueprintMechSkinR{
				BlueprintWeaponSkin: o,
			}
		} else {
			rel.R.BlueprintWeaponSkin = o
		}
	}
	return nil
}

// SetBlueprintMechSkins removes all previously related items of the
// blueprint_weapon_skin replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.BlueprintWeaponSkin's BlueprintMechSkins accordingly.
// Replaces o.R.BlueprintMechSkins with related.
// Sets related.R.BlueprintWeaponSkin's BlueprintMechSkins accordingly.
func (o *BlueprintWeaponSkin) SetBlueprintMechSkins(exec boil.Executor, insert bool, related ...*BlueprintMechSkin) error {
	query := "update \"blueprint_mech_skin\" set \"blueprint_weapon_skin_id\" = null where \"blueprint_weapon_skin_id\" = $1"
	values := []interface{}{o.ID}
	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	_, err := exec.Exec(query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.BlueprintMechSkins {
			queries.SetScanner(&rel.BlueprintWeaponSkinID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.BlueprintWeaponSkin = nil
		}

		o.R.BlueprintMechSkins = nil
	}
	return o.AddBlueprintMechSkins(exec, insert, related...)
}

// RemoveBlueprintMechSkins relationships from objects passed in.
// Removes related items from R.BlueprintMechSkins (uses pointer comparison, removal does not keep order)
// Sets related.R.BlueprintWeaponSkin.
func (o *BlueprintWeaponSkin) RemoveBlueprintMechSkins(exec boil.Executor, related ...*BlueprintMechSkin) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.BlueprintWeaponSkinID, nil)
		if rel.R != nil {
			rel.R.BlueprintWeaponSkin = nil
		}
		if _, err = rel.Update(exec, boil.Whitelist("blueprint_weapon_skin_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.BlueprintMechSkins {
			if rel != ri {
				continue
			}

			ln := len(o.R.BlueprintMechSkins)
			if ln > 1 && i < ln-1 {
				o.R.BlueprintMechSkins[i] = o.R.BlueprintMechSkins[ln-1]
			}
			o.R.BlueprintMechSkins = o.R.BlueprintMechSkins[:ln-1]
			break
		}
	}

	return nil
}

// AddDefaultSkinBlueprintWeapons adds the given related objects to the existing relationships
// of the blueprint_weapon_skin, optionally inserting them as new records.
// Appends related to o.R.DefaultSkinBlueprintWeapons.
// Sets related.R.DefaultSkin appropriately.
func (o *BlueprintWeaponSkin) AddDefaultSkinBlueprintWeapons(exec boil.Executor, insert bool, related ...*BlueprintWeapon) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.DefaultSkinID = o.ID
			if err = rel.Insert(exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"blueprint_weapons\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"default_skin_id"}),
				strmangle.WhereClause("\"", "\"", 2, blueprintWeaponPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}
			if _, err = exec.Exec(updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.DefaultSkinID = o.ID
		}
	}

	if o.R == nil {
		o.R = &blueprintWeaponSkinR{
			DefaultSkinBlueprintWeapons: related,
		}
	} else {
		o.R.DefaultSkinBlueprintWeapons = append(o.R.DefaultSkinBlueprintWeapons, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &blueprintWeaponR{
				DefaultSkin: o,
			}
		} else {
			rel.R.DefaultSkin = o
		}
	}
	return nil
}

// AddWeaponSkinBlueprintFiatProductItemBlueprints adds the given related objects to the existing relationships
// of the blueprint_weapon_skin, optionally inserting them as new records.
// Appends related to o.R.WeaponSkinBlueprintFiatProductItemBlueprints.
// Sets related.R.WeaponSkinBlueprint appropriately.
func (o *BlueprintWeaponSkin) AddWeaponSkinBlueprintFiatProductItemBlueprints(exec boil.Executor, insert bool, related ...*FiatProductItemBlueprint) error {
	var err error
	for _, rel := range related {
		if insert {
			queries.Assign(&rel.WeaponSkinBlueprintID, o.ID)
			if err = rel.Insert(exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"fiat_product_item_blueprints\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"weapon_skin_blueprint_id"}),
				strmangle.WhereClause("\"", "\"", 2, fiatProductItemBlueprintPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}
			if _, err = exec.Exec(updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			queries.Assign(&rel.WeaponSkinBlueprintID, o.ID)
		}
	}

	if o.R == nil {
		o.R = &blueprintWeaponSkinR{
			WeaponSkinBlueprintFiatProductItemBlueprints: related,
		}
	} else {
		o.R.WeaponSkinBlueprintFiatProductItemBlueprints = append(o.R.WeaponSkinBlueprintFiatProductItemBlueprints, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &fiatProductItemBlueprintR{
				WeaponSkinBlueprint: o,
			}
		} else {
			rel.R.WeaponSkinBlueprint = o
		}
	}
	return nil
}

// SetWeaponSkinBlueprintFiatProductItemBlueprints removes all previously related items of the
// blueprint_weapon_skin replacing them completely with the passed
// in related items, optionally inserting them as new records.
// Sets o.R.WeaponSkinBlueprint's WeaponSkinBlueprintFiatProductItemBlueprints accordingly.
// Replaces o.R.WeaponSkinBlueprintFiatProductItemBlueprints with related.
// Sets related.R.WeaponSkinBlueprint's WeaponSkinBlueprintFiatProductItemBlueprints accordingly.
func (o *BlueprintWeaponSkin) SetWeaponSkinBlueprintFiatProductItemBlueprints(exec boil.Executor, insert bool, related ...*FiatProductItemBlueprint) error {
	query := "update \"fiat_product_item_blueprints\" set \"weapon_skin_blueprint_id\" = null where \"weapon_skin_blueprint_id\" = $1"
	values := []interface{}{o.ID}
	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	_, err := exec.Exec(query, values...)
	if err != nil {
		return errors.Wrap(err, "failed to remove relationships before set")
	}

	if o.R != nil {
		for _, rel := range o.R.WeaponSkinBlueprintFiatProductItemBlueprints {
			queries.SetScanner(&rel.WeaponSkinBlueprintID, nil)
			if rel.R == nil {
				continue
			}

			rel.R.WeaponSkinBlueprint = nil
		}

		o.R.WeaponSkinBlueprintFiatProductItemBlueprints = nil
	}
	return o.AddWeaponSkinBlueprintFiatProductItemBlueprints(exec, insert, related...)
}

// RemoveWeaponSkinBlueprintFiatProductItemBlueprints relationships from objects passed in.
// Removes related items from R.WeaponSkinBlueprintFiatProductItemBlueprints (uses pointer comparison, removal does not keep order)
// Sets related.R.WeaponSkinBlueprint.
func (o *BlueprintWeaponSkin) RemoveWeaponSkinBlueprintFiatProductItemBlueprints(exec boil.Executor, related ...*FiatProductItemBlueprint) error {
	if len(related) == 0 {
		return nil
	}

	var err error
	for _, rel := range related {
		queries.SetScanner(&rel.WeaponSkinBlueprintID, nil)
		if rel.R != nil {
			rel.R.WeaponSkinBlueprint = nil
		}
		if _, err = rel.Update(exec, boil.Whitelist("weapon_skin_blueprint_id")); err != nil {
			return err
		}
	}
	if o.R == nil {
		return nil
	}

	for _, rel := range related {
		for i, ri := range o.R.WeaponSkinBlueprintFiatProductItemBlueprints {
			if rel != ri {
				continue
			}

			ln := len(o.R.WeaponSkinBlueprintFiatProductItemBlueprints)
			if ln > 1 && i < ln-1 {
				o.R.WeaponSkinBlueprintFiatProductItemBlueprints[i] = o.R.WeaponSkinBlueprintFiatProductItemBlueprints[ln-1]
			}
			o.R.WeaponSkinBlueprintFiatProductItemBlueprints = o.R.WeaponSkinBlueprintFiatProductItemBlueprints[:ln-1]
			break
		}
	}

	return nil
}

// AddWeaponModelSkinCompatibilities adds the given related objects to the existing relationships
// of the blueprint_weapon_skin, optionally inserting them as new records.
// Appends related to o.R.WeaponModelSkinCompatibilities.
// Sets related.R.BlueprintWeaponSkin appropriately.
func (o *BlueprintWeaponSkin) AddWeaponModelSkinCompatibilities(exec boil.Executor, insert bool, related ...*WeaponModelSkinCompatibility) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.BlueprintWeaponSkinID = o.ID
			if err = rel.Insert(exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"weapon_model_skin_compatibilities\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"blueprint_weapon_skin_id"}),
				strmangle.WhereClause("\"", "\"", 2, weaponModelSkinCompatibilityPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.BlueprintWeaponSkinID, rel.WeaponModelID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}
			if _, err = exec.Exec(updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.BlueprintWeaponSkinID = o.ID
		}
	}

	if o.R == nil {
		o.R = &blueprintWeaponSkinR{
			WeaponModelSkinCompatibilities: related,
		}
	} else {
		o.R.WeaponModelSkinCompatibilities = append(o.R.WeaponModelSkinCompatibilities, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &weaponModelSkinCompatibilityR{
				BlueprintWeaponSkin: o,
			}
		} else {
			rel.R.BlueprintWeaponSkin = o
		}
	}
	return nil
}

// AddBlueprintWeaponSkins adds the given related objects to the existing relationships
// of the blueprint_weapon_skin, optionally inserting them as new records.
// Appends related to o.R.BlueprintWeaponSkins.
// Sets related.R.Blueprint appropriately.
func (o *BlueprintWeaponSkin) AddBlueprintWeaponSkins(exec boil.Executor, insert bool, related ...*WeaponSkin) error {
	var err error
	for _, rel := range related {
		if insert {
			rel.BlueprintID = o.ID
			if err = rel.Insert(exec, boil.Infer()); err != nil {
				return errors.Wrap(err, "failed to insert into foreign table")
			}
		} else {
			updateQuery := fmt.Sprintf(
				"UPDATE \"weapon_skin\" SET %s WHERE %s",
				strmangle.SetParamNames("\"", "\"", 1, []string{"blueprint_id"}),
				strmangle.WhereClause("\"", "\"", 2, weaponSkinPrimaryKeyColumns),
			)
			values := []interface{}{o.ID, rel.ID}

			if boil.DebugMode {
				fmt.Fprintln(boil.DebugWriter, updateQuery)
				fmt.Fprintln(boil.DebugWriter, values)
			}
			if _, err = exec.Exec(updateQuery, values...); err != nil {
				return errors.Wrap(err, "failed to update foreign table")
			}

			rel.BlueprintID = o.ID
		}
	}

	if o.R == nil {
		o.R = &blueprintWeaponSkinR{
			BlueprintWeaponSkins: related,
		}
	} else {
		o.R.BlueprintWeaponSkins = append(o.R.BlueprintWeaponSkins, related...)
	}

	for _, rel := range related {
		if rel.R == nil {
			rel.R = &weaponSkinR{
				Blueprint: o,
			}
		} else {
			rel.R.Blueprint = o
		}
	}
	return nil
}

// BlueprintWeaponSkins retrieves all the records using an executor.
func BlueprintWeaponSkins(mods ...qm.QueryMod) blueprintWeaponSkinQuery {
	mods = append(mods, qm.From("\"blueprint_weapon_skin\""))
	return blueprintWeaponSkinQuery{NewQuery(mods...)}
}

// FindBlueprintWeaponSkin retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBlueprintWeaponSkin(exec boil.Executor, iD string, selectCols ...string) (*BlueprintWeaponSkin, error) {
	blueprintWeaponSkinObj := &BlueprintWeaponSkin{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"blueprint_weapon_skin\" where \"id\"=$1", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, blueprintWeaponSkinObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: unable to select from blueprint_weapon_skin")
	}

	if err = blueprintWeaponSkinObj.doAfterSelectHooks(exec); err != nil {
		return blueprintWeaponSkinObj, err
	}

	return blueprintWeaponSkinObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *BlueprintWeaponSkin) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no blueprint_weapon_skin provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(blueprintWeaponSkinColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	blueprintWeaponSkinInsertCacheMut.RLock()
	cache, cached := blueprintWeaponSkinInsertCache[key]
	blueprintWeaponSkinInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			blueprintWeaponSkinAllColumns,
			blueprintWeaponSkinColumnsWithDefault,
			blueprintWeaponSkinColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(blueprintWeaponSkinType, blueprintWeaponSkinMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(blueprintWeaponSkinType, blueprintWeaponSkinMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"blueprint_weapon_skin\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"blueprint_weapon_skin\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "boiler: unable to insert into blueprint_weapon_skin")
	}

	if !cached {
		blueprintWeaponSkinInsertCacheMut.Lock()
		blueprintWeaponSkinInsertCache[key] = cache
		blueprintWeaponSkinInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// Update uses an executor to update the BlueprintWeaponSkin.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *BlueprintWeaponSkin) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	blueprintWeaponSkinUpdateCacheMut.RLock()
	cache, cached := blueprintWeaponSkinUpdateCache[key]
	blueprintWeaponSkinUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			blueprintWeaponSkinAllColumns,
			blueprintWeaponSkinPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("boiler: unable to update blueprint_weapon_skin, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"blueprint_weapon_skin\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, blueprintWeaponSkinPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(blueprintWeaponSkinType, blueprintWeaponSkinMapping, append(wl, blueprintWeaponSkinPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update blueprint_weapon_skin row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by update for blueprint_weapon_skin")
	}

	if !cached {
		blueprintWeaponSkinUpdateCacheMut.Lock()
		blueprintWeaponSkinUpdateCache[key] = cache
		blueprintWeaponSkinUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q blueprintWeaponSkinQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all for blueprint_weapon_skin")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected for blueprint_weapon_skin")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BlueprintWeaponSkinSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("boiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), blueprintWeaponSkinPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"blueprint_weapon_skin\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, blueprintWeaponSkinPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all in blueprintWeaponSkin slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected all in update all blueprintWeaponSkin")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *BlueprintWeaponSkin) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no blueprint_weapon_skin provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(blueprintWeaponSkinColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	blueprintWeaponSkinUpsertCacheMut.RLock()
	cache, cached := blueprintWeaponSkinUpsertCache[key]
	blueprintWeaponSkinUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			blueprintWeaponSkinAllColumns,
			blueprintWeaponSkinColumnsWithDefault,
			blueprintWeaponSkinColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			blueprintWeaponSkinAllColumns,
			blueprintWeaponSkinPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("boiler: unable to upsert blueprint_weapon_skin, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(blueprintWeaponSkinPrimaryKeyColumns))
			copy(conflict, blueprintWeaponSkinPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"blueprint_weapon_skin\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(blueprintWeaponSkinType, blueprintWeaponSkinMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(blueprintWeaponSkinType, blueprintWeaponSkinMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "boiler: unable to upsert blueprint_weapon_skin")
	}

	if !cached {
		blueprintWeaponSkinUpsertCacheMut.Lock()
		blueprintWeaponSkinUpsertCache[key] = cache
		blueprintWeaponSkinUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// Delete deletes a single BlueprintWeaponSkin record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BlueprintWeaponSkin) Delete(exec boil.Executor) (int64, error) {
	if o == nil {
		return 0, errors.New("boiler: no BlueprintWeaponSkin provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	args := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), blueprintWeaponSkinPrimaryKeyMapping)
	sql := "DELETE FROM \"blueprint_weapon_skin\" WHERE \"id\"=$1"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete from blueprint_weapon_skin")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by delete for blueprint_weapon_skin")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q blueprintWeaponSkinQuery) DeleteAll(exec boil.Executor) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("boiler: no blueprintWeaponSkinQuery provided for delete all")
	}

	queries.SetDelete(q.Query)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from blueprint_weapon_skin")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for blueprint_weapon_skin")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BlueprintWeaponSkinSlice) DeleteAll(exec boil.Executor) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(blueprintWeaponSkinBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var args []interface{}
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), blueprintWeaponSkinPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "DELETE FROM \"blueprint_weapon_skin\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, blueprintWeaponSkinPrimaryKeyColumns, len(o))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from blueprintWeaponSkin slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for blueprint_weapon_skin")
	}

	if len(blueprintWeaponSkinAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BlueprintWeaponSkin) Reload(exec boil.Executor) error {
	ret, err := FindBlueprintWeaponSkin(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BlueprintWeaponSkinSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := BlueprintWeaponSkinSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), blueprintWeaponSkinPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"blueprint_weapon_skin\".* FROM \"blueprint_weapon_skin\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, blueprintWeaponSkinPrimaryKeyColumns, len(*o))

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to reload all in BlueprintWeaponSkinSlice")
	}

	*o = slice

	return nil
}

// BlueprintWeaponSkinExists checks if the BlueprintWeaponSkin row exists.
func BlueprintWeaponSkinExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"blueprint_weapon_skin\" where \"id\"=$1 limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "boiler: unable to check if blueprint_weapon_skin exists")
	}

	return exists, nil
}
