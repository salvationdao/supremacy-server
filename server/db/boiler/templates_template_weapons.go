// Code generated by SQLBoiler 4.8.6 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package boiler

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TemplatesTemplateWeapon is an object representing the database table.
type TemplatesTemplateWeapon struct {
	ID               string    `boiler:"id" boil:"id" json:"id" toml:"id" yaml:"id"`
	TemplateWeaponID string    `boiler:"template_weapon_id" boil:"template_weapon_id" json:"templateWeaponID" toml:"templateWeaponID" yaml:"templateWeaponID"`
	TemplateID       string    `boiler:"template_id" boil:"template_id" json:"templateID" toml:"templateID" yaml:"templateID"`
	DeletedAt        null.Time `boiler:"deleted_at" boil:"deleted_at" json:"deletedAt,omitempty" toml:"deletedAt" yaml:"deletedAt,omitempty"`
	UpdatedAt        time.Time `boiler:"updated_at" boil:"updated_at" json:"updatedAt" toml:"updatedAt" yaml:"updatedAt"`
	CreatedAt        time.Time `boiler:"created_at" boil:"created_at" json:"createdAt" toml:"createdAt" yaml:"createdAt"`

	R *templatesTemplateWeaponR `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
	L templatesTemplateWeaponL  `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TemplatesTemplateWeaponColumns = struct {
	ID               string
	TemplateWeaponID string
	TemplateID       string
	DeletedAt        string
	UpdatedAt        string
	CreatedAt        string
}{
	ID:               "id",
	TemplateWeaponID: "template_weapon_id",
	TemplateID:       "template_id",
	DeletedAt:        "deleted_at",
	UpdatedAt:        "updated_at",
	CreatedAt:        "created_at",
}

var TemplatesTemplateWeaponTableColumns = struct {
	ID               string
	TemplateWeaponID string
	TemplateID       string
	DeletedAt        string
	UpdatedAt        string
	CreatedAt        string
}{
	ID:               "templates_template_weapons.id",
	TemplateWeaponID: "templates_template_weapons.template_weapon_id",
	TemplateID:       "templates_template_weapons.template_id",
	DeletedAt:        "templates_template_weapons.deleted_at",
	UpdatedAt:        "templates_template_weapons.updated_at",
	CreatedAt:        "templates_template_weapons.created_at",
}

// Generated where

var TemplatesTemplateWeaponWhere = struct {
	ID               whereHelperstring
	TemplateWeaponID whereHelperstring
	TemplateID       whereHelperstring
	DeletedAt        whereHelpernull_Time
	UpdatedAt        whereHelpertime_Time
	CreatedAt        whereHelpertime_Time
}{
	ID:               whereHelperstring{field: "\"templates_template_weapons\".\"id\""},
	TemplateWeaponID: whereHelperstring{field: "\"templates_template_weapons\".\"template_weapon_id\""},
	TemplateID:       whereHelperstring{field: "\"templates_template_weapons\".\"template_id\""},
	DeletedAt:        whereHelpernull_Time{field: "\"templates_template_weapons\".\"deleted_at\""},
	UpdatedAt:        whereHelpertime_Time{field: "\"templates_template_weapons\".\"updated_at\""},
	CreatedAt:        whereHelpertime_Time{field: "\"templates_template_weapons\".\"created_at\""},
}

// TemplatesTemplateWeaponRels is where relationship names are stored.
var TemplatesTemplateWeaponRels = struct {
	Template       string
	TemplateWeapon string
}{
	Template:       "Template",
	TemplateWeapon: "TemplateWeapon",
}

// templatesTemplateWeaponR is where relationships are stored.
type templatesTemplateWeaponR struct {
	Template       *Template       `boiler:"Template" boil:"Template" json:"Template" toml:"Template" yaml:"Template"`
	TemplateWeapon *TemplateWeapon `boiler:"TemplateWeapon" boil:"TemplateWeapon" json:"TemplateWeapon" toml:"TemplateWeapon" yaml:"TemplateWeapon"`
}

// NewStruct creates a new relationship struct
func (*templatesTemplateWeaponR) NewStruct() *templatesTemplateWeaponR {
	return &templatesTemplateWeaponR{}
}

// templatesTemplateWeaponL is where Load methods for each relationship are stored.
type templatesTemplateWeaponL struct{}

var (
	templatesTemplateWeaponAllColumns            = []string{"id", "template_weapon_id", "template_id", "deleted_at", "updated_at", "created_at"}
	templatesTemplateWeaponColumnsWithoutDefault = []string{"template_weapon_id", "template_id"}
	templatesTemplateWeaponColumnsWithDefault    = []string{"id", "deleted_at", "updated_at", "created_at"}
	templatesTemplateWeaponPrimaryKeyColumns     = []string{"id"}
	templatesTemplateWeaponGeneratedColumns      = []string{}
)

type (
	// TemplatesTemplateWeaponSlice is an alias for a slice of pointers to TemplatesTemplateWeapon.
	// This should almost always be used instead of []TemplatesTemplateWeapon.
	TemplatesTemplateWeaponSlice []*TemplatesTemplateWeapon
	// TemplatesTemplateWeaponHook is the signature for custom TemplatesTemplateWeapon hook methods
	TemplatesTemplateWeaponHook func(boil.Executor, *TemplatesTemplateWeapon) error

	templatesTemplateWeaponQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	templatesTemplateWeaponType                 = reflect.TypeOf(&TemplatesTemplateWeapon{})
	templatesTemplateWeaponMapping              = queries.MakeStructMapping(templatesTemplateWeaponType)
	templatesTemplateWeaponPrimaryKeyMapping, _ = queries.BindMapping(templatesTemplateWeaponType, templatesTemplateWeaponMapping, templatesTemplateWeaponPrimaryKeyColumns)
	templatesTemplateWeaponInsertCacheMut       sync.RWMutex
	templatesTemplateWeaponInsertCache          = make(map[string]insertCache)
	templatesTemplateWeaponUpdateCacheMut       sync.RWMutex
	templatesTemplateWeaponUpdateCache          = make(map[string]updateCache)
	templatesTemplateWeaponUpsertCacheMut       sync.RWMutex
	templatesTemplateWeaponUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var templatesTemplateWeaponAfterSelectHooks []TemplatesTemplateWeaponHook

var templatesTemplateWeaponBeforeInsertHooks []TemplatesTemplateWeaponHook
var templatesTemplateWeaponAfterInsertHooks []TemplatesTemplateWeaponHook

var templatesTemplateWeaponBeforeUpdateHooks []TemplatesTemplateWeaponHook
var templatesTemplateWeaponAfterUpdateHooks []TemplatesTemplateWeaponHook

var templatesTemplateWeaponBeforeDeleteHooks []TemplatesTemplateWeaponHook
var templatesTemplateWeaponAfterDeleteHooks []TemplatesTemplateWeaponHook

var templatesTemplateWeaponBeforeUpsertHooks []TemplatesTemplateWeaponHook
var templatesTemplateWeaponAfterUpsertHooks []TemplatesTemplateWeaponHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TemplatesTemplateWeapon) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesTemplateWeaponAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TemplatesTemplateWeapon) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesTemplateWeaponBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TemplatesTemplateWeapon) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesTemplateWeaponAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TemplatesTemplateWeapon) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesTemplateWeaponBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TemplatesTemplateWeapon) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesTemplateWeaponAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TemplatesTemplateWeapon) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesTemplateWeaponBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TemplatesTemplateWeapon) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesTemplateWeaponAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TemplatesTemplateWeapon) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesTemplateWeaponBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TemplatesTemplateWeapon) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesTemplateWeaponAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTemplatesTemplateWeaponHook registers your hook function for all future operations.
func AddTemplatesTemplateWeaponHook(hookPoint boil.HookPoint, templatesTemplateWeaponHook TemplatesTemplateWeaponHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		templatesTemplateWeaponAfterSelectHooks = append(templatesTemplateWeaponAfterSelectHooks, templatesTemplateWeaponHook)
	case boil.BeforeInsertHook:
		templatesTemplateWeaponBeforeInsertHooks = append(templatesTemplateWeaponBeforeInsertHooks, templatesTemplateWeaponHook)
	case boil.AfterInsertHook:
		templatesTemplateWeaponAfterInsertHooks = append(templatesTemplateWeaponAfterInsertHooks, templatesTemplateWeaponHook)
	case boil.BeforeUpdateHook:
		templatesTemplateWeaponBeforeUpdateHooks = append(templatesTemplateWeaponBeforeUpdateHooks, templatesTemplateWeaponHook)
	case boil.AfterUpdateHook:
		templatesTemplateWeaponAfterUpdateHooks = append(templatesTemplateWeaponAfterUpdateHooks, templatesTemplateWeaponHook)
	case boil.BeforeDeleteHook:
		templatesTemplateWeaponBeforeDeleteHooks = append(templatesTemplateWeaponBeforeDeleteHooks, templatesTemplateWeaponHook)
	case boil.AfterDeleteHook:
		templatesTemplateWeaponAfterDeleteHooks = append(templatesTemplateWeaponAfterDeleteHooks, templatesTemplateWeaponHook)
	case boil.BeforeUpsertHook:
		templatesTemplateWeaponBeforeUpsertHooks = append(templatesTemplateWeaponBeforeUpsertHooks, templatesTemplateWeaponHook)
	case boil.AfterUpsertHook:
		templatesTemplateWeaponAfterUpsertHooks = append(templatesTemplateWeaponAfterUpsertHooks, templatesTemplateWeaponHook)
	}
}

// One returns a single templatesTemplateWeapon record from the query.
func (q templatesTemplateWeaponQuery) One(exec boil.Executor) (*TemplatesTemplateWeapon, error) {
	o := &TemplatesTemplateWeapon{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: failed to execute a one query for templates_template_weapons")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TemplatesTemplateWeapon records from the query.
func (q templatesTemplateWeaponQuery) All(exec boil.Executor) (TemplatesTemplateWeaponSlice, error) {
	var o []*TemplatesTemplateWeapon

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "boiler: failed to assign all query results to TemplatesTemplateWeapon slice")
	}

	if len(templatesTemplateWeaponAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TemplatesTemplateWeapon records in the query.
func (q templatesTemplateWeaponQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to count templates_template_weapons rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q templatesTemplateWeaponQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "boiler: failed to check if templates_template_weapons exists")
	}

	return count > 0, nil
}

// Template pointed to by the foreign key.
func (o *TemplatesTemplateWeapon) Template(mods ...qm.QueryMod) templateQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.TemplateID),
		qmhelper.WhereIsNull("deleted_at"),
	}

	queryMods = append(queryMods, mods...)

	query := Templates(queryMods...)
	queries.SetFrom(query.Query, "\"templates\"")

	return query
}

// TemplateWeapon pointed to by the foreign key.
func (o *TemplatesTemplateWeapon) TemplateWeapon(mods ...qm.QueryMod) templateWeaponQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.TemplateWeaponID),
		qmhelper.WhereIsNull("deleted_at"),
	}

	queryMods = append(queryMods, mods...)

	query := TemplateWeapons(queryMods...)
	queries.SetFrom(query.Query, "\"template_weapons\"")

	return query
}

// LoadTemplate allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (templatesTemplateWeaponL) LoadTemplate(e boil.Executor, singular bool, maybeTemplatesTemplateWeapon interface{}, mods queries.Applicator) error {
	var slice []*TemplatesTemplateWeapon
	var object *TemplatesTemplateWeapon

	if singular {
		object = maybeTemplatesTemplateWeapon.(*TemplatesTemplateWeapon)
	} else {
		slice = *maybeTemplatesTemplateWeapon.(*[]*TemplatesTemplateWeapon)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &templatesTemplateWeaponR{}
		}
		args = append(args, object.TemplateID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &templatesTemplateWeaponR{}
			}

			for _, a := range args {
				if a == obj.TemplateID {
					continue Outer
				}
			}

			args = append(args, obj.TemplateID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`templates`),
		qm.WhereIn(`templates.id in ?`, args...),
		qmhelper.WhereIsNull(`templates.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Template")
	}

	var resultSlice []*Template
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Template")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for templates")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for templates")
	}

	if len(templatesTemplateWeaponAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Template = foreign
		if foreign.R == nil {
			foreign.R = &templateR{}
		}
		foreign.R.TemplatesTemplateWeapons = append(foreign.R.TemplatesTemplateWeapons, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TemplateID == foreign.ID {
				local.R.Template = foreign
				if foreign.R == nil {
					foreign.R = &templateR{}
				}
				foreign.R.TemplatesTemplateWeapons = append(foreign.R.TemplatesTemplateWeapons, local)
				break
			}
		}
	}

	return nil
}

// LoadTemplateWeapon allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (templatesTemplateWeaponL) LoadTemplateWeapon(e boil.Executor, singular bool, maybeTemplatesTemplateWeapon interface{}, mods queries.Applicator) error {
	var slice []*TemplatesTemplateWeapon
	var object *TemplatesTemplateWeapon

	if singular {
		object = maybeTemplatesTemplateWeapon.(*TemplatesTemplateWeapon)
	} else {
		slice = *maybeTemplatesTemplateWeapon.(*[]*TemplatesTemplateWeapon)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &templatesTemplateWeaponR{}
		}
		args = append(args, object.TemplateWeaponID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &templatesTemplateWeaponR{}
			}

			for _, a := range args {
				if a == obj.TemplateWeaponID {
					continue Outer
				}
			}

			args = append(args, obj.TemplateWeaponID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`template_weapons`),
		qm.WhereIn(`template_weapons.id in ?`, args...),
		qmhelper.WhereIsNull(`template_weapons.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load TemplateWeapon")
	}

	var resultSlice []*TemplateWeapon
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice TemplateWeapon")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for template_weapons")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for template_weapons")
	}

	if len(templatesTemplateWeaponAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.TemplateWeapon = foreign
		if foreign.R == nil {
			foreign.R = &templateWeaponR{}
		}
		foreign.R.TemplatesTemplateWeapons = append(foreign.R.TemplatesTemplateWeapons, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TemplateWeaponID == foreign.ID {
				local.R.TemplateWeapon = foreign
				if foreign.R == nil {
					foreign.R = &templateWeaponR{}
				}
				foreign.R.TemplatesTemplateWeapons = append(foreign.R.TemplatesTemplateWeapons, local)
				break
			}
		}
	}

	return nil
}

// SetTemplate of the templatesTemplateWeapon to the related item.
// Sets o.R.Template to related.
// Adds o to related.R.TemplatesTemplateWeapons.
func (o *TemplatesTemplateWeapon) SetTemplate(exec boil.Executor, insert bool, related *Template) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"templates_template_weapons\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"template_id"}),
		strmangle.WhereClause("\"", "\"", 2, templatesTemplateWeaponPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TemplateID = related.ID
	if o.R == nil {
		o.R = &templatesTemplateWeaponR{
			Template: related,
		}
	} else {
		o.R.Template = related
	}

	if related.R == nil {
		related.R = &templateR{
			TemplatesTemplateWeapons: TemplatesTemplateWeaponSlice{o},
		}
	} else {
		related.R.TemplatesTemplateWeapons = append(related.R.TemplatesTemplateWeapons, o)
	}

	return nil
}

// SetTemplateWeapon of the templatesTemplateWeapon to the related item.
// Sets o.R.TemplateWeapon to related.
// Adds o to related.R.TemplatesTemplateWeapons.
func (o *TemplatesTemplateWeapon) SetTemplateWeapon(exec boil.Executor, insert bool, related *TemplateWeapon) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"templates_template_weapons\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"template_weapon_id"}),
		strmangle.WhereClause("\"", "\"", 2, templatesTemplateWeaponPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TemplateWeaponID = related.ID
	if o.R == nil {
		o.R = &templatesTemplateWeaponR{
			TemplateWeapon: related,
		}
	} else {
		o.R.TemplateWeapon = related
	}

	if related.R == nil {
		related.R = &templateWeaponR{
			TemplatesTemplateWeapons: TemplatesTemplateWeaponSlice{o},
		}
	} else {
		related.R.TemplatesTemplateWeapons = append(related.R.TemplatesTemplateWeapons, o)
	}

	return nil
}

// TemplatesTemplateWeapons retrieves all the records using an executor.
func TemplatesTemplateWeapons(mods ...qm.QueryMod) templatesTemplateWeaponQuery {
	mods = append(mods, qm.From("\"templates_template_weapons\""), qmhelper.WhereIsNull("\"templates_template_weapons\".\"deleted_at\""))
	return templatesTemplateWeaponQuery{NewQuery(mods...)}
}

// FindTemplatesTemplateWeapon retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTemplatesTemplateWeapon(exec boil.Executor, iD string, selectCols ...string) (*TemplatesTemplateWeapon, error) {
	templatesTemplateWeaponObj := &TemplatesTemplateWeapon{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"templates_template_weapons\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, templatesTemplateWeaponObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: unable to select from templates_template_weapons")
	}

	if err = templatesTemplateWeaponObj.doAfterSelectHooks(exec); err != nil {
		return templatesTemplateWeaponObj, err
	}

	return templatesTemplateWeaponObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TemplatesTemplateWeapon) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no templates_template_weapons provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.UpdatedAt.IsZero() {
		o.UpdatedAt = currTime
	}
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(templatesTemplateWeaponColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	templatesTemplateWeaponInsertCacheMut.RLock()
	cache, cached := templatesTemplateWeaponInsertCache[key]
	templatesTemplateWeaponInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			templatesTemplateWeaponAllColumns,
			templatesTemplateWeaponColumnsWithDefault,
			templatesTemplateWeaponColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(templatesTemplateWeaponType, templatesTemplateWeaponMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(templatesTemplateWeaponType, templatesTemplateWeaponMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"templates_template_weapons\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"templates_template_weapons\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "boiler: unable to insert into templates_template_weapons")
	}

	if !cached {
		templatesTemplateWeaponInsertCacheMut.Lock()
		templatesTemplateWeaponInsertCache[key] = cache
		templatesTemplateWeaponInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// Update uses an executor to update the TemplatesTemplateWeapon.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TemplatesTemplateWeapon) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime

	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	templatesTemplateWeaponUpdateCacheMut.RLock()
	cache, cached := templatesTemplateWeaponUpdateCache[key]
	templatesTemplateWeaponUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			templatesTemplateWeaponAllColumns,
			templatesTemplateWeaponPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("boiler: unable to update templates_template_weapons, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"templates_template_weapons\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, templatesTemplateWeaponPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(templatesTemplateWeaponType, templatesTemplateWeaponMapping, append(wl, templatesTemplateWeaponPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update templates_template_weapons row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by update for templates_template_weapons")
	}

	if !cached {
		templatesTemplateWeaponUpdateCacheMut.Lock()
		templatesTemplateWeaponUpdateCache[key] = cache
		templatesTemplateWeaponUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q templatesTemplateWeaponQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all for templates_template_weapons")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected for templates_template_weapons")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TemplatesTemplateWeaponSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("boiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), templatesTemplateWeaponPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"templates_template_weapons\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, templatesTemplateWeaponPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all in templatesTemplateWeapon slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected all in update all templatesTemplateWeapon")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TemplatesTemplateWeapon) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no templates_template_weapons provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(templatesTemplateWeaponColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	templatesTemplateWeaponUpsertCacheMut.RLock()
	cache, cached := templatesTemplateWeaponUpsertCache[key]
	templatesTemplateWeaponUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			templatesTemplateWeaponAllColumns,
			templatesTemplateWeaponColumnsWithDefault,
			templatesTemplateWeaponColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			templatesTemplateWeaponAllColumns,
			templatesTemplateWeaponPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("boiler: unable to upsert templates_template_weapons, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(templatesTemplateWeaponPrimaryKeyColumns))
			copy(conflict, templatesTemplateWeaponPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"templates_template_weapons\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(templatesTemplateWeaponType, templatesTemplateWeaponMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(templatesTemplateWeaponType, templatesTemplateWeaponMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "boiler: unable to upsert templates_template_weapons")
	}

	if !cached {
		templatesTemplateWeaponUpsertCacheMut.Lock()
		templatesTemplateWeaponUpsertCache[key] = cache
		templatesTemplateWeaponUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// Delete deletes a single TemplatesTemplateWeapon record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TemplatesTemplateWeapon) Delete(exec boil.Executor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("boiler: no TemplatesTemplateWeapon provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), templatesTemplateWeaponPrimaryKeyMapping)
		sql = "DELETE FROM \"templates_template_weapons\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"templates_template_weapons\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(templatesTemplateWeaponType, templatesTemplateWeaponMapping, append(wl, templatesTemplateWeaponPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete from templates_template_weapons")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by delete for templates_template_weapons")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q templatesTemplateWeaponQuery) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("boiler: no templatesTemplateWeaponQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from templates_template_weapons")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for templates_template_weapons")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TemplatesTemplateWeaponSlice) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(templatesTemplateWeaponBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), templatesTemplateWeaponPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"templates_template_weapons\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, templatesTemplateWeaponPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), templatesTemplateWeaponPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"templates_template_weapons\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, templatesTemplateWeaponPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from templatesTemplateWeapon slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for templates_template_weapons")
	}

	if len(templatesTemplateWeaponAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TemplatesTemplateWeapon) Reload(exec boil.Executor) error {
	ret, err := FindTemplatesTemplateWeapon(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TemplatesTemplateWeaponSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TemplatesTemplateWeaponSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), templatesTemplateWeaponPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"templates_template_weapons\".* FROM \"templates_template_weapons\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, templatesTemplateWeaponPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to reload all in TemplatesTemplateWeaponSlice")
	}

	*o = slice

	return nil
}

// TemplatesTemplateWeaponExists checks if the TemplatesTemplateWeapon row exists.
func TemplatesTemplateWeaponExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"templates_template_weapons\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "boiler: unable to check if templates_template_weapons exists")
	}

	return exists, nil
}
