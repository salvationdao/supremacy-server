// Code generated by SQLBoiler 4.8.6 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package boiler

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// BannedPlayer is an object representing the database table.
type BannedPlayer struct {
	ID               string      `boiler:"id" boil:"id" json:"id" toml:"id" yaml:"id"`
	PlayerID         string      `boiler:"player_id" boil:"player_id" json:"player_id" toml:"player_id" yaml:"player_id"`
	BanTypeID        string      `boiler:"ban_type_id" boil:"ban_type_id" json:"ban_type_id" toml:"ban_type_id" yaml:"ban_type_id"`
	BanUntil         time.Time   `boiler:"ban_until" boil:"ban_until" json:"ban_until" toml:"ban_until" yaml:"ban_until"`
	RelatedBanVoteID null.String `boiler:"related_ban_vote_id" boil:"related_ban_vote_id" json:"related_ban_vote_id,omitempty" toml:"related_ban_vote_id" yaml:"related_ban_vote_id,omitempty"`
	CreatedAt        time.Time   `boiler:"created_at" boil:"created_at" json:"created_at" toml:"created_at" yaml:"created_at"`
	UpdatedAt        time.Time   `boiler:"updated_at" boil:"updated_at" json:"updated_at" toml:"updated_at" yaml:"updated_at"`
	DeletedAt        null.Time   `boiler:"deleted_at" boil:"deleted_at" json:"deleted_at,omitempty" toml:"deleted_at" yaml:"deleted_at,omitempty"`

	R *bannedPlayerR `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
	L bannedPlayerL  `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
}

var BannedPlayerColumns = struct {
	ID               string
	PlayerID         string
	BanTypeID        string
	BanUntil         string
	RelatedBanVoteID string
	CreatedAt        string
	UpdatedAt        string
	DeletedAt        string
}{
	ID:               "id",
	PlayerID:         "player_id",
	BanTypeID:        "ban_type_id",
	BanUntil:         "ban_until",
	RelatedBanVoteID: "related_ban_vote_id",
	CreatedAt:        "created_at",
	UpdatedAt:        "updated_at",
	DeletedAt:        "deleted_at",
}

var BannedPlayerTableColumns = struct {
	ID               string
	PlayerID         string
	BanTypeID        string
	BanUntil         string
	RelatedBanVoteID string
	CreatedAt        string
	UpdatedAt        string
	DeletedAt        string
}{
	ID:               "banned_players.id",
	PlayerID:         "banned_players.player_id",
	BanTypeID:        "banned_players.ban_type_id",
	BanUntil:         "banned_players.ban_until",
	RelatedBanVoteID: "banned_players.related_ban_vote_id",
	CreatedAt:        "banned_players.created_at",
	UpdatedAt:        "banned_players.updated_at",
	DeletedAt:        "banned_players.deleted_at",
}

// Generated where

type whereHelpernull_String struct{ field string }

func (w whereHelpernull_String) EQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, false, x)
}
func (w whereHelpernull_String) NEQ(x null.String) qm.QueryMod {
	return qmhelper.WhereNullEQ(w.field, true, x)
}
func (w whereHelpernull_String) LT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LT, x)
}
func (w whereHelpernull_String) LTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.LTE, x)
}
func (w whereHelpernull_String) GT(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GT, x)
}
func (w whereHelpernull_String) GTE(x null.String) qm.QueryMod {
	return qmhelper.Where(w.field, qmhelper.GTE, x)
}

func (w whereHelpernull_String) IsNull() qm.QueryMod    { return qmhelper.WhereIsNull(w.field) }
func (w whereHelpernull_String) IsNotNull() qm.QueryMod { return qmhelper.WhereIsNotNull(w.field) }

var BannedPlayerWhere = struct {
	ID               whereHelperstring
	PlayerID         whereHelperstring
	BanTypeID        whereHelperstring
	BanUntil         whereHelpertime_Time
	RelatedBanVoteID whereHelpernull_String
	CreatedAt        whereHelpertime_Time
	UpdatedAt        whereHelpertime_Time
	DeletedAt        whereHelpernull_Time
}{
	ID:               whereHelperstring{field: "\"banned_players\".\"id\""},
	PlayerID:         whereHelperstring{field: "\"banned_players\".\"player_id\""},
	BanTypeID:        whereHelperstring{field: "\"banned_players\".\"ban_type_id\""},
	BanUntil:         whereHelpertime_Time{field: "\"banned_players\".\"ban_until\""},
	RelatedBanVoteID: whereHelpernull_String{field: "\"banned_players\".\"related_ban_vote_id\""},
	CreatedAt:        whereHelpertime_Time{field: "\"banned_players\".\"created_at\""},
	UpdatedAt:        whereHelpertime_Time{field: "\"banned_players\".\"updated_at\""},
	DeletedAt:        whereHelpernull_Time{field: "\"banned_players\".\"deleted_at\""},
}

// BannedPlayerRels is where relationship names are stored.
var BannedPlayerRels = struct {
	BanType        string
	Player         string
	RelatedBanVote string
}{
	BanType:        "BanType",
	Player:         "Player",
	RelatedBanVote: "RelatedBanVote",
}

// bannedPlayerR is where relationships are stored.
type bannedPlayerR struct {
	BanType        *BanType `boiler:"BanType" boil:"BanType" json:"BanType" toml:"BanType" yaml:"BanType"`
	Player         *Player  `boiler:"Player" boil:"Player" json:"Player" toml:"Player" yaml:"Player"`
	RelatedBanVote *BanVote `boiler:"RelatedBanVote" boil:"RelatedBanVote" json:"RelatedBanVote" toml:"RelatedBanVote" yaml:"RelatedBanVote"`
}

// NewStruct creates a new relationship struct
func (*bannedPlayerR) NewStruct() *bannedPlayerR {
	return &bannedPlayerR{}
}

// bannedPlayerL is where Load methods for each relationship are stored.
type bannedPlayerL struct{}

var (
	bannedPlayerAllColumns            = []string{"id", "player_id", "ban_type_id", "ban_until", "related_ban_vote_id", "created_at", "updated_at", "deleted_at"}
	bannedPlayerColumnsWithoutDefault = []string{"player_id", "ban_type_id", "ban_until"}
	bannedPlayerColumnsWithDefault    = []string{"id", "related_ban_vote_id", "created_at", "updated_at", "deleted_at"}
	bannedPlayerPrimaryKeyColumns     = []string{"id"}
	bannedPlayerGeneratedColumns      = []string{}
)

type (
	// BannedPlayerSlice is an alias for a slice of pointers to BannedPlayer.
	// This should almost always be used instead of []BannedPlayer.
	BannedPlayerSlice []*BannedPlayer
	// BannedPlayerHook is the signature for custom BannedPlayer hook methods
	BannedPlayerHook func(boil.Executor, *BannedPlayer) error

	bannedPlayerQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	bannedPlayerType                 = reflect.TypeOf(&BannedPlayer{})
	bannedPlayerMapping              = queries.MakeStructMapping(bannedPlayerType)
	bannedPlayerPrimaryKeyMapping, _ = queries.BindMapping(bannedPlayerType, bannedPlayerMapping, bannedPlayerPrimaryKeyColumns)
	bannedPlayerInsertCacheMut       sync.RWMutex
	bannedPlayerInsertCache          = make(map[string]insertCache)
	bannedPlayerUpdateCacheMut       sync.RWMutex
	bannedPlayerUpdateCache          = make(map[string]updateCache)
	bannedPlayerUpsertCacheMut       sync.RWMutex
	bannedPlayerUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var bannedPlayerAfterSelectHooks []BannedPlayerHook

var bannedPlayerBeforeInsertHooks []BannedPlayerHook
var bannedPlayerAfterInsertHooks []BannedPlayerHook

var bannedPlayerBeforeUpdateHooks []BannedPlayerHook
var bannedPlayerAfterUpdateHooks []BannedPlayerHook

var bannedPlayerBeforeDeleteHooks []BannedPlayerHook
var bannedPlayerAfterDeleteHooks []BannedPlayerHook

var bannedPlayerBeforeUpsertHooks []BannedPlayerHook
var bannedPlayerAfterUpsertHooks []BannedPlayerHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *BannedPlayer) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range bannedPlayerAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *BannedPlayer) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range bannedPlayerBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *BannedPlayer) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range bannedPlayerAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *BannedPlayer) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range bannedPlayerBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *BannedPlayer) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range bannedPlayerAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *BannedPlayer) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range bannedPlayerBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *BannedPlayer) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range bannedPlayerAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *BannedPlayer) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range bannedPlayerBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *BannedPlayer) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range bannedPlayerAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddBannedPlayerHook registers your hook function for all future operations.
func AddBannedPlayerHook(hookPoint boil.HookPoint, bannedPlayerHook BannedPlayerHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		bannedPlayerAfterSelectHooks = append(bannedPlayerAfterSelectHooks, bannedPlayerHook)
	case boil.BeforeInsertHook:
		bannedPlayerBeforeInsertHooks = append(bannedPlayerBeforeInsertHooks, bannedPlayerHook)
	case boil.AfterInsertHook:
		bannedPlayerAfterInsertHooks = append(bannedPlayerAfterInsertHooks, bannedPlayerHook)
	case boil.BeforeUpdateHook:
		bannedPlayerBeforeUpdateHooks = append(bannedPlayerBeforeUpdateHooks, bannedPlayerHook)
	case boil.AfterUpdateHook:
		bannedPlayerAfterUpdateHooks = append(bannedPlayerAfterUpdateHooks, bannedPlayerHook)
	case boil.BeforeDeleteHook:
		bannedPlayerBeforeDeleteHooks = append(bannedPlayerBeforeDeleteHooks, bannedPlayerHook)
	case boil.AfterDeleteHook:
		bannedPlayerAfterDeleteHooks = append(bannedPlayerAfterDeleteHooks, bannedPlayerHook)
	case boil.BeforeUpsertHook:
		bannedPlayerBeforeUpsertHooks = append(bannedPlayerBeforeUpsertHooks, bannedPlayerHook)
	case boil.AfterUpsertHook:
		bannedPlayerAfterUpsertHooks = append(bannedPlayerAfterUpsertHooks, bannedPlayerHook)
	}
}

// One returns a single bannedPlayer record from the query.
func (q bannedPlayerQuery) One(exec boil.Executor) (*BannedPlayer, error) {
	o := &BannedPlayer{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: failed to execute a one query for banned_players")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all BannedPlayer records from the query.
func (q bannedPlayerQuery) All(exec boil.Executor) (BannedPlayerSlice, error) {
	var o []*BannedPlayer

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "boiler: failed to assign all query results to BannedPlayer slice")
	}

	if len(bannedPlayerAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all BannedPlayer records in the query.
func (q bannedPlayerQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to count banned_players rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q bannedPlayerQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "boiler: failed to check if banned_players exists")
	}

	return count > 0, nil
}

// BanType pointed to by the foreign key.
func (o *BannedPlayer) BanType(mods ...qm.QueryMod) banTypeQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.BanTypeID),
		qmhelper.WhereIsNull("deleted_at"),
	}

	queryMods = append(queryMods, mods...)

	query := BanTypes(queryMods...)
	queries.SetFrom(query.Query, "\"ban_types\"")

	return query
}

// Player pointed to by the foreign key.
func (o *BannedPlayer) Player(mods ...qm.QueryMod) playerQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.PlayerID),
		qmhelper.WhereIsNull("deleted_at"),
	}

	queryMods = append(queryMods, mods...)

	query := Players(queryMods...)
	queries.SetFrom(query.Query, "\"players\"")

	return query
}

// RelatedBanVote pointed to by the foreign key.
func (o *BannedPlayer) RelatedBanVote(mods ...qm.QueryMod) banVoteQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.RelatedBanVoteID),
		qmhelper.WhereIsNull("deleted_at"),
	}

	queryMods = append(queryMods, mods...)

	query := BanVotes(queryMods...)
	queries.SetFrom(query.Query, "\"ban_votes\"")

	return query
}

// LoadBanType allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (bannedPlayerL) LoadBanType(e boil.Executor, singular bool, maybeBannedPlayer interface{}, mods queries.Applicator) error {
	var slice []*BannedPlayer
	var object *BannedPlayer

	if singular {
		object = maybeBannedPlayer.(*BannedPlayer)
	} else {
		slice = *maybeBannedPlayer.(*[]*BannedPlayer)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &bannedPlayerR{}
		}
		args = append(args, object.BanTypeID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &bannedPlayerR{}
			}

			for _, a := range args {
				if a == obj.BanTypeID {
					continue Outer
				}
			}

			args = append(args, obj.BanTypeID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`ban_types`),
		qm.WhereIn(`ban_types.id in ?`, args...),
		qmhelper.WhereIsNull(`ban_types.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load BanType")
	}

	var resultSlice []*BanType
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice BanType")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for ban_types")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ban_types")
	}

	if len(bannedPlayerAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.BanType = foreign
		if foreign.R == nil {
			foreign.R = &banTypeR{}
		}
		foreign.R.BannedPlayers = append(foreign.R.BannedPlayers, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BanTypeID == foreign.ID {
				local.R.BanType = foreign
				if foreign.R == nil {
					foreign.R = &banTypeR{}
				}
				foreign.R.BannedPlayers = append(foreign.R.BannedPlayers, local)
				break
			}
		}
	}

	return nil
}

// LoadPlayer allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (bannedPlayerL) LoadPlayer(e boil.Executor, singular bool, maybeBannedPlayer interface{}, mods queries.Applicator) error {
	var slice []*BannedPlayer
	var object *BannedPlayer

	if singular {
		object = maybeBannedPlayer.(*BannedPlayer)
	} else {
		slice = *maybeBannedPlayer.(*[]*BannedPlayer)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &bannedPlayerR{}
		}
		args = append(args, object.PlayerID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &bannedPlayerR{}
			}

			for _, a := range args {
				if a == obj.PlayerID {
					continue Outer
				}
			}

			args = append(args, obj.PlayerID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`players`),
		qm.WhereIn(`players.id in ?`, args...),
		qmhelper.WhereIsNull(`players.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Player")
	}

	var resultSlice []*Player
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Player")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for players")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for players")
	}

	if len(bannedPlayerAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Player = foreign
		if foreign.R == nil {
			foreign.R = &playerR{}
		}
		foreign.R.BannedPlayers = append(foreign.R.BannedPlayers, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.PlayerID == foreign.ID {
				local.R.Player = foreign
				if foreign.R == nil {
					foreign.R = &playerR{}
				}
				foreign.R.BannedPlayers = append(foreign.R.BannedPlayers, local)
				break
			}
		}
	}

	return nil
}

// LoadRelatedBanVote allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (bannedPlayerL) LoadRelatedBanVote(e boil.Executor, singular bool, maybeBannedPlayer interface{}, mods queries.Applicator) error {
	var slice []*BannedPlayer
	var object *BannedPlayer

	if singular {
		object = maybeBannedPlayer.(*BannedPlayer)
	} else {
		slice = *maybeBannedPlayer.(*[]*BannedPlayer)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &bannedPlayerR{}
		}
		if !queries.IsNil(object.RelatedBanVoteID) {
			args = append(args, object.RelatedBanVoteID)
		}

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &bannedPlayerR{}
			}

			for _, a := range args {
				if queries.Equal(a, obj.RelatedBanVoteID) {
					continue Outer
				}
			}

			if !queries.IsNil(obj.RelatedBanVoteID) {
				args = append(args, obj.RelatedBanVoteID)
			}

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`ban_votes`),
		qm.WhereIn(`ban_votes.id in ?`, args...),
		qmhelper.WhereIsNull(`ban_votes.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load BanVote")
	}

	var resultSlice []*BanVote
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice BanVote")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for ban_votes")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for ban_votes")
	}

	if len(bannedPlayerAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.RelatedBanVote = foreign
		if foreign.R == nil {
			foreign.R = &banVoteR{}
		}
		foreign.R.RelatedBanVoteBannedPlayers = append(foreign.R.RelatedBanVoteBannedPlayers, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if queries.Equal(local.RelatedBanVoteID, foreign.ID) {
				local.R.RelatedBanVote = foreign
				if foreign.R == nil {
					foreign.R = &banVoteR{}
				}
				foreign.R.RelatedBanVoteBannedPlayers = append(foreign.R.RelatedBanVoteBannedPlayers, local)
				break
			}
		}
	}

	return nil
}

// SetBanType of the bannedPlayer to the related item.
// Sets o.R.BanType to related.
// Adds o to related.R.BannedPlayers.
func (o *BannedPlayer) SetBanType(exec boil.Executor, insert bool, related *BanType) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"banned_players\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"ban_type_id"}),
		strmangle.WhereClause("\"", "\"", 2, bannedPlayerPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BanTypeID = related.ID
	if o.R == nil {
		o.R = &bannedPlayerR{
			BanType: related,
		}
	} else {
		o.R.BanType = related
	}

	if related.R == nil {
		related.R = &banTypeR{
			BannedPlayers: BannedPlayerSlice{o},
		}
	} else {
		related.R.BannedPlayers = append(related.R.BannedPlayers, o)
	}

	return nil
}

// SetPlayer of the bannedPlayer to the related item.
// Sets o.R.Player to related.
// Adds o to related.R.BannedPlayers.
func (o *BannedPlayer) SetPlayer(exec boil.Executor, insert bool, related *Player) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"banned_players\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"player_id"}),
		strmangle.WhereClause("\"", "\"", 2, bannedPlayerPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.PlayerID = related.ID
	if o.R == nil {
		o.R = &bannedPlayerR{
			Player: related,
		}
	} else {
		o.R.Player = related
	}

	if related.R == nil {
		related.R = &playerR{
			BannedPlayers: BannedPlayerSlice{o},
		}
	} else {
		related.R.BannedPlayers = append(related.R.BannedPlayers, o)
	}

	return nil
}

// SetRelatedBanVote of the bannedPlayer to the related item.
// Sets o.R.RelatedBanVote to related.
// Adds o to related.R.RelatedBanVoteBannedPlayers.
func (o *BannedPlayer) SetRelatedBanVote(exec boil.Executor, insert bool, related *BanVote) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"banned_players\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"related_ban_vote_id"}),
		strmangle.WhereClause("\"", "\"", 2, bannedPlayerPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	queries.Assign(&o.RelatedBanVoteID, related.ID)
	if o.R == nil {
		o.R = &bannedPlayerR{
			RelatedBanVote: related,
		}
	} else {
		o.R.RelatedBanVote = related
	}

	if related.R == nil {
		related.R = &banVoteR{
			RelatedBanVoteBannedPlayers: BannedPlayerSlice{o},
		}
	} else {
		related.R.RelatedBanVoteBannedPlayers = append(related.R.RelatedBanVoteBannedPlayers, o)
	}

	return nil
}

// RemoveRelatedBanVote relationship.
// Sets o.R.RelatedBanVote to nil.
// Removes o from all passed in related items' relationships struct (Optional).
func (o *BannedPlayer) RemoveRelatedBanVote(exec boil.Executor, related *BanVote) error {
	var err error

	queries.SetScanner(&o.RelatedBanVoteID, nil)
	if _, err = o.Update(exec, boil.Whitelist("related_ban_vote_id")); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	if o.R != nil {
		o.R.RelatedBanVote = nil
	}
	if related == nil || related.R == nil {
		return nil
	}

	for i, ri := range related.R.RelatedBanVoteBannedPlayers {
		if queries.Equal(o.RelatedBanVoteID, ri.RelatedBanVoteID) {
			continue
		}

		ln := len(related.R.RelatedBanVoteBannedPlayers)
		if ln > 1 && i < ln-1 {
			related.R.RelatedBanVoteBannedPlayers[i] = related.R.RelatedBanVoteBannedPlayers[ln-1]
		}
		related.R.RelatedBanVoteBannedPlayers = related.R.RelatedBanVoteBannedPlayers[:ln-1]
		break
	}
	return nil
}

// BannedPlayers retrieves all the records using an executor.
func BannedPlayers(mods ...qm.QueryMod) bannedPlayerQuery {
	mods = append(mods, qm.From("\"banned_players\""), qmhelper.WhereIsNull("\"banned_players\".\"deleted_at\""))
	return bannedPlayerQuery{NewQuery(mods...)}
}

// FindBannedPlayer retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindBannedPlayer(exec boil.Executor, iD string, selectCols ...string) (*BannedPlayer, error) {
	bannedPlayerObj := &BannedPlayer{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"banned_players\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, bannedPlayerObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: unable to select from banned_players")
	}

	if err = bannedPlayerObj.doAfterSelectHooks(exec); err != nil {
		return bannedPlayerObj, err
	}

	return bannedPlayerObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *BannedPlayer) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no banned_players provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}
	if o.UpdatedAt.IsZero() {
		o.UpdatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(bannedPlayerColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	bannedPlayerInsertCacheMut.RLock()
	cache, cached := bannedPlayerInsertCache[key]
	bannedPlayerInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			bannedPlayerAllColumns,
			bannedPlayerColumnsWithDefault,
			bannedPlayerColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(bannedPlayerType, bannedPlayerMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(bannedPlayerType, bannedPlayerMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"banned_players\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"banned_players\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "boiler: unable to insert into banned_players")
	}

	if !cached {
		bannedPlayerInsertCacheMut.Lock()
		bannedPlayerInsertCache[key] = cache
		bannedPlayerInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// Update uses an executor to update the BannedPlayer.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *BannedPlayer) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime

	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	bannedPlayerUpdateCacheMut.RLock()
	cache, cached := bannedPlayerUpdateCache[key]
	bannedPlayerUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			bannedPlayerAllColumns,
			bannedPlayerPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("boiler: unable to update banned_players, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"banned_players\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, bannedPlayerPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(bannedPlayerType, bannedPlayerMapping, append(wl, bannedPlayerPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update banned_players row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by update for banned_players")
	}

	if !cached {
		bannedPlayerUpdateCacheMut.Lock()
		bannedPlayerUpdateCache[key] = cache
		bannedPlayerUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q bannedPlayerQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all for banned_players")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected for banned_players")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o BannedPlayerSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("boiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), bannedPlayerPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"banned_players\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, bannedPlayerPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all in bannedPlayer slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected all in update all bannedPlayer")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *BannedPlayer) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no banned_players provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}
	o.UpdatedAt = currTime

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(bannedPlayerColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	bannedPlayerUpsertCacheMut.RLock()
	cache, cached := bannedPlayerUpsertCache[key]
	bannedPlayerUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			bannedPlayerAllColumns,
			bannedPlayerColumnsWithDefault,
			bannedPlayerColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			bannedPlayerAllColumns,
			bannedPlayerPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("boiler: unable to upsert banned_players, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(bannedPlayerPrimaryKeyColumns))
			copy(conflict, bannedPlayerPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"banned_players\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(bannedPlayerType, bannedPlayerMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(bannedPlayerType, bannedPlayerMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "boiler: unable to upsert banned_players")
	}

	if !cached {
		bannedPlayerUpsertCacheMut.Lock()
		bannedPlayerUpsertCache[key] = cache
		bannedPlayerUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// Delete deletes a single BannedPlayer record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *BannedPlayer) Delete(exec boil.Executor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("boiler: no BannedPlayer provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), bannedPlayerPrimaryKeyMapping)
		sql = "DELETE FROM \"banned_players\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"banned_players\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(bannedPlayerType, bannedPlayerMapping, append(wl, bannedPlayerPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete from banned_players")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by delete for banned_players")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q bannedPlayerQuery) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("boiler: no bannedPlayerQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from banned_players")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for banned_players")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o BannedPlayerSlice) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(bannedPlayerBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), bannedPlayerPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"banned_players\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, bannedPlayerPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), bannedPlayerPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"banned_players\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, bannedPlayerPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from bannedPlayer slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for banned_players")
	}

	if len(bannedPlayerAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *BannedPlayer) Reload(exec boil.Executor) error {
	ret, err := FindBannedPlayer(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *BannedPlayerSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := BannedPlayerSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), bannedPlayerPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"banned_players\".* FROM \"banned_players\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, bannedPlayerPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to reload all in BannedPlayerSlice")
	}

	*o = slice

	return nil
}

// BannedPlayerExists checks if the BannedPlayer row exists.
func BannedPlayerExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"banned_players\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "boiler: unable to check if banned_players exists")
	}

	return exists, nil
}
