// Code generated by SQLBoiler 4.8.6 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package boiler

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TemplatesBlueprintWeapon is an object representing the database table.
type TemplatesBlueprintWeapon struct {
	ID                string    `boiler:"id" boil:"id" json:"id" toml:"id" yaml:"id"`
	BlueprintWeaponID string    `boiler:"blueprint_weapon_id" boil:"blueprint_weapon_id" json:"blueprintWeaponID" toml:"blueprintWeaponID" yaml:"blueprintWeaponID"`
	TemplateID        string    `boiler:"template_id" boil:"template_id" json:"templateID" toml:"templateID" yaml:"templateID"`
	DeletedAt         null.Time `boiler:"deleted_at" boil:"deleted_at" json:"deletedAt,omitempty" toml:"deletedAt" yaml:"deletedAt,omitempty"`
	UpdatedAt         time.Time `boiler:"updated_at" boil:"updated_at" json:"updatedAt" toml:"updatedAt" yaml:"updatedAt"`
	CreatedAt         time.Time `boiler:"created_at" boil:"created_at" json:"createdAt" toml:"createdAt" yaml:"createdAt"`

	R *templatesBlueprintWeaponR `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
	L templatesBlueprintWeaponL  `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TemplatesBlueprintWeaponColumns = struct {
	ID                string
	BlueprintWeaponID string
	TemplateID        string
	DeletedAt         string
	UpdatedAt         string
	CreatedAt         string
}{
	ID:                "id",
	BlueprintWeaponID: "blueprint_weapon_id",
	TemplateID:        "template_id",
	DeletedAt:         "deleted_at",
	UpdatedAt:         "updated_at",
	CreatedAt:         "created_at",
}

var TemplatesBlueprintWeaponTableColumns = struct {
	ID                string
	BlueprintWeaponID string
	TemplateID        string
	DeletedAt         string
	UpdatedAt         string
	CreatedAt         string
}{
	ID:                "templates_blueprint_weapons.id",
	BlueprintWeaponID: "templates_blueprint_weapons.blueprint_weapon_id",
	TemplateID:        "templates_blueprint_weapons.template_id",
	DeletedAt:         "templates_blueprint_weapons.deleted_at",
	UpdatedAt:         "templates_blueprint_weapons.updated_at",
	CreatedAt:         "templates_blueprint_weapons.created_at",
}

// Generated where

var TemplatesBlueprintWeaponWhere = struct {
	ID                whereHelperstring
	BlueprintWeaponID whereHelperstring
	TemplateID        whereHelperstring
	DeletedAt         whereHelpernull_Time
	UpdatedAt         whereHelpertime_Time
	CreatedAt         whereHelpertime_Time
}{
	ID:                whereHelperstring{field: "\"templates_blueprint_weapons\".\"id\""},
	BlueprintWeaponID: whereHelperstring{field: "\"templates_blueprint_weapons\".\"blueprint_weapon_id\""},
	TemplateID:        whereHelperstring{field: "\"templates_blueprint_weapons\".\"template_id\""},
	DeletedAt:         whereHelpernull_Time{field: "\"templates_blueprint_weapons\".\"deleted_at\""},
	UpdatedAt:         whereHelpertime_Time{field: "\"templates_blueprint_weapons\".\"updated_at\""},
	CreatedAt:         whereHelpertime_Time{field: "\"templates_blueprint_weapons\".\"created_at\""},
}

// TemplatesBlueprintWeaponRels is where relationship names are stored.
var TemplatesBlueprintWeaponRels = struct {
	BlueprintWeapon string
	Template        string
}{
	BlueprintWeapon: "BlueprintWeapon",
	Template:        "Template",
}

// templatesBlueprintWeaponR is where relationships are stored.
type templatesBlueprintWeaponR struct {
	BlueprintWeapon *BlueprintWeapon `boiler:"BlueprintWeapon" boil:"BlueprintWeapon" json:"BlueprintWeapon" toml:"BlueprintWeapon" yaml:"BlueprintWeapon"`
	Template        *Template        `boiler:"Template" boil:"Template" json:"Template" toml:"Template" yaml:"Template"`
}

// NewStruct creates a new relationship struct
func (*templatesBlueprintWeaponR) NewStruct() *templatesBlueprintWeaponR {
	return &templatesBlueprintWeaponR{}
}

// templatesBlueprintWeaponL is where Load methods for each relationship are stored.
type templatesBlueprintWeaponL struct{}

var (
	templatesBlueprintWeaponAllColumns            = []string{"id", "blueprint_weapon_id", "template_id", "deleted_at", "updated_at", "created_at"}
	templatesBlueprintWeaponColumnsWithoutDefault = []string{"blueprint_weapon_id", "template_id"}
	templatesBlueprintWeaponColumnsWithDefault    = []string{"id", "deleted_at", "updated_at", "created_at"}
	templatesBlueprintWeaponPrimaryKeyColumns     = []string{"id"}
	templatesBlueprintWeaponGeneratedColumns      = []string{}
)

type (
	// TemplatesBlueprintWeaponSlice is an alias for a slice of pointers to TemplatesBlueprintWeapon.
	// This should almost always be used instead of []TemplatesBlueprintWeapon.
	TemplatesBlueprintWeaponSlice []*TemplatesBlueprintWeapon
	// TemplatesBlueprintWeaponHook is the signature for custom TemplatesBlueprintWeapon hook methods
	TemplatesBlueprintWeaponHook func(boil.Executor, *TemplatesBlueprintWeapon) error

	templatesBlueprintWeaponQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	templatesBlueprintWeaponType                 = reflect.TypeOf(&TemplatesBlueprintWeapon{})
	templatesBlueprintWeaponMapping              = queries.MakeStructMapping(templatesBlueprintWeaponType)
	templatesBlueprintWeaponPrimaryKeyMapping, _ = queries.BindMapping(templatesBlueprintWeaponType, templatesBlueprintWeaponMapping, templatesBlueprintWeaponPrimaryKeyColumns)
	templatesBlueprintWeaponInsertCacheMut       sync.RWMutex
	templatesBlueprintWeaponInsertCache          = make(map[string]insertCache)
	templatesBlueprintWeaponUpdateCacheMut       sync.RWMutex
	templatesBlueprintWeaponUpdateCache          = make(map[string]updateCache)
	templatesBlueprintWeaponUpsertCacheMut       sync.RWMutex
	templatesBlueprintWeaponUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var templatesBlueprintWeaponAfterSelectHooks []TemplatesBlueprintWeaponHook

var templatesBlueprintWeaponBeforeInsertHooks []TemplatesBlueprintWeaponHook
var templatesBlueprintWeaponAfterInsertHooks []TemplatesBlueprintWeaponHook

var templatesBlueprintWeaponBeforeUpdateHooks []TemplatesBlueprintWeaponHook
var templatesBlueprintWeaponAfterUpdateHooks []TemplatesBlueprintWeaponHook

var templatesBlueprintWeaponBeforeDeleteHooks []TemplatesBlueprintWeaponHook
var templatesBlueprintWeaponAfterDeleteHooks []TemplatesBlueprintWeaponHook

var templatesBlueprintWeaponBeforeUpsertHooks []TemplatesBlueprintWeaponHook
var templatesBlueprintWeaponAfterUpsertHooks []TemplatesBlueprintWeaponHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TemplatesBlueprintWeapon) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintWeaponAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TemplatesBlueprintWeapon) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintWeaponBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TemplatesBlueprintWeapon) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintWeaponAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TemplatesBlueprintWeapon) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintWeaponBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TemplatesBlueprintWeapon) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintWeaponAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TemplatesBlueprintWeapon) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintWeaponBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TemplatesBlueprintWeapon) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintWeaponAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TemplatesBlueprintWeapon) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintWeaponBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TemplatesBlueprintWeapon) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintWeaponAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTemplatesBlueprintWeaponHook registers your hook function for all future operations.
func AddTemplatesBlueprintWeaponHook(hookPoint boil.HookPoint, templatesBlueprintWeaponHook TemplatesBlueprintWeaponHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		templatesBlueprintWeaponAfterSelectHooks = append(templatesBlueprintWeaponAfterSelectHooks, templatesBlueprintWeaponHook)
	case boil.BeforeInsertHook:
		templatesBlueprintWeaponBeforeInsertHooks = append(templatesBlueprintWeaponBeforeInsertHooks, templatesBlueprintWeaponHook)
	case boil.AfterInsertHook:
		templatesBlueprintWeaponAfterInsertHooks = append(templatesBlueprintWeaponAfterInsertHooks, templatesBlueprintWeaponHook)
	case boil.BeforeUpdateHook:
		templatesBlueprintWeaponBeforeUpdateHooks = append(templatesBlueprintWeaponBeforeUpdateHooks, templatesBlueprintWeaponHook)
	case boil.AfterUpdateHook:
		templatesBlueprintWeaponAfterUpdateHooks = append(templatesBlueprintWeaponAfterUpdateHooks, templatesBlueprintWeaponHook)
	case boil.BeforeDeleteHook:
		templatesBlueprintWeaponBeforeDeleteHooks = append(templatesBlueprintWeaponBeforeDeleteHooks, templatesBlueprintWeaponHook)
	case boil.AfterDeleteHook:
		templatesBlueprintWeaponAfterDeleteHooks = append(templatesBlueprintWeaponAfterDeleteHooks, templatesBlueprintWeaponHook)
	case boil.BeforeUpsertHook:
		templatesBlueprintWeaponBeforeUpsertHooks = append(templatesBlueprintWeaponBeforeUpsertHooks, templatesBlueprintWeaponHook)
	case boil.AfterUpsertHook:
		templatesBlueprintWeaponAfterUpsertHooks = append(templatesBlueprintWeaponAfterUpsertHooks, templatesBlueprintWeaponHook)
	}
}

// One returns a single templatesBlueprintWeapon record from the query.
func (q templatesBlueprintWeaponQuery) One(exec boil.Executor) (*TemplatesBlueprintWeapon, error) {
	o := &TemplatesBlueprintWeapon{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: failed to execute a one query for templates_blueprint_weapons")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TemplatesBlueprintWeapon records from the query.
func (q templatesBlueprintWeaponQuery) All(exec boil.Executor) (TemplatesBlueprintWeaponSlice, error) {
	var o []*TemplatesBlueprintWeapon

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "boiler: failed to assign all query results to TemplatesBlueprintWeapon slice")
	}

	if len(templatesBlueprintWeaponAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TemplatesBlueprintWeapon records in the query.
func (q templatesBlueprintWeaponQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to count templates_blueprint_weapons rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q templatesBlueprintWeaponQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "boiler: failed to check if templates_blueprint_weapons exists")
	}

	return count > 0, nil
}

// BlueprintWeapon pointed to by the foreign key.
func (o *TemplatesBlueprintWeapon) BlueprintWeapon(mods ...qm.QueryMod) blueprintWeaponQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.BlueprintWeaponID),
		qmhelper.WhereIsNull("deleted_at"),
	}

	queryMods = append(queryMods, mods...)

	query := BlueprintWeapons(queryMods...)
	queries.SetFrom(query.Query, "\"blueprint_weapons\"")

	return query
}

// Template pointed to by the foreign key.
func (o *TemplatesBlueprintWeapon) Template(mods ...qm.QueryMod) templateQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.TemplateID),
		qmhelper.WhereIsNull("deleted_at"),
	}

	queryMods = append(queryMods, mods...)

	query := Templates(queryMods...)
	queries.SetFrom(query.Query, "\"templates\"")

	return query
}

// LoadBlueprintWeapon allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (templatesBlueprintWeaponL) LoadBlueprintWeapon(e boil.Executor, singular bool, maybeTemplatesBlueprintWeapon interface{}, mods queries.Applicator) error {
	var slice []*TemplatesBlueprintWeapon
	var object *TemplatesBlueprintWeapon

	if singular {
		object = maybeTemplatesBlueprintWeapon.(*TemplatesBlueprintWeapon)
	} else {
		slice = *maybeTemplatesBlueprintWeapon.(*[]*TemplatesBlueprintWeapon)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &templatesBlueprintWeaponR{}
		}
		args = append(args, object.BlueprintWeaponID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &templatesBlueprintWeaponR{}
			}

			for _, a := range args {
				if a == obj.BlueprintWeaponID {
					continue Outer
				}
			}

			args = append(args, obj.BlueprintWeaponID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`blueprint_weapons`),
		qm.WhereIn(`blueprint_weapons.id in ?`, args...),
		qmhelper.WhereIsNull(`blueprint_weapons.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load BlueprintWeapon")
	}

	var resultSlice []*BlueprintWeapon
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice BlueprintWeapon")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for blueprint_weapons")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for blueprint_weapons")
	}

	if len(templatesBlueprintWeaponAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.BlueprintWeapon = foreign
		if foreign.R == nil {
			foreign.R = &blueprintWeaponR{}
		}
		foreign.R.TemplatesBlueprintWeapons = append(foreign.R.TemplatesBlueprintWeapons, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BlueprintWeaponID == foreign.ID {
				local.R.BlueprintWeapon = foreign
				if foreign.R == nil {
					foreign.R = &blueprintWeaponR{}
				}
				foreign.R.TemplatesBlueprintWeapons = append(foreign.R.TemplatesBlueprintWeapons, local)
				break
			}
		}
	}

	return nil
}

// LoadTemplate allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (templatesBlueprintWeaponL) LoadTemplate(e boil.Executor, singular bool, maybeTemplatesBlueprintWeapon interface{}, mods queries.Applicator) error {
	var slice []*TemplatesBlueprintWeapon
	var object *TemplatesBlueprintWeapon

	if singular {
		object = maybeTemplatesBlueprintWeapon.(*TemplatesBlueprintWeapon)
	} else {
		slice = *maybeTemplatesBlueprintWeapon.(*[]*TemplatesBlueprintWeapon)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &templatesBlueprintWeaponR{}
		}
		args = append(args, object.TemplateID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &templatesBlueprintWeaponR{}
			}

			for _, a := range args {
				if a == obj.TemplateID {
					continue Outer
				}
			}

			args = append(args, obj.TemplateID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`templates`),
		qm.WhereIn(`templates.id in ?`, args...),
		qmhelper.WhereIsNull(`templates.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Template")
	}

	var resultSlice []*Template
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Template")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for templates")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for templates")
	}

	if len(templatesBlueprintWeaponAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Template = foreign
		if foreign.R == nil {
			foreign.R = &templateR{}
		}
		foreign.R.TemplatesBlueprintWeapons = append(foreign.R.TemplatesBlueprintWeapons, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TemplateID == foreign.ID {
				local.R.Template = foreign
				if foreign.R == nil {
					foreign.R = &templateR{}
				}
				foreign.R.TemplatesBlueprintWeapons = append(foreign.R.TemplatesBlueprintWeapons, local)
				break
			}
		}
	}

	return nil
}

// SetBlueprintWeapon of the templatesBlueprintWeapon to the related item.
// Sets o.R.BlueprintWeapon to related.
// Adds o to related.R.TemplatesBlueprintWeapons.
func (o *TemplatesBlueprintWeapon) SetBlueprintWeapon(exec boil.Executor, insert bool, related *BlueprintWeapon) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"templates_blueprint_weapons\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"blueprint_weapon_id"}),
		strmangle.WhereClause("\"", "\"", 2, templatesBlueprintWeaponPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BlueprintWeaponID = related.ID
	if o.R == nil {
		o.R = &templatesBlueprintWeaponR{
			BlueprintWeapon: related,
		}
	} else {
		o.R.BlueprintWeapon = related
	}

	if related.R == nil {
		related.R = &blueprintWeaponR{
			TemplatesBlueprintWeapons: TemplatesBlueprintWeaponSlice{o},
		}
	} else {
		related.R.TemplatesBlueprintWeapons = append(related.R.TemplatesBlueprintWeapons, o)
	}

	return nil
}

// SetTemplate of the templatesBlueprintWeapon to the related item.
// Sets o.R.Template to related.
// Adds o to related.R.TemplatesBlueprintWeapons.
func (o *TemplatesBlueprintWeapon) SetTemplate(exec boil.Executor, insert bool, related *Template) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"templates_blueprint_weapons\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"template_id"}),
		strmangle.WhereClause("\"", "\"", 2, templatesBlueprintWeaponPrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TemplateID = related.ID
	if o.R == nil {
		o.R = &templatesBlueprintWeaponR{
			Template: related,
		}
	} else {
		o.R.Template = related
	}

	if related.R == nil {
		related.R = &templateR{
			TemplatesBlueprintWeapons: TemplatesBlueprintWeaponSlice{o},
		}
	} else {
		related.R.TemplatesBlueprintWeapons = append(related.R.TemplatesBlueprintWeapons, o)
	}

	return nil
}

// TemplatesBlueprintWeapons retrieves all the records using an executor.
func TemplatesBlueprintWeapons(mods ...qm.QueryMod) templatesBlueprintWeaponQuery {
	mods = append(mods, qm.From("\"templates_blueprint_weapons\""), qmhelper.WhereIsNull("\"templates_blueprint_weapons\".\"deleted_at\""))
	return templatesBlueprintWeaponQuery{NewQuery(mods...)}
}

// FindTemplatesBlueprintWeapon retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTemplatesBlueprintWeapon(exec boil.Executor, iD string, selectCols ...string) (*TemplatesBlueprintWeapon, error) {
	templatesBlueprintWeaponObj := &TemplatesBlueprintWeapon{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"templates_blueprint_weapons\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, templatesBlueprintWeaponObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: unable to select from templates_blueprint_weapons")
	}

	if err = templatesBlueprintWeaponObj.doAfterSelectHooks(exec); err != nil {
		return templatesBlueprintWeaponObj, err
	}

	return templatesBlueprintWeaponObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TemplatesBlueprintWeapon) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no templates_blueprint_weapons provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.UpdatedAt.IsZero() {
		o.UpdatedAt = currTime
	}
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(templatesBlueprintWeaponColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	templatesBlueprintWeaponInsertCacheMut.RLock()
	cache, cached := templatesBlueprintWeaponInsertCache[key]
	templatesBlueprintWeaponInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			templatesBlueprintWeaponAllColumns,
			templatesBlueprintWeaponColumnsWithDefault,
			templatesBlueprintWeaponColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(templatesBlueprintWeaponType, templatesBlueprintWeaponMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(templatesBlueprintWeaponType, templatesBlueprintWeaponMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"templates_blueprint_weapons\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"templates_blueprint_weapons\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "boiler: unable to insert into templates_blueprint_weapons")
	}

	if !cached {
		templatesBlueprintWeaponInsertCacheMut.Lock()
		templatesBlueprintWeaponInsertCache[key] = cache
		templatesBlueprintWeaponInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// Update uses an executor to update the TemplatesBlueprintWeapon.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TemplatesBlueprintWeapon) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime

	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	templatesBlueprintWeaponUpdateCacheMut.RLock()
	cache, cached := templatesBlueprintWeaponUpdateCache[key]
	templatesBlueprintWeaponUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			templatesBlueprintWeaponAllColumns,
			templatesBlueprintWeaponPrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("boiler: unable to update templates_blueprint_weapons, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"templates_blueprint_weapons\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, templatesBlueprintWeaponPrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(templatesBlueprintWeaponType, templatesBlueprintWeaponMapping, append(wl, templatesBlueprintWeaponPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update templates_blueprint_weapons row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by update for templates_blueprint_weapons")
	}

	if !cached {
		templatesBlueprintWeaponUpdateCacheMut.Lock()
		templatesBlueprintWeaponUpdateCache[key] = cache
		templatesBlueprintWeaponUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q templatesBlueprintWeaponQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all for templates_blueprint_weapons")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected for templates_blueprint_weapons")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TemplatesBlueprintWeaponSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("boiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), templatesBlueprintWeaponPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"templates_blueprint_weapons\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, templatesBlueprintWeaponPrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all in templatesBlueprintWeapon slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected all in update all templatesBlueprintWeapon")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TemplatesBlueprintWeapon) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no templates_blueprint_weapons provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(templatesBlueprintWeaponColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	templatesBlueprintWeaponUpsertCacheMut.RLock()
	cache, cached := templatesBlueprintWeaponUpsertCache[key]
	templatesBlueprintWeaponUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			templatesBlueprintWeaponAllColumns,
			templatesBlueprintWeaponColumnsWithDefault,
			templatesBlueprintWeaponColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			templatesBlueprintWeaponAllColumns,
			templatesBlueprintWeaponPrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("boiler: unable to upsert templates_blueprint_weapons, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(templatesBlueprintWeaponPrimaryKeyColumns))
			copy(conflict, templatesBlueprintWeaponPrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"templates_blueprint_weapons\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(templatesBlueprintWeaponType, templatesBlueprintWeaponMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(templatesBlueprintWeaponType, templatesBlueprintWeaponMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "boiler: unable to upsert templates_blueprint_weapons")
	}

	if !cached {
		templatesBlueprintWeaponUpsertCacheMut.Lock()
		templatesBlueprintWeaponUpsertCache[key] = cache
		templatesBlueprintWeaponUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// Delete deletes a single TemplatesBlueprintWeapon record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TemplatesBlueprintWeapon) Delete(exec boil.Executor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("boiler: no TemplatesBlueprintWeapon provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), templatesBlueprintWeaponPrimaryKeyMapping)
		sql = "DELETE FROM \"templates_blueprint_weapons\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"templates_blueprint_weapons\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(templatesBlueprintWeaponType, templatesBlueprintWeaponMapping, append(wl, templatesBlueprintWeaponPrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete from templates_blueprint_weapons")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by delete for templates_blueprint_weapons")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q templatesBlueprintWeaponQuery) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("boiler: no templatesBlueprintWeaponQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from templates_blueprint_weapons")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for templates_blueprint_weapons")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TemplatesBlueprintWeaponSlice) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(templatesBlueprintWeaponBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), templatesBlueprintWeaponPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"templates_blueprint_weapons\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, templatesBlueprintWeaponPrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), templatesBlueprintWeaponPrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"templates_blueprint_weapons\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, templatesBlueprintWeaponPrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from templatesBlueprintWeapon slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for templates_blueprint_weapons")
	}

	if len(templatesBlueprintWeaponAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TemplatesBlueprintWeapon) Reload(exec boil.Executor) error {
	ret, err := FindTemplatesBlueprintWeapon(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TemplatesBlueprintWeaponSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TemplatesBlueprintWeaponSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), templatesBlueprintWeaponPrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"templates_blueprint_weapons\".* FROM \"templates_blueprint_weapons\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, templatesBlueprintWeaponPrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to reload all in TemplatesBlueprintWeaponSlice")
	}

	*o = slice

	return nil
}

// TemplatesBlueprintWeaponExists checks if the TemplatesBlueprintWeapon row exists.
func TemplatesBlueprintWeaponExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"templates_blueprint_weapons\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "boiler: unable to check if templates_blueprint_weapons exists")
	}

	return exists, nil
}
