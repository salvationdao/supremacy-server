// Code generated by SQLBoiler 4.8.6 (https://github.com/volatiletech/sqlboiler). DO NOT EDIT.
// This file is meant to be re-generated in place and/or deleted at any time.

package boiler

import (
	"database/sql"
	"fmt"
	"reflect"
	"strconv"
	"strings"
	"sync"
	"time"

	"github.com/friendsofgo/errors"
	"github.com/volatiletech/null/v8"
	"github.com/volatiletech/sqlboiler/v4/boil"
	"github.com/volatiletech/sqlboiler/v4/queries"
	"github.com/volatiletech/sqlboiler/v4/queries/qm"
	"github.com/volatiletech/sqlboiler/v4/queries/qmhelper"
	"github.com/volatiletech/strmangle"
)

// TemplatesBlueprintModule is an object representing the database table.
type TemplatesBlueprintModule struct {
	ID                string    `boiler:"id" boil:"id" json:"id" toml:"id" yaml:"id"`
	BlueprintModuleID string    `boiler:"blueprint_module_id" boil:"blueprint_module_id" json:"blueprintModuleID" toml:"blueprintModuleID" yaml:"blueprintModuleID"`
	TemplateID        string    `boiler:"template_id" boil:"template_id" json:"templateID" toml:"templateID" yaml:"templateID"`
	DeletedAt         null.Time `boiler:"deleted_at" boil:"deleted_at" json:"deletedAt,omitempty" toml:"deletedAt" yaml:"deletedAt,omitempty"`
	UpdatedAt         time.Time `boiler:"updated_at" boil:"updated_at" json:"updatedAt" toml:"updatedAt" yaml:"updatedAt"`
	CreatedAt         time.Time `boiler:"created_at" boil:"created_at" json:"createdAt" toml:"createdAt" yaml:"createdAt"`

	R *templatesBlueprintModuleR `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
	L templatesBlueprintModuleL  `boiler:"-" boil:"-" json:"-" toml:"-" yaml:"-"`
}

var TemplatesBlueprintModuleColumns = struct {
	ID                string
	BlueprintModuleID string
	TemplateID        string
	DeletedAt         string
	UpdatedAt         string
	CreatedAt         string
}{
	ID:                "id",
	BlueprintModuleID: "blueprint_module_id",
	TemplateID:        "template_id",
	DeletedAt:         "deleted_at",
	UpdatedAt:         "updated_at",
	CreatedAt:         "created_at",
}

var TemplatesBlueprintModuleTableColumns = struct {
	ID                string
	BlueprintModuleID string
	TemplateID        string
	DeletedAt         string
	UpdatedAt         string
	CreatedAt         string
}{
	ID:                "templates_blueprint_modules.id",
	BlueprintModuleID: "templates_blueprint_modules.blueprint_module_id",
	TemplateID:        "templates_blueprint_modules.template_id",
	DeletedAt:         "templates_blueprint_modules.deleted_at",
	UpdatedAt:         "templates_blueprint_modules.updated_at",
	CreatedAt:         "templates_blueprint_modules.created_at",
}

// Generated where

var TemplatesBlueprintModuleWhere = struct {
	ID                whereHelperstring
	BlueprintModuleID whereHelperstring
	TemplateID        whereHelperstring
	DeletedAt         whereHelpernull_Time
	UpdatedAt         whereHelpertime_Time
	CreatedAt         whereHelpertime_Time
}{
	ID:                whereHelperstring{field: "\"templates_blueprint_modules\".\"id\""},
	BlueprintModuleID: whereHelperstring{field: "\"templates_blueprint_modules\".\"blueprint_module_id\""},
	TemplateID:        whereHelperstring{field: "\"templates_blueprint_modules\".\"template_id\""},
	DeletedAt:         whereHelpernull_Time{field: "\"templates_blueprint_modules\".\"deleted_at\""},
	UpdatedAt:         whereHelpertime_Time{field: "\"templates_blueprint_modules\".\"updated_at\""},
	CreatedAt:         whereHelpertime_Time{field: "\"templates_blueprint_modules\".\"created_at\""},
}

// TemplatesBlueprintModuleRels is where relationship names are stored.
var TemplatesBlueprintModuleRels = struct {
	BlueprintModule string
	Template        string
}{
	BlueprintModule: "BlueprintModule",
	Template:        "Template",
}

// templatesBlueprintModuleR is where relationships are stored.
type templatesBlueprintModuleR struct {
	BlueprintModule *BlueprintModule `boiler:"BlueprintModule" boil:"BlueprintModule" json:"BlueprintModule" toml:"BlueprintModule" yaml:"BlueprintModule"`
	Template        *Template        `boiler:"Template" boil:"Template" json:"Template" toml:"Template" yaml:"Template"`
}

// NewStruct creates a new relationship struct
func (*templatesBlueprintModuleR) NewStruct() *templatesBlueprintModuleR {
	return &templatesBlueprintModuleR{}
}

// templatesBlueprintModuleL is where Load methods for each relationship are stored.
type templatesBlueprintModuleL struct{}

var (
	templatesBlueprintModuleAllColumns            = []string{"id", "blueprint_module_id", "template_id", "deleted_at", "updated_at", "created_at"}
	templatesBlueprintModuleColumnsWithoutDefault = []string{"blueprint_module_id", "template_id"}
	templatesBlueprintModuleColumnsWithDefault    = []string{"id", "deleted_at", "updated_at", "created_at"}
	templatesBlueprintModulePrimaryKeyColumns     = []string{"id"}
	templatesBlueprintModuleGeneratedColumns      = []string{}
)

type (
	// TemplatesBlueprintModuleSlice is an alias for a slice of pointers to TemplatesBlueprintModule.
	// This should almost always be used instead of []TemplatesBlueprintModule.
	TemplatesBlueprintModuleSlice []*TemplatesBlueprintModule
	// TemplatesBlueprintModuleHook is the signature for custom TemplatesBlueprintModule hook methods
	TemplatesBlueprintModuleHook func(boil.Executor, *TemplatesBlueprintModule) error

	templatesBlueprintModuleQuery struct {
		*queries.Query
	}
)

// Cache for insert, update and upsert
var (
	templatesBlueprintModuleType                 = reflect.TypeOf(&TemplatesBlueprintModule{})
	templatesBlueprintModuleMapping              = queries.MakeStructMapping(templatesBlueprintModuleType)
	templatesBlueprintModulePrimaryKeyMapping, _ = queries.BindMapping(templatesBlueprintModuleType, templatesBlueprintModuleMapping, templatesBlueprintModulePrimaryKeyColumns)
	templatesBlueprintModuleInsertCacheMut       sync.RWMutex
	templatesBlueprintModuleInsertCache          = make(map[string]insertCache)
	templatesBlueprintModuleUpdateCacheMut       sync.RWMutex
	templatesBlueprintModuleUpdateCache          = make(map[string]updateCache)
	templatesBlueprintModuleUpsertCacheMut       sync.RWMutex
	templatesBlueprintModuleUpsertCache          = make(map[string]insertCache)
)

var (
	// Force time package dependency for automated UpdatedAt/CreatedAt.
	_ = time.Second
	// Force qmhelper dependency for where clause generation (which doesn't
	// always happen)
	_ = qmhelper.Where
)

var templatesBlueprintModuleAfterSelectHooks []TemplatesBlueprintModuleHook

var templatesBlueprintModuleBeforeInsertHooks []TemplatesBlueprintModuleHook
var templatesBlueprintModuleAfterInsertHooks []TemplatesBlueprintModuleHook

var templatesBlueprintModuleBeforeUpdateHooks []TemplatesBlueprintModuleHook
var templatesBlueprintModuleAfterUpdateHooks []TemplatesBlueprintModuleHook

var templatesBlueprintModuleBeforeDeleteHooks []TemplatesBlueprintModuleHook
var templatesBlueprintModuleAfterDeleteHooks []TemplatesBlueprintModuleHook

var templatesBlueprintModuleBeforeUpsertHooks []TemplatesBlueprintModuleHook
var templatesBlueprintModuleAfterUpsertHooks []TemplatesBlueprintModuleHook

// doAfterSelectHooks executes all "after Select" hooks.
func (o *TemplatesBlueprintModule) doAfterSelectHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintModuleAfterSelectHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeInsertHooks executes all "before insert" hooks.
func (o *TemplatesBlueprintModule) doBeforeInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintModuleBeforeInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterInsertHooks executes all "after Insert" hooks.
func (o *TemplatesBlueprintModule) doAfterInsertHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintModuleAfterInsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpdateHooks executes all "before Update" hooks.
func (o *TemplatesBlueprintModule) doBeforeUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintModuleBeforeUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpdateHooks executes all "after Update" hooks.
func (o *TemplatesBlueprintModule) doAfterUpdateHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintModuleAfterUpdateHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeDeleteHooks executes all "before Delete" hooks.
func (o *TemplatesBlueprintModule) doBeforeDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintModuleBeforeDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterDeleteHooks executes all "after Delete" hooks.
func (o *TemplatesBlueprintModule) doAfterDeleteHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintModuleAfterDeleteHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doBeforeUpsertHooks executes all "before Upsert" hooks.
func (o *TemplatesBlueprintModule) doBeforeUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintModuleBeforeUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// doAfterUpsertHooks executes all "after Upsert" hooks.
func (o *TemplatesBlueprintModule) doAfterUpsertHooks(exec boil.Executor) (err error) {
	for _, hook := range templatesBlueprintModuleAfterUpsertHooks {
		if err := hook(exec, o); err != nil {
			return err
		}
	}

	return nil
}

// AddTemplatesBlueprintModuleHook registers your hook function for all future operations.
func AddTemplatesBlueprintModuleHook(hookPoint boil.HookPoint, templatesBlueprintModuleHook TemplatesBlueprintModuleHook) {
	switch hookPoint {
	case boil.AfterSelectHook:
		templatesBlueprintModuleAfterSelectHooks = append(templatesBlueprintModuleAfterSelectHooks, templatesBlueprintModuleHook)
	case boil.BeforeInsertHook:
		templatesBlueprintModuleBeforeInsertHooks = append(templatesBlueprintModuleBeforeInsertHooks, templatesBlueprintModuleHook)
	case boil.AfterInsertHook:
		templatesBlueprintModuleAfterInsertHooks = append(templatesBlueprintModuleAfterInsertHooks, templatesBlueprintModuleHook)
	case boil.BeforeUpdateHook:
		templatesBlueprintModuleBeforeUpdateHooks = append(templatesBlueprintModuleBeforeUpdateHooks, templatesBlueprintModuleHook)
	case boil.AfterUpdateHook:
		templatesBlueprintModuleAfterUpdateHooks = append(templatesBlueprintModuleAfterUpdateHooks, templatesBlueprintModuleHook)
	case boil.BeforeDeleteHook:
		templatesBlueprintModuleBeforeDeleteHooks = append(templatesBlueprintModuleBeforeDeleteHooks, templatesBlueprintModuleHook)
	case boil.AfterDeleteHook:
		templatesBlueprintModuleAfterDeleteHooks = append(templatesBlueprintModuleAfterDeleteHooks, templatesBlueprintModuleHook)
	case boil.BeforeUpsertHook:
		templatesBlueprintModuleBeforeUpsertHooks = append(templatesBlueprintModuleBeforeUpsertHooks, templatesBlueprintModuleHook)
	case boil.AfterUpsertHook:
		templatesBlueprintModuleAfterUpsertHooks = append(templatesBlueprintModuleAfterUpsertHooks, templatesBlueprintModuleHook)
	}
}

// One returns a single templatesBlueprintModule record from the query.
func (q templatesBlueprintModuleQuery) One(exec boil.Executor) (*TemplatesBlueprintModule, error) {
	o := &TemplatesBlueprintModule{}

	queries.SetLimit(q.Query, 1)

	err := q.Bind(nil, exec, o)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: failed to execute a one query for templates_blueprint_modules")
	}

	if err := o.doAfterSelectHooks(exec); err != nil {
		return o, err
	}

	return o, nil
}

// All returns all TemplatesBlueprintModule records from the query.
func (q templatesBlueprintModuleQuery) All(exec boil.Executor) (TemplatesBlueprintModuleSlice, error) {
	var o []*TemplatesBlueprintModule

	err := q.Bind(nil, exec, &o)
	if err != nil {
		return nil, errors.Wrap(err, "boiler: failed to assign all query results to TemplatesBlueprintModule slice")
	}

	if len(templatesBlueprintModuleAfterSelectHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterSelectHooks(exec); err != nil {
				return o, err
			}
		}
	}

	return o, nil
}

// Count returns the count of all TemplatesBlueprintModule records in the query.
func (q templatesBlueprintModuleQuery) Count(exec boil.Executor) (int64, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to count templates_blueprint_modules rows")
	}

	return count, nil
}

// Exists checks if the row exists in the table.
func (q templatesBlueprintModuleQuery) Exists(exec boil.Executor) (bool, error) {
	var count int64

	queries.SetSelect(q.Query, nil)
	queries.SetCount(q.Query)
	queries.SetLimit(q.Query, 1)

	err := q.Query.QueryRow(exec).Scan(&count)
	if err != nil {
		return false, errors.Wrap(err, "boiler: failed to check if templates_blueprint_modules exists")
	}

	return count > 0, nil
}

// BlueprintModule pointed to by the foreign key.
func (o *TemplatesBlueprintModule) BlueprintModule(mods ...qm.QueryMod) blueprintModuleQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.BlueprintModuleID),
		qmhelper.WhereIsNull("deleted_at"),
	}

	queryMods = append(queryMods, mods...)

	query := BlueprintModules(queryMods...)
	queries.SetFrom(query.Query, "\"blueprint_modules\"")

	return query
}

// Template pointed to by the foreign key.
func (o *TemplatesBlueprintModule) Template(mods ...qm.QueryMod) templateQuery {
	queryMods := []qm.QueryMod{
		qm.Where("\"id\" = ?", o.TemplateID),
		qmhelper.WhereIsNull("deleted_at"),
	}

	queryMods = append(queryMods, mods...)

	query := Templates(queryMods...)
	queries.SetFrom(query.Query, "\"templates\"")

	return query
}

// LoadBlueprintModule allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (templatesBlueprintModuleL) LoadBlueprintModule(e boil.Executor, singular bool, maybeTemplatesBlueprintModule interface{}, mods queries.Applicator) error {
	var slice []*TemplatesBlueprintModule
	var object *TemplatesBlueprintModule

	if singular {
		object = maybeTemplatesBlueprintModule.(*TemplatesBlueprintModule)
	} else {
		slice = *maybeTemplatesBlueprintModule.(*[]*TemplatesBlueprintModule)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &templatesBlueprintModuleR{}
		}
		args = append(args, object.BlueprintModuleID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &templatesBlueprintModuleR{}
			}

			for _, a := range args {
				if a == obj.BlueprintModuleID {
					continue Outer
				}
			}

			args = append(args, obj.BlueprintModuleID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`blueprint_modules`),
		qm.WhereIn(`blueprint_modules.id in ?`, args...),
		qmhelper.WhereIsNull(`blueprint_modules.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load BlueprintModule")
	}

	var resultSlice []*BlueprintModule
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice BlueprintModule")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for blueprint_modules")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for blueprint_modules")
	}

	if len(templatesBlueprintModuleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.BlueprintModule = foreign
		if foreign.R == nil {
			foreign.R = &blueprintModuleR{}
		}
		foreign.R.TemplatesBlueprintModules = append(foreign.R.TemplatesBlueprintModules, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.BlueprintModuleID == foreign.ID {
				local.R.BlueprintModule = foreign
				if foreign.R == nil {
					foreign.R = &blueprintModuleR{}
				}
				foreign.R.TemplatesBlueprintModules = append(foreign.R.TemplatesBlueprintModules, local)
				break
			}
		}
	}

	return nil
}

// LoadTemplate allows an eager lookup of values, cached into the
// loaded structs of the objects. This is for an N-1 relationship.
func (templatesBlueprintModuleL) LoadTemplate(e boil.Executor, singular bool, maybeTemplatesBlueprintModule interface{}, mods queries.Applicator) error {
	var slice []*TemplatesBlueprintModule
	var object *TemplatesBlueprintModule

	if singular {
		object = maybeTemplatesBlueprintModule.(*TemplatesBlueprintModule)
	} else {
		slice = *maybeTemplatesBlueprintModule.(*[]*TemplatesBlueprintModule)
	}

	args := make([]interface{}, 0, 1)
	if singular {
		if object.R == nil {
			object.R = &templatesBlueprintModuleR{}
		}
		args = append(args, object.TemplateID)

	} else {
	Outer:
		for _, obj := range slice {
			if obj.R == nil {
				obj.R = &templatesBlueprintModuleR{}
			}

			for _, a := range args {
				if a == obj.TemplateID {
					continue Outer
				}
			}

			args = append(args, obj.TemplateID)

		}
	}

	if len(args) == 0 {
		return nil
	}

	query := NewQuery(
		qm.From(`templates`),
		qm.WhereIn(`templates.id in ?`, args...),
		qmhelper.WhereIsNull(`templates.deleted_at`),
	)
	if mods != nil {
		mods.Apply(query)
	}

	results, err := query.Query(e)
	if err != nil {
		return errors.Wrap(err, "failed to eager load Template")
	}

	var resultSlice []*Template
	if err = queries.Bind(results, &resultSlice); err != nil {
		return errors.Wrap(err, "failed to bind eager loaded slice Template")
	}

	if err = results.Close(); err != nil {
		return errors.Wrap(err, "failed to close results of eager load for templates")
	}
	if err = results.Err(); err != nil {
		return errors.Wrap(err, "error occurred during iteration of eager loaded relations for templates")
	}

	if len(templatesBlueprintModuleAfterSelectHooks) != 0 {
		for _, obj := range resultSlice {
			if err := obj.doAfterSelectHooks(e); err != nil {
				return err
			}
		}
	}

	if len(resultSlice) == 0 {
		return nil
	}

	if singular {
		foreign := resultSlice[0]
		object.R.Template = foreign
		if foreign.R == nil {
			foreign.R = &templateR{}
		}
		foreign.R.TemplatesBlueprintModules = append(foreign.R.TemplatesBlueprintModules, object)
		return nil
	}

	for _, local := range slice {
		for _, foreign := range resultSlice {
			if local.TemplateID == foreign.ID {
				local.R.Template = foreign
				if foreign.R == nil {
					foreign.R = &templateR{}
				}
				foreign.R.TemplatesBlueprintModules = append(foreign.R.TemplatesBlueprintModules, local)
				break
			}
		}
	}

	return nil
}

// SetBlueprintModule of the templatesBlueprintModule to the related item.
// Sets o.R.BlueprintModule to related.
// Adds o to related.R.TemplatesBlueprintModules.
func (o *TemplatesBlueprintModule) SetBlueprintModule(exec boil.Executor, insert bool, related *BlueprintModule) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"templates_blueprint_modules\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"blueprint_module_id"}),
		strmangle.WhereClause("\"", "\"", 2, templatesBlueprintModulePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.BlueprintModuleID = related.ID
	if o.R == nil {
		o.R = &templatesBlueprintModuleR{
			BlueprintModule: related,
		}
	} else {
		o.R.BlueprintModule = related
	}

	if related.R == nil {
		related.R = &blueprintModuleR{
			TemplatesBlueprintModules: TemplatesBlueprintModuleSlice{o},
		}
	} else {
		related.R.TemplatesBlueprintModules = append(related.R.TemplatesBlueprintModules, o)
	}

	return nil
}

// SetTemplate of the templatesBlueprintModule to the related item.
// Sets o.R.Template to related.
// Adds o to related.R.TemplatesBlueprintModules.
func (o *TemplatesBlueprintModule) SetTemplate(exec boil.Executor, insert bool, related *Template) error {
	var err error
	if insert {
		if err = related.Insert(exec, boil.Infer()); err != nil {
			return errors.Wrap(err, "failed to insert into foreign table")
		}
	}

	updateQuery := fmt.Sprintf(
		"UPDATE \"templates_blueprint_modules\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, []string{"template_id"}),
		strmangle.WhereClause("\"", "\"", 2, templatesBlueprintModulePrimaryKeyColumns),
	)
	values := []interface{}{related.ID, o.ID}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, updateQuery)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	if _, err = exec.Exec(updateQuery, values...); err != nil {
		return errors.Wrap(err, "failed to update local table")
	}

	o.TemplateID = related.ID
	if o.R == nil {
		o.R = &templatesBlueprintModuleR{
			Template: related,
		}
	} else {
		o.R.Template = related
	}

	if related.R == nil {
		related.R = &templateR{
			TemplatesBlueprintModules: TemplatesBlueprintModuleSlice{o},
		}
	} else {
		related.R.TemplatesBlueprintModules = append(related.R.TemplatesBlueprintModules, o)
	}

	return nil
}

// TemplatesBlueprintModules retrieves all the records using an executor.
func TemplatesBlueprintModules(mods ...qm.QueryMod) templatesBlueprintModuleQuery {
	mods = append(mods, qm.From("\"templates_blueprint_modules\""), qmhelper.WhereIsNull("\"templates_blueprint_modules\".\"deleted_at\""))
	return templatesBlueprintModuleQuery{NewQuery(mods...)}
}

// FindTemplatesBlueprintModule retrieves a single record by ID with an executor.
// If selectCols is empty Find will return all columns.
func FindTemplatesBlueprintModule(exec boil.Executor, iD string, selectCols ...string) (*TemplatesBlueprintModule, error) {
	templatesBlueprintModuleObj := &TemplatesBlueprintModule{}

	sel := "*"
	if len(selectCols) > 0 {
		sel = strings.Join(strmangle.IdentQuoteSlice(dialect.LQ, dialect.RQ, selectCols), ",")
	}
	query := fmt.Sprintf(
		"select %s from \"templates_blueprint_modules\" where \"id\"=$1 and \"deleted_at\" is null", sel,
	)

	q := queries.Raw(query, iD)

	err := q.Bind(nil, exec, templatesBlueprintModuleObj)
	if err != nil {
		if errors.Cause(err) == sql.ErrNoRows {
			return nil, sql.ErrNoRows
		}
		return nil, errors.Wrap(err, "boiler: unable to select from templates_blueprint_modules")
	}

	if err = templatesBlueprintModuleObj.doAfterSelectHooks(exec); err != nil {
		return templatesBlueprintModuleObj, err
	}

	return templatesBlueprintModuleObj, nil
}

// Insert a single record using an executor.
// See boil.Columns.InsertColumnSet documentation to understand column list inference for inserts.
func (o *TemplatesBlueprintModule) Insert(exec boil.Executor, columns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no templates_blueprint_modules provided for insertion")
	}

	var err error
	currTime := time.Now().In(boil.GetLocation())

	if o.UpdatedAt.IsZero() {
		o.UpdatedAt = currTime
	}
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeInsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(templatesBlueprintModuleColumnsWithDefault, o)

	key := makeCacheKey(columns, nzDefaults)
	templatesBlueprintModuleInsertCacheMut.RLock()
	cache, cached := templatesBlueprintModuleInsertCache[key]
	templatesBlueprintModuleInsertCacheMut.RUnlock()

	if !cached {
		wl, returnColumns := columns.InsertColumnSet(
			templatesBlueprintModuleAllColumns,
			templatesBlueprintModuleColumnsWithDefault,
			templatesBlueprintModuleColumnsWithoutDefault,
			nzDefaults,
		)

		cache.valueMapping, err = queries.BindMapping(templatesBlueprintModuleType, templatesBlueprintModuleMapping, wl)
		if err != nil {
			return err
		}
		cache.retMapping, err = queries.BindMapping(templatesBlueprintModuleType, templatesBlueprintModuleMapping, returnColumns)
		if err != nil {
			return err
		}
		if len(wl) != 0 {
			cache.query = fmt.Sprintf("INSERT INTO \"templates_blueprint_modules\" (\"%s\") %%sVALUES (%s)%%s", strings.Join(wl, "\",\""), strmangle.Placeholders(dialect.UseIndexPlaceholders, len(wl), 1, 1))
		} else {
			cache.query = "INSERT INTO \"templates_blueprint_modules\" %sDEFAULT VALUES%s"
		}

		var queryOutput, queryReturning string

		if len(cache.retMapping) != 0 {
			queryReturning = fmt.Sprintf(" RETURNING \"%s\"", strings.Join(returnColumns, "\",\""))
		}

		cache.query = fmt.Sprintf(cache.query, queryOutput, queryReturning)
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}

	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(queries.PtrsFromMapping(value, cache.retMapping)...)
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}

	if err != nil {
		return errors.Wrap(err, "boiler: unable to insert into templates_blueprint_modules")
	}

	if !cached {
		templatesBlueprintModuleInsertCacheMut.Lock()
		templatesBlueprintModuleInsertCache[key] = cache
		templatesBlueprintModuleInsertCacheMut.Unlock()
	}

	return o.doAfterInsertHooks(exec)
}

// Update uses an executor to update the TemplatesBlueprintModule.
// See boil.Columns.UpdateColumnSet documentation to understand column list inference for updates.
// Update does not automatically update the record in case of default values. Use .Reload() to refresh the records.
func (o *TemplatesBlueprintModule) Update(exec boil.Executor, columns boil.Columns) (int64, error) {
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime

	var err error
	if err = o.doBeforeUpdateHooks(exec); err != nil {
		return 0, err
	}
	key := makeCacheKey(columns, nil)
	templatesBlueprintModuleUpdateCacheMut.RLock()
	cache, cached := templatesBlueprintModuleUpdateCache[key]
	templatesBlueprintModuleUpdateCacheMut.RUnlock()

	if !cached {
		wl := columns.UpdateColumnSet(
			templatesBlueprintModuleAllColumns,
			templatesBlueprintModulePrimaryKeyColumns,
		)

		if !columns.IsWhitelist() {
			wl = strmangle.SetComplement(wl, []string{"created_at"})
		}
		if len(wl) == 0 {
			return 0, errors.New("boiler: unable to update templates_blueprint_modules, could not build whitelist")
		}

		cache.query = fmt.Sprintf("UPDATE \"templates_blueprint_modules\" SET %s WHERE %s",
			strmangle.SetParamNames("\"", "\"", 1, wl),
			strmangle.WhereClause("\"", "\"", len(wl)+1, templatesBlueprintModulePrimaryKeyColumns),
		)
		cache.valueMapping, err = queries.BindMapping(templatesBlueprintModuleType, templatesBlueprintModuleMapping, append(wl, templatesBlueprintModulePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
	}

	values := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), cache.valueMapping)

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, values)
	}
	var result sql.Result
	result, err = exec.Exec(cache.query, values...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update templates_blueprint_modules row")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by update for templates_blueprint_modules")
	}

	if !cached {
		templatesBlueprintModuleUpdateCacheMut.Lock()
		templatesBlueprintModuleUpdateCache[key] = cache
		templatesBlueprintModuleUpdateCacheMut.Unlock()
	}

	return rowsAff, o.doAfterUpdateHooks(exec)
}

// UpdateAll updates all rows with the specified column values.
func (q templatesBlueprintModuleQuery) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	queries.SetUpdate(q.Query, cols)

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all for templates_blueprint_modules")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected for templates_blueprint_modules")
	}

	return rowsAff, nil
}

// UpdateAll updates all rows with the specified column values, using an executor.
func (o TemplatesBlueprintModuleSlice) UpdateAll(exec boil.Executor, cols M) (int64, error) {
	ln := int64(len(o))
	if ln == 0 {
		return 0, nil
	}

	if len(cols) == 0 {
		return 0, errors.New("boiler: update all requires at least one column argument")
	}

	colNames := make([]string, len(cols))
	args := make([]interface{}, len(cols))

	i := 0
	for name, value := range cols {
		colNames[i] = name
		args[i] = value
		i++
	}

	// Append all of the primary key values for each column
	for _, obj := range o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), templatesBlueprintModulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := fmt.Sprintf("UPDATE \"templates_blueprint_modules\" SET %s WHERE %s",
		strmangle.SetParamNames("\"", "\"", 1, colNames),
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), len(colNames)+1, templatesBlueprintModulePrimaryKeyColumns, len(o)))

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to update all in templatesBlueprintModule slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to retrieve rows affected all in update all templatesBlueprintModule")
	}
	return rowsAff, nil
}

// Upsert attempts an insert using an executor, and does an update or ignore on conflict.
// See boil.Columns documentation for how to properly use updateColumns and insertColumns.
func (o *TemplatesBlueprintModule) Upsert(exec boil.Executor, updateOnConflict bool, conflictColumns []string, updateColumns, insertColumns boil.Columns) error {
	if o == nil {
		return errors.New("boiler: no templates_blueprint_modules provided for upsert")
	}
	currTime := time.Now().In(boil.GetLocation())

	o.UpdatedAt = currTime
	if o.CreatedAt.IsZero() {
		o.CreatedAt = currTime
	}

	if err := o.doBeforeUpsertHooks(exec); err != nil {
		return err
	}

	nzDefaults := queries.NonZeroDefaultSet(templatesBlueprintModuleColumnsWithDefault, o)

	// Build cache key in-line uglily - mysql vs psql problems
	buf := strmangle.GetBuffer()
	if updateOnConflict {
		buf.WriteByte('t')
	} else {
		buf.WriteByte('f')
	}
	buf.WriteByte('.')
	for _, c := range conflictColumns {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(updateColumns.Kind))
	for _, c := range updateColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	buf.WriteString(strconv.Itoa(insertColumns.Kind))
	for _, c := range insertColumns.Cols {
		buf.WriteString(c)
	}
	buf.WriteByte('.')
	for _, c := range nzDefaults {
		buf.WriteString(c)
	}
	key := buf.String()
	strmangle.PutBuffer(buf)

	templatesBlueprintModuleUpsertCacheMut.RLock()
	cache, cached := templatesBlueprintModuleUpsertCache[key]
	templatesBlueprintModuleUpsertCacheMut.RUnlock()

	var err error

	if !cached {
		insert, ret := insertColumns.InsertColumnSet(
			templatesBlueprintModuleAllColumns,
			templatesBlueprintModuleColumnsWithDefault,
			templatesBlueprintModuleColumnsWithoutDefault,
			nzDefaults,
		)

		update := updateColumns.UpdateColumnSet(
			templatesBlueprintModuleAllColumns,
			templatesBlueprintModulePrimaryKeyColumns,
		)

		if updateOnConflict && len(update) == 0 {
			return errors.New("boiler: unable to upsert templates_blueprint_modules, could not build update column list")
		}

		conflict := conflictColumns
		if len(conflict) == 0 {
			conflict = make([]string, len(templatesBlueprintModulePrimaryKeyColumns))
			copy(conflict, templatesBlueprintModulePrimaryKeyColumns)
		}
		cache.query = buildUpsertQueryPostgres(dialect, "\"templates_blueprint_modules\"", updateOnConflict, ret, update, conflict, insert)

		cache.valueMapping, err = queries.BindMapping(templatesBlueprintModuleType, templatesBlueprintModuleMapping, insert)
		if err != nil {
			return err
		}
		if len(ret) != 0 {
			cache.retMapping, err = queries.BindMapping(templatesBlueprintModuleType, templatesBlueprintModuleMapping, ret)
			if err != nil {
				return err
			}
		}
	}

	value := reflect.Indirect(reflect.ValueOf(o))
	vals := queries.ValuesFromMapping(value, cache.valueMapping)
	var returns []interface{}
	if len(cache.retMapping) != 0 {
		returns = queries.PtrsFromMapping(value, cache.retMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, cache.query)
		fmt.Fprintln(boil.DebugWriter, vals)
	}
	if len(cache.retMapping) != 0 {
		err = exec.QueryRow(cache.query, vals...).Scan(returns...)
		if err == sql.ErrNoRows {
			err = nil // Postgres doesn't return anything when there's no update
		}
	} else {
		_, err = exec.Exec(cache.query, vals...)
	}
	if err != nil {
		return errors.Wrap(err, "boiler: unable to upsert templates_blueprint_modules")
	}

	if !cached {
		templatesBlueprintModuleUpsertCacheMut.Lock()
		templatesBlueprintModuleUpsertCache[key] = cache
		templatesBlueprintModuleUpsertCacheMut.Unlock()
	}

	return o.doAfterUpsertHooks(exec)
}

// Delete deletes a single TemplatesBlueprintModule record with an executor.
// Delete will match against the primary key column to find the record to delete.
func (o *TemplatesBlueprintModule) Delete(exec boil.Executor, hardDelete bool) (int64, error) {
	if o == nil {
		return 0, errors.New("boiler: no TemplatesBlueprintModule provided for delete")
	}

	if err := o.doBeforeDeleteHooks(exec); err != nil {
		return 0, err
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), templatesBlueprintModulePrimaryKeyMapping)
		sql = "DELETE FROM \"templates_blueprint_modules\" WHERE \"id\"=$1"
	} else {
		currTime := time.Now().In(boil.GetLocation())
		o.DeletedAt = null.TimeFrom(currTime)
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"templates_blueprint_modules\" SET %s WHERE \"id\"=$2",
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		valueMapping, err := queries.BindMapping(templatesBlueprintModuleType, templatesBlueprintModuleMapping, append(wl, templatesBlueprintModulePrimaryKeyColumns...))
		if err != nil {
			return 0, err
		}
		args = queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(o)), valueMapping)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args...)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete from templates_blueprint_modules")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by delete for templates_blueprint_modules")
	}

	if err := o.doAfterDeleteHooks(exec); err != nil {
		return 0, err
	}

	return rowsAff, nil
}

// DeleteAll deletes all matching rows.
func (q templatesBlueprintModuleQuery) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if q.Query == nil {
		return 0, errors.New("boiler: no templatesBlueprintModuleQuery provided for delete all")
	}

	if hardDelete {
		queries.SetDelete(q.Query)
	} else {
		currTime := time.Now().In(boil.GetLocation())
		queries.SetUpdate(q.Query, M{"deleted_at": currTime})
	}

	result, err := q.Query.Exec(exec)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from templates_blueprint_modules")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for templates_blueprint_modules")
	}

	return rowsAff, nil
}

// DeleteAll deletes all rows in the slice, using an executor.
func (o TemplatesBlueprintModuleSlice) DeleteAll(exec boil.Executor, hardDelete bool) (int64, error) {
	if len(o) == 0 {
		return 0, nil
	}

	if len(templatesBlueprintModuleBeforeDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doBeforeDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	var (
		sql  string
		args []interface{}
	)
	if hardDelete {
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), templatesBlueprintModulePrimaryKeyMapping)
			args = append(args, pkeyArgs...)
		}
		sql = "DELETE FROM \"templates_blueprint_modules\" WHERE " +
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, templatesBlueprintModulePrimaryKeyColumns, len(o))
	} else {
		currTime := time.Now().In(boil.GetLocation())
		for _, obj := range o {
			pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), templatesBlueprintModulePrimaryKeyMapping)
			args = append(args, pkeyArgs...)
			obj.DeletedAt = null.TimeFrom(currTime)
		}
		wl := []string{"deleted_at"}
		sql = fmt.Sprintf("UPDATE \"templates_blueprint_modules\" SET %s WHERE "+
			strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 2, templatesBlueprintModulePrimaryKeyColumns, len(o)),
			strmangle.SetParamNames("\"", "\"", 1, wl),
		)
		args = append([]interface{}{currTime}, args...)
	}

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, args)
	}
	result, err := exec.Exec(sql, args...)
	if err != nil {
		return 0, errors.Wrap(err, "boiler: unable to delete all from templatesBlueprintModule slice")
	}

	rowsAff, err := result.RowsAffected()
	if err != nil {
		return 0, errors.Wrap(err, "boiler: failed to get rows affected by deleteall for templates_blueprint_modules")
	}

	if len(templatesBlueprintModuleAfterDeleteHooks) != 0 {
		for _, obj := range o {
			if err := obj.doAfterDeleteHooks(exec); err != nil {
				return 0, err
			}
		}
	}

	return rowsAff, nil
}

// Reload refetches the object from the database
// using the primary keys with an executor.
func (o *TemplatesBlueprintModule) Reload(exec boil.Executor) error {
	ret, err := FindTemplatesBlueprintModule(exec, o.ID)
	if err != nil {
		return err
	}

	*o = *ret
	return nil
}

// ReloadAll refetches every row with matching primary key column values
// and overwrites the original object slice with the newly updated slice.
func (o *TemplatesBlueprintModuleSlice) ReloadAll(exec boil.Executor) error {
	if o == nil || len(*o) == 0 {
		return nil
	}

	slice := TemplatesBlueprintModuleSlice{}
	var args []interface{}
	for _, obj := range *o {
		pkeyArgs := queries.ValuesFromMapping(reflect.Indirect(reflect.ValueOf(obj)), templatesBlueprintModulePrimaryKeyMapping)
		args = append(args, pkeyArgs...)
	}

	sql := "SELECT \"templates_blueprint_modules\".* FROM \"templates_blueprint_modules\" WHERE " +
		strmangle.WhereClauseRepeated(string(dialect.LQ), string(dialect.RQ), 1, templatesBlueprintModulePrimaryKeyColumns, len(*o)) +
		"and \"deleted_at\" is null"

	q := queries.Raw(sql, args...)

	err := q.Bind(nil, exec, &slice)
	if err != nil {
		return errors.Wrap(err, "boiler: unable to reload all in TemplatesBlueprintModuleSlice")
	}

	*o = slice

	return nil
}

// TemplatesBlueprintModuleExists checks if the TemplatesBlueprintModule row exists.
func TemplatesBlueprintModuleExists(exec boil.Executor, iD string) (bool, error) {
	var exists bool
	sql := "select exists(select 1 from \"templates_blueprint_modules\" where \"id\"=$1 and \"deleted_at\" is null limit 1)"

	if boil.DebugMode {
		fmt.Fprintln(boil.DebugWriter, sql)
		fmt.Fprintln(boil.DebugWriter, iD)
	}
	row := exec.QueryRow(sql, iD)

	err := row.Scan(&exists)
	if err != nil {
		return false, errors.Wrap(err, "boiler: unable to check if templates_blueprint_modules exists")
	}

	return exists, nil
}
